# 2026-08-19 工作日志

## 每日数字保鲜校验（自动化 · 05:55 CST）
- `verify_live_numbers.py` 只读运行 → **RED**：真值 729 实体 / 19 品类 / 325 OSS，线上 `api/data.json`+`api/entities.json` 仍 710 实体 / 19 品类（实体缺 19）。17/17 页 + 2/3 接口失配。
- 根因：本地工作树有大批未提交数据改动（8 个新品类文件 untracked + 大量 `api/*.json` modified），线上跑旧提交 = 部署漂移，非假红。
- 已写入 `ops/results/_NEEDS_USER.md` 新待办（部署漂移，P0，需用户确认真值后 `git commit` + `node scripts/deploy.mjs`）。自动化只读、未改线上、未触发部署。
- 注：`llms.txt` 仍写「710 实体 / 2026-08-05」，4 篇文章页仍嵌「11 类别」陈旧文案，待部署前一并修。
