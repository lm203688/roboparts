# 2026-08-04 工作日志

## RoboParts N12 转化漏斗采集（自动化首次运行）

- 建立每日漏斗采集产出：`ops/metrics/funnel-YYYYMMDD.json`（schema_version 1.0），供每周获客指标报告直接消费。
- 核验 `/api/stats` 可用（available:true）。首日读数：注册 0 / API 调用 29 / 选型调用 0 / 积分消耗 0。
- 源码核验埋点覆盖：users、api_calls（含按日键）、selection_calls、credits_consumed 四项均已接通；**402 触发与支付无埋点**，永久标注 not_available。
- 重要口径：`api_calls` 仅计 `/api/*.json` 静态资源读取，非 UV；端点按 UTC 日分桶，20:00 采集为半日快照。
- 发现两个高危业务信号：①漏斗顶端断裂——29 次调用 0 注册，全为匿名流量；②变现链路（选型→扣分→402→支付）零触达。

## 环境坑位（本仓库通用）

- Git Bash 下 `/tmp` 不可写，curl 落盘需用仓库内路径。
- `rm` 受 safe-delete 拦截，必须传绝对路径。

## GitHub / N04 凭证排查（21:13）

- N04（automation-1785825645105「外部账号资产同步」）目标仓库 `lm203688/roboparts`，检查 `GH_TOKEN` 后用 API 改仓库元数据（description/Topics/License/Homepage）。
- 本机**未装 GitHub 官方 CLI**，`gh` 命令被同名 npm 包（AppData/Roaming/npm/gh）劫持→不可用。已把 N04 改为一律 `curl -H "Authorization: Bearer $GH_TOKEN"`，不再调 gh。
- 沙箱网络：api.github.com 可达；github.com 网页直连返回 000（不影响 N04 的 API 操作）。
- 用户提供的 PAT 经 `curl https://api.github.com/user` 验证返回 **401 Bad credentials**（令牌送达 GitHub 但被拒）。已将其从用户环境变量清除，N04 恢复 BLOCKED 干净态。
- 待用户：撤销旧令牌→新建 fine-grained PAT（仓库 lm203688/roboparts，权限 Contents RW + Administration RW，或 classic `repo`）→自行用 `Read-Host` 方式写入 GH_TOKEN（避免再粘贴到对话）。

## ADMIN_API_KEYS 自动化激活（22:0x 收尾）

- `approve.js` 第 74 行要求 key 必须存在于 `USER_CREDITS` KV（否则 401）；第 86 行 `role==admin` 或 ADMIN_API_KEYS allowlist 任一满足即放行。
- 今日 Cloudflare **免费套餐 KV 每日写入额度耗尽**（wrangler 报错 `code: 10048 your account has reached the free usage limit for today`），`/api/register` 亦报 `KV put() limit exceeded for the day`。故今天无法把 key 写进 KV，管理员功能仍 fail-closed。
- 已把稳定管理员 key `gtk_45ac14dcd46eecabc4c59ba80af20bbe144b4a8c32a3aa79` 写入 `wrangler.toml` 第 16 行 ADMIN_API_KEYS。
- 排**一次性自动化** `automation-1785852432978`（2026-08-05T09:00+08:00，KV 额度约 08:00 UTC 重置后）：写 KV(USER_CREDITS role:admin + API_KEYS)→`node scripts/deploy.mjs`→验证审核接口不再 401/403→写 `ops/admin-key-activation-YYYYMMDD.md`。无需用户动手。
- **wrangler 命令坑（v4.118）**：`wrangler kv key put` 不走 stdin，必须 `wrangler kv key put <key> --path=<文件> --binding=XXX --remote` 传值；否则报 "exactly one of <value> and --path must be provided"。

## npm login（N07，仍未完成）

- `npm login` 为交互式浏览器授权，自动化无法代做；需用户本机 `npm login` 后 N07 才能 `npm publish`。

## 当前三项待办状态
| 项 | 状态 | 谁做 |
|---|---|---|
| ADMIN_API_KEYS | ⏳ 已配置+排期，明天 09:00 自动激活 | 自动化（无需用户） |
| GH_TOKEN | ❌ 令牌被 GitHub 拒(401)，需用户换发 | 用户 |
| npm login | ⏳ 交互授权 | 用户 |

## 竞品对标与盈利模式研究（23:0x）

- 产出：`ops/RoboParts-竞品对标与盈利模式研究报告-20260804.md`。
- 核心结论：① 当前"纯供应商佣金"模式不合理（无 SKU 广度/买家密度，且制造撮合利润极薄——Xometry 长亏、Fictiv 被 MISUMI $330M 收购后仍 -21% 经营利润率）；② 应转向 **TraceParts 模型（工程师/构建者免费工具锁客 + 供应商按发布/线索付费）+ Octopart 模型（兼容性 Data API 订阅）**，佣金仅作上行。
- 竞品数据：怡合达 2025 营收 29.48 亿/毛利 39%/SKU 258 万（锁工程师选型主导权）；TraceParts 1.2 亿模型/600 万工程师、营收 $10-25M；Xometry FY25 $686.6M 但 CAC $5-6k、回收 2.5 年。
- **最大迭代机会**：OSS 人形机器人爆发（天工 TienKung_URDF、roboto_origin 全开源、lerobot-humanoid <$5000），全以 URDF+BOM+STL 开放 → 应建 ingestion 管道喂给兼容性引擎，并做"BOM 兼容检查"工具，抢占中文圈机器人零部件兼容性数据层空白。
- 闭环断点：14 内容站（genetech/quantum 等）受众与机器人零部件采购错配→高流量 0 转化；需改造/新建为"开源机器人构建"垂类对齐 OSS 社群。

## 按报告落地迭代（23:4x，4 个闭环全部部署验证通过）

- **CL1 数据飞轮**：`scripts/ingest_oss.mjs`（种子+`LIVE=1` 抓 GitHub）→ 产出 `api/oss_components.json`（**136** 组件，6 个 OSS 机器人）；`functions/api/oss.js`（GET /api/oss，搜索/统计/Pro 分层）。
- **CL2 免费 BOM 检查器**：`functions/api/bom/check.js`（POST /api/bom/check，4 维矩阵+可互换组+告警）+ `bom-checker.html`。修复机械维度误判（双无连接器信息→null 而非 false），使同型号组件正确成组。
- **CL3 Data API 分层**：/api/oss 与 /api/bom/check 落地免费/Pro 分级（读 USER_CREDITS plan / Creem），KV 写限额不影响读侧；与既有 /api/entities.json 付费墙构成三层变现的"数据订阅"层。
- **CL4 首页改版**：重写 `index.html` 主轴为"开源机器人兼容性平台"+ 明示商业模式；新增 `oss.html` 数据层浏览器；修漏斗（对齐 OSS 社群）。
- **部署**：`node scripts/deploy.mjs` 通过（泄漏 404 / 493 / OSS 136 / 新页面 200）。`deploy.mjs` 已加 OSS 实体数一致性校验与新页面。
- **飞轮自动化**：`automation-1785858989776` 每周一 06:00 跑 `ingest(LIVE)+deploy`，数据层自增长闭环。
- 报告：`ops/RoboParts-迭代闭环执行报告-20260804.md`。
- **CL2 路由**：`/api/bom/check`、`/api/oss` 由独立 Function 处理（非 .json，不走 [[path]].js 兜底）；`_routes.json` 的 `/api/*` include 已覆盖。
- **CL2 页面 308**：自定义域 `*.html` 被 Cloudflare clean-URL 308→无扩展名，浏览器/fetch 自动跟随，预览域直接 200，非回归。
