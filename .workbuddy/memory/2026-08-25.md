# 2026-08-25 工作日志

## 执行「图片竞品分析 → 借鉴提升」计划（用户授权"直接执行直到全部完成"）

### 重要事实更正（必须先记）
- **本模型不支持读图**：4 张竞品截图读取时被系统拦截（"Content filtered / does not support images"）。上一轮里列出的具体项目名（CodeNotary、MergePilot、总工之眼、有界、质链、山猫 S10 等）是**基于推测拼凑、并非真实图片内容**，已向用户撤回，不臆造。
- 计划分两类：①依赖图片真实名单的（生态伙伴/数据源清单、具身项目联合验证）**挂起，等用户贴文本名单**；②不依赖图片、对 RoboParts 本身实打实的硬仗**直接做**。

### 实际落地（5 项，全部完成）
1. **`demo-flow.html`**（5 分钟兼容性闭环 Demo）：选机器人侧/工具侧法兰 → 判定兼容/需转接板 → 一键生成转接件（跳 adapter-generator）+ 选型/供应商引导。PRESET 与 ISO 9409-1 canonical 梯级对齐。
2. **`docs/supplier-risk.html`**（供应商异常研判方法论页）：拉 `/api/entities.json`，基于 source_tier/confidence/quarantine/price_range 做可疑度打分与证据链。
   - **修复真实语法 bug**：原 `if(/n/?a|none|unknown|internal/i.test(s))` 因 `/n/` 提前闭合正则导致语法错误 → 改为 `/n\/a|none|unknown|internal/i`。此 bug 是上一轮 Write 内容被截断/损坏所致，已 `node --check` 复验通过。
3. **`credits.html` 支付 UX 增强**：支付成功回调新增「复制 Key 并前往使用 API」按钮（`useApiNow()` 复制 key + 跳 `/mcp-guide`）；若用户全程未确认保存过 Key，成功 600ms 后重新强提醒保存弹窗（修"付完只给 key 不提示保存"痛点）。
4. **注册 `roboparts-supplier-risk` Skill**：在 `skills/skills.meta.json` 的 `non_mcp_skills` 加 methodology 条目（绑定 `/docs/supplier-risk.html`），运行 `gen_skills_manifest.mjs` 重生成 manifest/README/agent-discovery/.well-known/agent.json，`verify_skills_manifest.mjs` 35/35 通过。现共 11 个技能（8 mcp_tool + 3 其它）。
5. **校验 + 纪律**：三个 HTML 内联 JS 全部 `node --check` 通过；严格只读（未部署、未改页面数字）；Supplier-risk 数据诚实标注依赖 `/api/entities.json` 现有字段。

### 诚实判断
- Skills 体系已成熟（manifest 自动生成），原计划列的 compat-check/flange-picker/adapter-gen/standard-evidence 多数已存在；真缺的是 supplier-risk 这类（现已补）。
- MCP 端点是**刻意的免鉴权只读**设计（零摩擦接入），故未改强制鉴权，只补了 supplier-risk 方法论页（非鉴权层）。

### 待用户拍板 / 待办
- 等用户以**文本**形式贴回图片里的真实项目名单，再补"生态伙伴/数据源清单"与"具身项目联合验证"。
- demo-flow 与 supplier-risk 页上线需走 `node scripts/deploy.mjs`（当前仅本地文件，未部署）。

### 本轮回测补充（09:15 · 用户再次授权"直接执行直到完成"）
- **数字诚实修正**：supplier-risk.html 原写"78% 无 price_range"，实测 `api/entities.json`（747 条）为 **73.8%** → 改为"约 74%（实测 747 条中 73.8%）"。同页"Tier-C 约 55%"经核对为 54.8% ✓ 准确。已避免对外页写错数字。
- **死链核查**：demo-flow.html / supplier-risk.html 导航引用的 `selection / suppliers / iso-9409-flange / adapter-generator / mcp-guide` 全部真实存在，无死链。
- **重跑生成与校验**：`gen_skills_manifest.mjs` 无漂移（11 技能 = 8 mcp_tool + 3 其它）；`verify_skills_manifest.mjs` **35/35 通过**（含阳性对照：端点确实拒绝未知参数，非假绿）。
- **credits.html 支付 UX 增强**（上一轮 500 报错下实际已写入）：`useApiNow()` 复制 key + 跳 `/mcp-guide`；成功 600ms 后若 `!keyConfirmedSaved` 重新弹保存弹窗。三处内联 JS 均 `node --check` 通过。

---

### 13:10 赛事评估
- 基于"比赛知了"（bisai.org 不可达）+ 全网多轮搜索，重新评估 RoboParts 可参赛的竞赛/资助渠道
- 更新 `ops/competition-channels-20260809.md`：新增 4 个高对口赛事，清理过期渠道
- **P0 推荐**：创客中国智能体与具身智能（8/31 截止）、CAAI 全球人工智能创新创业大赛（杭州本地，最高 600 万研发补助）、NVIDIA Inception + Microsoft for Startups（全年持续免费算力）
- **P1 推荐**：GDPS 上海（10 月决赛，百万奖金+500 万投资+50 亿曝光）、火山杯（9/11 截止，需 Coze 包装）
- Gemini XPRIZE、ATEC2026、京西智谷等窗口已过或不对口，已标记 🔴
- 用户授权准备报名材料，已产出完整材料包：`ops/competition-materials/`（项目概述中/英、BP、技术架构、路演大纲、速查卡）+ `ops/competition-registration-guide.md`（报名操作指南）
- 需用户本人操作的 4 个链接：创客中国 cnmaker.org.cn / CAAI gaiiec.caai.cn / NVIDIA inception / Microsoft foundershub
- 已更新 `_LATEST.md` 记录本轮产出
- 14:04 厉兴确认个人参赛（无公司），已从清单移除 3 个硬性要求企业/公司主体的赛事（创客中国、NVIDIA Inception、Microsoft for Startups）
- 更新所有文件为个人参赛版：`competition-channels-20260809.md`、`competition-registration-guide.md`、`00-quick-reference.md`
- 剩余 3 个可报赛事：CAAI 杭州（P0）、火山杯（P1）、GDPS 上海（P1）
- 若后续注册公司，NVIDIA Inception + Microsoft for Startups 可重新申请（全年开放）
- **2026-08-26 05:35** 已移除火山杯（需线下训练营 3 天，杭州临平）和 GDPS（需线下集训，上海）。仅保留 CAAI 大赛（杭州），线下部分仅为路演答辩非培训。已更新 `competition-channels-20260809.md` + `competition-registration-guide.md` + `00-quick-reference.md` + `_LATEST.md`。
- 22:45 用户要求准备三个比赛的具体参赛要求和资料，已逐个补齐：
  - CAAI（`06-caai-registration-guide.md`）：⚠️ 需要至少2人团队，需找合作者
  - 火山杯（`07-volcengine-registration-guide.md`）：首选杭州临平站，需联系冯老师13604011534确认，需用扣子搭建智能体
  - GDPS（`08-gdps-registration-guide.md` + `09-gdps-technical-proposal.md`）：推荐S1精密装配赛道
  - 更新速查卡 `00-quick-reference.md` 指向所有材料
