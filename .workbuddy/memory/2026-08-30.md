# RoboParts 工作日志 · 2026-08-30

## 08:20–08:42 轮次：copilot 复探修复 + harness 自污染修复 + P3 闭环

### 触发
用户「继续完成任务」。连接器列表显示 agent-mail 已从 disconnected→connected，遂复探。

### 复探发现（真缺陷，非回归引入）
- `/api/copilot` 429 已消失，但形态变了：4 探 3×200、1×60s 硬超时；成功样本延迟 1.3s/2.9s/56.4s（严重且不稳）。
- **领域幻觉**：问「ISO 9409-1 是什么」，模型答「规定焊接接头坡口几何形状的国际标准」= **ISO 9692**。平台建在 ISO 9409-1 法兰上，copilot 答错核心标准。

### 修复（均不替用户决策后端）
- `functions/api/copilot.js`：① `fetch` 加 25s `AbortController` 超时，超时改返 504 `agnes_timeout`（不再 502 误导、不再 60s 卡 worker）；② 注入 grounding system prompt（同源拉 `/api/data.json` 取真实 meta + canonical 法兰梯级 `A40=4×M6 … A250=8×M20`，明确「ISO 9409-1 ≠ 焊接坡口」）。部署后实测同一问题答**正确**、3.0s。
- 回归 harness 自污染 `_SUMMARY`：`_run('--hour','00','--allow-empty')` 在新的一天（08-30）因 00:00 档位无内容会**写出空壳行**又被自己棘轮拦下（EXIT=1）。`digest_due.py` 加可选 `--now`（YYYY-MM-DDTHH:MM）；阳性测试改指已有实质内容的 `2026-08-29 00:00`，确定性拒绝（rc3+棘轮）且**不再落盘**；删被误写空壳行 `2026-08-30 00:00 | 修复:无 | 提升:无 | 待办:无`。

### P3 闭环
- `mcp__agent-mail__GetMe` 现返回真实别名 `alxealxe3329@agent.qq.com`（含 `mail:send`），disconnected→connected。
- 但这是 **agent.qq.com 服务别名**，非个人收件箱；仓库无任何出站邮件消费方 ⇒ 「绑定」仅通道可用，未启用自动发信。飞轮汇报维持「对话说汇报」。

### 顺序铁律（再次验证）
提交 `1557d53`（3 文件）→ 增量 `push-gitdata.mjs`（远端 `e11aa071`、tree `6a081ef6` 一致）→ `deploy.mjs`（`97685df3.robotparts-924.pages.dev`，四段全过）→ `verify_live_numbers.py`（768/20/325，17 页+3 接口 0 未核验）→ 回归全绿 EXIT=0。

### 两个纪律点（已固化进 MEMORY.md）
- **日报过期须补发，不能盖 skipped**：`--stamp` 在 digest 逾期（>24h）时拒签 skipped（「到期还跳过＝漏报」）。本轮机读 `_last_digest.txt` 停在 08-28 17:06、已逾期 39.5h ⇒ 必须写 `_DAILY_DIGEST-20260830.md` + 更新标记后 `--stamp` 才落 `issued`。这是闸门「倒逼出日报」的设计，不是 bug。
- **回归阳性测试会污染历史**：任何「写文件做断言」的阳性测试，在新的一天会因目标档位无内容而写出空壳——测试必须指向确定存在的内容或先造内容，否则自相矛盾。

### 待办仍 2 项（AI 无法代劳）
1. **P0** 真实 BOM 机械接口声明，声明率仍 **1.52%**（分母 395=768−373），未靠代码抬升。
2. **P2** `/api/copilot` 后端选择：Agnes 不稳定+会答错核心标准（续费不解决质量）；切 ECS 网关 `150.158.119.19:8420/v1` **本机直连不可达**（curl exit 7，需用户确认在线+给密钥）；或维护中降级。

## 09:00–09:12 轮次：copilot 多后端路由 + 自动化后端选型 + AU 借鉴分析

### 触发
用户「继续完善项目；copilot 后端走哪条路用你自动化运行的逻辑看看哪个最合适；accelerated understanding 神经算子通用物理大模型借鉴分析」。

### 后端选型（自动化探针 + 线上实测，非记忆）
- Agnes `/v1/models`：`http=401 time=0.35s` → 端点存活、仅缺鉴权。
- ECS `150.158.119.19:8420`：agent 机 `http=000 exit7` 不可达（Cloudflare edge 可达性未知，需用户确认在线）。
- 线上经路由实测：Agnes `200/11.7s`、ISO 9409-1 答对（A100=6×M10、PCD=100mm、区分 ISO 9692 焊接坡口）。
- 落地：`copilot.js` 改**多后端路由**（ECS 优先 / Agnes 次之 / 全败返回 200 友好降级），保留 grounding + 25s 超时。今日跑 Agnes；用户配 `ECS_API_KEY` 即零代码切自托管。决策矩阵见 `ops/results/copilot-backend-decision-20260830.md`。

### AU 神经算子物理大模型借鉴（诚实、不附和）
- 事实锚定：AU 2026-08-25 成立，Anima Anandkumar（Fourier Neural Operator 开创者）+ Benedikt Jenik；`au-physics-v1` 上 OpenRouter；神经算子非 Transformer；单次 5T「场数据点」（非 token）；目标芯片热/电磁、天气、地质、机器人接触力。
- 判：机制**真实合理**（函数空间→函数空间映射、分辨率无关，CFD 基准快于数值解）；但属模型/资产生成层，与 RoboParts 数据/兼容性基础设施层**无挂载点**。
- 可借鉴：① 方法论镜像（结构化兼容数据=领域基础，模型/agent 是消费端）② phase-2「适配器受力评级」用神经算子做 FEA 快速代理（需真实几何数据 + 云 GPU，本机无 NVIDIA GPU 不能自托管）。与 1.52% 声明率无关，不解决数据缺口。

### 纪律
- 回归 1 项红（日报声明时刻 08 早于最新小时报告 09）→ 重盖 `DIGEST-CLAIM: skipped @09:09` 修复，复跑应全绿。
- GitHub 远端推送 `e6edf82` 本会话被阻断（credential store 非交互态返回空、无 `GITHUB_TOKEN`），本地已提交待重放；部署照常（已提交，仅远端滞后）。

### 待办仍 2 项（同前，AI 无法代劳）
1. **P0** 真实 BOM 机械接口声明（声明率 1.52%，数据缺口）。
2. **P2** `/api/copilot` 后端升级：路由已落地；用户点亮 ECS 网关 + 设 `ECS_API_KEY` 即自动优先。

## 09:22– 轮次：Perceptron Isaac 0.5 借鉴分析 + 推进未完成项

### 触发
用户「Perceptron 本周正式推出 Isaac 0.5（36B 动态稀疏 MoE，统一多模态视频理解/具身推理/机器人控制），对我们项目有什么借鉴？另外继续之前没有完成的任务」。

### 事实核验（关键：先证真伪，不盲从）
- 经 5+ 独立源交叉印证（TechCrunch 2026-08-26、BusinessWire 2026-08-28、官方博客、HF/GitHub）确认 **Isaac 0.5 真实存在**，非用户臆测。Perceptron AI（Bellevue，ex-Meta FAIR 创立，$21M Bessemer）。
- 规格：36B 动态 MoE / 2.5B 激活每 token；100 万小时视频 + 10 万小时机器人轨迹 + 3T token；开放权重 + 训练/推理代码（LeRobot）。
- 核心硬货：**数据缩放律**——通用视频 1k→1M 小时，使达同动作损失所需遥操作从 ~5900h 降到 28h（**210×**）。架构创新 **Null Experts**（每 token 自选 0~8 专家，算力随难度伸缩）。
- **诚实注记**：「开放权重 ≠ 无条件商业许可」，模型卡条款须先核验（RoboParts 是商业产品）。

### 借鉴分析结论（不附和、分可落地性）
- **跨层**：Isaac 0.5=模型/控制层（生成动作），RoboParts=数据/兼容基础设施层，**无直接挂载点**，不能复用架构。
- **D1【最高价值·可落地】**：把 RoboParts 实体 schema 升格为版本化带校验「类型契约」（类比 Isaac 0.5 开源的 mHarmony）。**本轮已实现**（见下）。
- **D2【战略定位】**：RoboParts 作为「具身基础模型的结构化兼容供给层」——让 Isaac 0.5 这类模型在选型/适配器决策时消费 RoboParts 事实。呼应既有 phase-2 + AU 分析。
- **D3【框架】**：为「声明覆盖率」定义自己的数据混合 scaling law（210× 是框架借鉴，非技术移植；RoboParts 真值在标准定义的离散空间，不可靠像素自监督补）。
- **D4【远期】**：Null Experts→兼容性查询动态算力路由（当前 768 实体不需要）。
- **不可借鉴/劝阻**：本机无 NVIDIA GPU（Radeon 8060S ~4.2GB）不能自托管/微调；第三方不可控重演 Agnes 老坑；护城河是数据+标准情报+agent API 而非机器人脑。
- 文档：`ops/results/perceptron-isaac-05-borrowing-20260830.md`。

### 推进未完成项（诚实结论：仅剩项均用户凭证阻塞）
- **GitHub 远端推送 `e6edf82` 仍被阻断**：token 探针确认无 `GITHUB_TOKEN`/env、`git credential helper` 未配置、`~/.git-credentials` 不存在、`gh` CLI 损坏。push 脚本 `TOKEN=process.env.GITHUB_TOKEN` 或 `git credential fill`，二者皆空 ⇒ 需用户提供 token 才能增量重放。
- **P0 真实 BOM**：仍数据缺口，需用户提供带出处的真实零件（UR5e/Franka FR3/Kinova Gen3 的 ISO 9409-1 法兰）。
- **P2 ECS 网关**：路由已落地，需用户点亮网关 + 设 `ECS_API_KEY`。

### 自主可做的改进（D1 已落地）
- 新增 `scripts/schema_contract.py`：SCHEMA_VERSION=1.0.0 + CORE_REQUIRED(id/rp_id/entity_kind/name/category，全量 100% 存在) + MI_STATUS_ENUM；`validate()` 返回违规列表。
- `regression.py` 新增 `layer_schema_contract()` 闸门（call 在 layer4 后），缺核心字段或 status 越界即判红——**根治历史「写错层被静默丢弃」类缺陷**（PROTO-010/XFA-017）。
- 待回归全绿后提交（main 仓本地；远端待 token）。

### 20260830-22 GitHub 远端推送阻塞闭环
- 用户 22:03 提供 `github_pat_…` 细粒度 PAT。本机配 `git config --global credential.helper store` + 写 `~/.git-credentials`（不入仓/不入前端）。
- `node scripts/push-gitdata.mjs` 增量重放 `e6edf82`(copilot 路由)+`f31e202`(schema 契约) → 远端 `aec3eb50`；脚本校验「远端 tree fb5d9265 与本地 HEAD 完全一致」PASS。
- 飞轮推送通道恢复，后续提交免重复粘贴 token。_LATEST/_NEEDS_USER/_SUMMARY 已更新，ops 仓待提交。
