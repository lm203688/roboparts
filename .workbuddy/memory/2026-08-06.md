
## 02:00 飞轮第 12 次运行 · 进入官方 MCP Registry

- 撤销此前「提交官方 MCP Registry 需人工鉴权」的误判：Registry 的 HTTP 域名验证只要求在自家域名托管 `/.well-known/mcp-registry-auth`，全程零人工。`cc.roboparts/roboparts@1.1.0` 已发布并独立查证 active/isLatest。
- 私钥存放于仓库外 `C:\Users\xing\.roboparts-secrets\`（换机需备份）；回归 L1.17 锁死"仓库出现 .pem/.key 即阻断"。
- 发现并修复同型断点：已注册的 MCP 端点在 llms.txt 与 18 个被抓页面中未提及，已下沉到 onboarding_block 单一真相源。
- 新增 L1.17 共 19 条断言，7 类违规注入全部可阻断；2 次部署全绿；push bb94e22。

## 03:35 飞轮第 13 次运行

- 修复 `scripts/read_metrics.py`：`mcp:*` 埋点写入 KV 但读取端无对应分段被静默丢弃，连续三轮误读为"无 Agent 接入"。补 `mcp:` 分段 + 未归类兜底。
- 新增回归 `layer1_18()`（L1.18，4 条断言），锁死"埋点↔读取端必须成对"。4 类违规注入全部确认可阻断。commit `8f53b13`。
- 首个可归因 AI 通道信号：/mcp 有真实外部 client（目录爬虫与探针为主），Glama 已从官方 MCP Registry 被动收录（200）。业务调用仍为 0。
- 自造事故：误判时间差（13.25h 当成 >24h）覆盖 `ops/results/_DAILY_DIGEST.md`，因 `ops/` 在 .gitignore 外导致原 8/5 日报永久丢失。已改按 `-YYYYMMDD-HHMM` 归档。
- **项目约定**：`ops/` 整目录不在版本控制内（.gitignore:54），对该目录任何 Write 覆盖均不可逆，写前必须先确认有第二副本。
  - 【已于 04 轮解除】`ops/` 已建**独立本地仓** `ops/.git`（116 份基线，无远端）。现在可回滚，但仍不进主仓、不上公网。

## 06:00 飞轮（第 15 次运行）

- **关闭公开仓库泄露**：13 份内部运营文档（营收目标/竞品评估/策略）长期随主仓推送到公开仓库 `lm203688/roboparts`，raw 可匿名读取。04 轮已发现并 `git rm --cached`，但**中断在提交前**，口子一直开着。本轮实测 raw 仍 200 → 提交 `c804f6c` 推送，GitHub API 复核 `ops/` 目录与文件均 404。磁盘 0 缺失。
- **纠正自身过度告警**：泄露文件内 `gtk_` 片段最长 12 字符且带 `...` 截断（测试账号，不可利用）；11 个含完整 52 字符密钥的文件全部仅在本地。实际损害是运营口径外泄，非凭据泄露。
- **新增 L1.21 上报链路完整性**（`scripts/regression.py`）：4 条不变式，用「有痕迹的工作（commit/mtime）」反查「没留下报告的运行」。5 类违规注入全部可阻断。配套 `scripts/append_summary.py` 收敛摘要追加为唯一入口。commit `83b6a1a`（**因 GitHub 网络中断未推送，下轮补推**）。
- 修复 `_SUMMARY.md` 中 00:00 与 02:00 粘连成一行（非行首记录被下游按行解析静默丢弃）；补建 04 轮孤儿报告并标注「事后重建」。
- 回归全绿 / 数据无漂移 688 / OSS 0.84 天新鲜 / 无 BLOCKED / 日报 16.35h 未满 24h 跳过 / **站点内容零变化，未部署**。
- 情报：T/CAEE 060-2026 互操作团标（7/31）+ 工信部 WG7 数据组。命题获官方背书，但标准落地后「兼容查询」会缩水，价值应向「旧接口↔新标准映射」偏移。实测站内已覆盖，无缺口。

### 教训（第 30–32 条）

30. **上报是最后一步，所以它最容易整段丢失**。工作会留下痕迹（commit、mtime、代码注释），运行不会。必须用前者反查后者，否则一次截断就同时造成「下轮重做已完成的事」和「未完成的危险修复无人知晓」两种后果——本轮两者都发生了。
31. **暂存不等于修复**。`git rm --cached` 只改索引；未提交未推送时，公网上的东西一个字都没少。任何以"外部状态"为目标的修复，验收必须查外部（且 raw CDN 会缓存，要用 API 查真实树）。
32. **写闸门时要先拿它去撞自己已知的那个用例**。L1.21 初版的 ±1h 容差恰好把它唯一要抓的孤儿（20260806-04）判给上一小时报告而放行——闸门在自己的立案理由上假绿。先报红、再修复、再反向注入，三步缺一不可。

## 07:00 飞轮 · MCP 预算过滤诚实性修复

- 修复线上行为缺陷：`recommend_for_application` 的 budget 过滤让「无价格」冒充「在预算内」。实测 budget=50 返回 Tesla Optimus/Figure 03。根因 ①无价格条目无条件放行（494/688）②价格解析不含千分位逗号，`From $13,500`→13。
- 改为 `priceBounds`（解上下界）+ `priceFit` 四分类（within/partial/over/unknown），只剔 over，unknown 打标计数。副作用是真便宜的零件（DYNAMIXEL $0-100、GD32H7 $5-25）终于排到前面。
- 按 Glama 目录页扣分项补全 5 个工具的参数语义，依据均为实测：ID 前缀↔category 38/688 不一致；keyword 中英文命中交集为 0。
- 新增回归闸门 L1.22（12 条断言），8 类违规注入全部可阻断。
- 提交 `f119131`（已推送，远端一致）；ops 本地仓 `be278a4`。
- P0 情报：国标《人形机器人模块化通用技术要求》给出 L1/L2 合规分级 rubric，但我方判据覆盖率仅 2%~11%，本轮否决上线，改为把「补 declared 机械/电气数据」升为战略关键路径。

## 08:00 用户反馈痛点 + 飞轮上报机制改造

- **用户痛点**：飞轮每小时在跑、在写 `ops/results/` 文件，但用户"一概不知每个总指挥做了什么/提升了什么/需要我操作什么"。根因：①之前 prompt 第18步写"可发送、收件地址待配置"，等于从不主动推送；② agent-mail 连接器虽显示 connected，但 `GetMe` 返回 `not_bound`（实际未绑定），无法发邮件。
- **改造（automation-1785907133940）**：新增 `_LATEST.md`（每轮覆盖=最新一次结论三段式）、`_NEEDS_USER.md`（累积去重"需用户本人操作项"）；明确"禁止新建独立对话框、跑完自身对话可删"；反馈通道改为"用户在本总指挥对话说'汇报'，总指挥读这两个文件反馈"。已同步建好 `_LATEST.md` 与 `_NEEDS_USER.md` 初版。
- 现场演示：从 `_SUMMARY.md`(20条流水) + 最新运行(07:00) 给患者做了合并汇报，确认飞轮实际产出极多（L1.1–L1.22 回归闸门、MCP 端点、GEO、参数语义、合规分级研判等）。

## 08:00 飞轮 — 兼容性判定三态修复（假红）

- **核实 07 轮 P0**：ROS/mechanical_interface 的两个 100% 是**字段存在率**，有效声明率仅 11.5%(79条)/0.3%(2条)。站内对外口径用的是有效声明率且带 gap_note，无占位符冒充覆盖 → P0 关闭。
- **深挖出更严重缺陷并修复**：数据层标了 not_declared，引擎却 `compatible: shared.length > 0` 无 null 分支 → 厂商未公开被输出成「不兼容」。全库 A/B：**7140 组合中 6062(84.9%) 被错报不兼容**，修后 5969 转为 null，真冲突全部保留（且新增 71 对此前被词表漏掉的），true→false 为 0。
- 同时修：词表扩充(UDP/TCP/PCIe 等)、matchVocab 改词边界正则(防 io-link→lin)、proprietary 不作共享依据、EtherCAT≠Ethernet、overall=true 需硬约束证据(防舵机×AI芯片凭 ROS2 满分)、overall=null 时 score 返 null。
- 删除 `functions/api/bom/check.js` 自带的 evalDimension 副本改为 import 引擎；告警改为仅 `=== false` 且指明维度。
- 新增回归闸门 **L1.23**（21 条断言），7 类回退写法反向注入全部拦下。
- 提交 `6e1bede` → 推送 → 部署 → 线上往返复验通过；ops 本地快照 `f172785`；数据无漂移 688==688。
- 报告：`ops/results/roboparts-20260806-08.md`

## 09:00 飞轮（第 17 次运行）

- **修复**：MCP 端点把「调用方没给参数」报成「未找到实体: undefined」，并提示去 `search_components` 核对 ID。这是对数据库的断言，真相是请求缺参；Agent 照办会发现 ID 明明存在而卡死。改为 dispatch 层按 `TOOLS.inputSchema.required` 校验 → `-32602` + `missing_parameters`，两类失败用 `error_kind` 区分。提交 `917e6b2`，已推送/部署/线上复验。
- **闸门**：新增 L1.24（12 断言），8 类回退注入全阻断；初版自身假绿（全局搜 `-32602` 被「未知工具」分支喂饱）已收紧到分支内。
- **并发**：08 轮当时仍在运行，本轮转为只读核验，仅补其漏掉的 `_LATEST.md`。
- **自查**：核验 08 轮三态修复时因用错字段名（`compatible` vs `overall_compatible`）一度读出"全为 null"，险些误报对方造假；查清后其修复属实。
- 回归全绿、数据无漂移（688/325）、无 BLOCKED、日报 18.8h 未满 24h 跳过。ops 本地仓 `7c5abc1`。

## 10:00 飞轮（第 18 次运行）· 账本诚实缺陷 L1.25 收口+上线

- **闭环 09 轮遗留的 L1.25 阻断项**：闸门扫原始源码命中 credits.html 解释性注释里的旧写法 `preData.credits || 0` 假红（诱导删注释修绿）。改 section 6 剥离注释后扫描（同 section 7），并反向注入真实坏写法确认仍拦下。
- **账本诚实缺陷（涉钱断言）本体上线**：`USER_CREDIT_HISTORY` 从未绑定→4 处 `if(env.USER_CREDIT_HISTORY)` 生产恒 false，账本整体静默失效。新建 `functions/_lib/ledger.js` 四态统一入口（安全回落 `USER_CREDITS` 的 `hist:` 前缀），改写 history/balance/register/webhook + 前端守卫 + selection/bom 出口。线上复验：不存在 key 现返 `404 + error_kind:not_found`，不再谎报 credits:0。
- 新增 L1.25（8 段闸门）；L1.21 补齐 `_LATEST.md` 同口径 + `_NEEDS_USER.md` 存在校验。回归全绿、部署成功、数据无漂移、无 BLOCKED、日报 ~19.8h 跳过。提交 `5a5db8b` 已推送。
- 诚实性教义「失败的种类」四出口已全锁：L1.22 无价格→在预算内(假绿)、L1.23 未声明→不兼容(假红)、L1.24 缺参→查无此物(错语义)、L1.25 账本读不出→你没有交易(涉钱)。

## 11:00 飞轮第 19 轮 · 观测层假绿（L1.26，同族第 5 例）

- 修复 `read_metrics.py` 谎称「探针 client 16 次已剔除」——`biz` 从未被减过；`client:*` 记于 initialize、`tool:*` 记于 tools/call，无状态 HTTP 下**两条计数线不可相减**。这是第一次「假绿」出口落在观测层：前四例骗用户，本例骗决策者自己。
- 根因是埋点缺失：`functions/mcp.js` 在 tools/call 现场从不记录调用方。已补 `callerKind()`（按 UA 归类）+ `toolsrc:<kind>:<tool>` 归因线，与 `tool:*` 同分支成对写入；读侧改区间口径「真实需求 [0,18]」，历史数据显式标「不可归因」。
- 新增 L1.26（8 断言，两次反向注入验证未假绿）。回归全绿→部署→线上往返复验（新埋点正确隔离进 `selftest:`）。提交 `893ef56` 已 push；ops 仓 `727314d`。
- **纠正传了多轮的错误结论**：MCP 并非零业务调用（今日 18 次：check_compatibility 14 / detail 3 / search 1），但同样不能宣称"有人在用"。已同步修正 `_NEEDS_USER.md`。
- 起手时我自己也犯了同类错：凭印象断定"日报已过期"并写进 `_LATEST.md`，实测 21.57h 未满 24h 后推翻改回。
- 环境坑：`rm -rf` 被 safe-delete 拦截会返回非零并截断 `&&` 链，导致 commit 静默不执行（须事后核对）；`timeout` 在 Git Bash 解析为 Windows TIMEOUT.EXE；curl 输出到 `/dev/null` 或管道会显示 size=0 误判站点宕机。

## 2026-08-06 13:15（第 20 次运行 · L1.27 故障归类 + ROS2 布尔假红）

- **上一轮记忆里预判的两个出口，本轮同时坐实**（记忆 38 条写的「上游超时→查无此物」「权限不足→无结果」）：
  ① `api/oss.js` 数据源加载 catch→`{data:[]}`，以 **HTTP 200 + total:0** 返回，等于说「零件不存在」；
  ② `api/bom/check.js` 三源全失败仍返回空 map，下游以 undecided + 「证据不足」交付——
     **把 L1.23 刚修的三态从后门重新伪造**；
  ③ 两处 `resolveTier()` 的 `catch(e){}`→`'free'`：KV 抛错/Creem 超时/绑定缺失一律渲染成
     「你不是 Pro」，付费用户被剥字段 + 402 拒服务（涉钱，L1.25 的反向孪生）。
  修法：新建 `functions/_lib/upstream.js`（503 upstream_unavailable + 鉴权四态
  anonymous/verified_pro/verified_free/**unverified** + auth_unavailable 让路）。
- **本轮最值得记的是第二个发现，且它是"顺手验证时撞出来的"**：为验证 bom/check 改动做了一次
  在线往返，返回 `incompatible=1`——一个什么都没声明的组件被判不兼容，告警还写着
  「双方均有声明且不匹配」。追下去是 `compat_engine` software 维度 `ros_support === true`，
  把「未声明」压成「声明为不支持」。**688 实体中 614 个（89%）未声明该字段**，
  与任一 ROS2 组件配对全部误判。20260806-08 修 protocol/mechanical 时漏掉这一个，
  原因是字符串维度靠「空串=未声明」天然区分，**布尔维度没有这层保护**——
  同一条教义在不同数据类型上要分别落地一次。
- 闸门：L1.27（20 断言）+ L1.23 扩布尔三态（4 断言）；**三次反向注入**均被拦下
  （空集合回归 / 二值 resolveTier 回归 / 布尔压平回归）。
- **自查纠出自己的反向错误**：首版把 Creem 对无效 license 的 404 判成「校验链路不可用」，
  那是替一个无效密钥编造系统故障——与本轮所修同族、方向相反。改判 verified_free + `key_status`：
  **404/400 = 上游的裁决，5xx/429/网络 = 真故障**，两者不可混为一谈。
- 回归全绿→部署→线上双向复验（未声明→null 无告警；双方声明冲突→仍判红，没有为消假红而放水）。
  数据无漂移（688/325）；无 BLOCKED；日报 22.93h 未满 24h 跳过。`0dc685a` 已 push；ops 仓 `0f283dc`。

### 教义补充（第 42–44 条）

42. **一条不变式要在每种数据类型上各落地一次**。字符串靠空串、数组靠长度天然区分「未声明」，
    **布尔和数字不行**（false / 0 既是合法值又像缺省）。修完三态后必须按字段类型逐个复查，
    否则就会像本轮：protocol/mechanical 修好了，software 带着同一个 bug 又活了四轮。
43. **上游状态码要分「裁决」与「故障」**。404/400 通常是上游明确答复「没有这条」，属裁决；
    5xx/429/超时/网络异常才是故障。把裁决说成故障，和把故障说成裁决是同一种病的两个方向。
44. **改完之后的那次"顺手验证"，往往比改动本身更值钱**。本轮 89% 数据面的假红不是查出来的，
    是复验 bom/check 时看见一个不该出现的 `incompatible=1` 撞出来的。
    **线上往返复验要看全字段，不要只看自己改的那几个键。**

### 下轮关注

- **明日（8/7）此时读 `toolsrc:*`**：归因埋点已跑满一天，首次能把 MCP 真实需求从 [0,18] 收敛成确定结论。优先级最高。
- 待办优先级：外链建设(P0，`ops/seo/` 至今**空目录**，已多轮未动，是最大缺口)
  > 读 toolsrc 定 MCP 真实需求(P0) > 补 declared 机械/ROS2 数据（P0，614/688 未声明 ros_support
  本身就是数据缺口，修完假红后更凸显）> filter-repo(P1,人工) > GSC 验证(P1,人工) > 首笔实付(P1)。
- 日报将于 8/6 14:20 后到期，下一轮（14:00 或 15:00）需生成 `_DAILY_DIGEST.md`。
- 8/15 触发 N15 复盘；9/1 触发 T6 经营分析与战略下沉。
- 本运行对话可删除（产出一律落盘 ops/results/，不依赖对话留存）。

## 13:51 用户要求：汇总所有定时任务执行结果 + 取消侧边栏任务列表

- 用户要求：①把整个项目所有定时任务的执行结果汇总（修了什么 / 需我操作什么）；②定时任务执行完不要再在侧边栏加任务列表，执行好就删掉列表，把结果发给他。
- 已对**全部 6 个定时任务**的 prompt 追加「任务清单纪律」：禁止 TaskCreate/TaskList，运行结束前用 TaskUpdate 置 `deleted` 清理；结果只走 `_LATEST.md`/`_NEEDS_USER.md`+本对话。当前会话侧边栏 TaskList 已确认为空。
- 汇总结论已在本总指挥对话反馈（见对话正文）：23h 内飞轮约 25 轮，修复横跨诚实性/部署/数据污染/分发/引擎正确性五大类；待用户操作仍为 5 项（首笔实付/GSC·百度 TXT/GH_TOKEN/filter-repo/agent-mail 绑定可选）。
- 说明：GeneTech + SwarmLabs×3 的运行结果不在本仓库（各自 workspace），本次仅汇总 RoboParts 飞轮；如需另拉需切 workspace 或读其 GitHub/目录报告。

## 14:28 飞轮第 21 次运行：选型引擎 ROS 假红 + 对外素材数字全错

- 修 `functions/api/selection/engine.js` 四处 truthy 判断 `ros_support`：把「厂商未声明」
  渲染成「原生不支持 ROS」。执行器 **167/199（84%）**受影响，是 /designer 旗舰功能。
  与前一轮 compat_engine 同型缺陷的漏网 → **固化教训：同型缺陷从不只有一处，
  修完一处必须全仓搜同字段的 truthy 用法**。
- 修 `content/ros-discourse-post.md`（外链主投稿）：`489+ entities`（真值 688）、
  分类表每格过期、声称字段 `ros2_compatible` 数据集中根本不存在、选型引擎维度描述错误。
- 新增回归闸门 **L1.28**（全文件级禁 truthy）与 **L1.29**（对外素材数字须等于真相源，
  含防「删数字冒充一致」），均反向注入验证。主仓 `3978364` 已 push；ops 仓 `3a027ac` 本地。
- **`ops/seo/` 多轮空目录问题解除**：真因是素材数字全错不能发，非缺时间。已建
  `ops/seo/backlink-targets-20260806.md`（13 渠道 + 投放纪律）。
- 生成 24h 日报 `_DAILY_DIGEST.md`（21 次运行，7 类同族缺陷 L1.22→L1.29）。
- **新增一项需用户本人操作**：ROS Discourse 投稿（社区禁自动化发帖），素材已就绪。
- 环境要点：`ops/` 仓**无 remote 是刻意设计**（防运营文档泄露），push 会输出 usage
  但退出码 0，勿误判为已推送；含 `${}` 的文本走 Bash heredoc 会被 shell 展开并中断
  && 链（即便 <<'EOF'），须改用 Write/Edit 工具。

## 16:50 用户反馈：pricing 点击 Creem 报错 + 没有 ¥9 档位

- 用户点击 `/pricing` Pro 订阅后，Creem 收银台报错 `Live Payments Not Enabled`，说明 Creem 账户未开启 live 收款，代码侧无异常。
- 用户反馈页面只有 29 元档、没有 9 元档。根因：`pricing.html` 只渲染了 Free/Pro/Enterprise，`CREEM_PRODUCT_ID_STARTER` 未使用。
- 已修改 `pricing.html`：四档布局 Free/Starter(¥9)/Pro(¥29)/Enterprise；年付切换同时改 Starter 与 Pro；货币统一为 ¥ 并注明以收银台实际币种为准；前端识别 live 未启用错误并给出明确提示。
- 已部署到 `https://roboparts.cc/pricing`（预览 `https://0e5de199.robotparts-924.pages.dev`）。
- 下一步：用户需登录 Creem 后台开启 Live Payments，然后刷新 `/pricing` 用 Starter ¥9 走一遍真实支付验证回调链路。

## 17:50 总指挥飞轮第 24 轮

- 发现 15:46/16:10 两轮"孤儿运行"：做完实质工作并 commit 却完全没上报（OSS 189 条 ros_support 去伪造、MCP 遥测竞态修复），16:10 的 commit 连 push 都没做。已补写两份报告并推送。
- 结构性修复：运行留痕从事后检测前移到部署前（deploy.mjs 落 AUTO-STUB 占位），新增 L1.32 防占位糊住孤儿检测，5 处反向注入验证通过。
- 实跑暴露 Windows shell:true 拆空格参数导致 git HEAD 取空（源码级断言看不出），已修并补实跑断言。
- Googlebot 首次出现（0→10 次），域名权重瓶颈开始松动。MCP 真实需求区间收紧为 [1,19]。
- 新增 P0 情报：北京亦庄「机因工场」供应链平台（含选型选件中心），比英孚更重，定位应对写入 ops/intel/。

## 18:59 飞轮第 25 次运行 · L1.32 闸门自造恒红误报

- 修复 L1.32 判据缺陷：裸子串 `'AUTO-STUB' in text` 把「如实描述留痕机制的正常报告」
  误判成过期占位，闸门恒红。改为独占一行的 `STUB_SENTINEL`
  （`<!-- ROBOPARTS-RUN-TRACE:AUTO-STUB -->`）+ 行锚定正则。
- 教训（已入自动化记忆第 46 条）：**闸门判据的"语义宽度"是独立于正确性的失效维度**；
  用关键字做判据前必问"正常内容会不会自然出现这个词"；**恒红误报比漏报更危险，
  它会主动诱导人拆掉闸门**。另：扫描自身源码的守卫，needle 必须拼接构造以防自指。
- L1.32 7→12 条断言（阴性/阳性/端到端/防退化），5 处反向注入全拦下。
- 提交：主仓 `bdd9503`（**因 GitHub 网络不通未 push，下轮补推**）；ops 仓 `bca5bba`。
- Cloudflare 已部署上线；数据无漂移 688/325；Googlebot 10→14。

## 20:00 飞轮第 26 次运行

- 补推上轮滞留 `bdd9503`；本轮主仓 `de8ecdf`、ops 仓 `2b939dd`。
- 修 MCP `getKeySpecs` truthy 折叠（同族第三处）：`embodied_ai || false` 把未声明
  断言为 false（llms 20/42 条）；并补齐 3 个漏 case 的品类（108 条 key_specs 恒空）。
  全库仅有的 7 条 embodied_ai=true 恰好全在漏掉的品类里。
- 新增 L1.33 闸门 7 条（功能性，真跑 688 条），6 处反向注入全拦下。
- **发现 `_NEEDS_USER.md` 漏登记一项 P0**：`roboparts-mcp-server` 在 npm 实测 404
  未发布，但 README 教用户 `npx` 安装 —— 对外分发断路。需用户建 Granular Access Token。
- P0 情报：UL 1604:2026 / 模块化机械臂接口团标（本月发布）/ T/WEA 122-2026。

## 21:37 用户任务：竞品/市场分析 + 自动发现 + 3D 开源集成

- 用户要求：①分析竞品与市场需求，让客户能"自动找到我们"；②把 3D 设计类开源平台的好项目参考集成进来。
- 产出战略报告 `ops/RoboParts-竞品市场与3D集成战略-20260806.md`：竞品格局 2026（机器人备件电商 $4.2B→$11.8B CAGR12.8%、亚太 14.2% 最快；CAD 零件库 $548M→$1068M CAGR10%）；自动发现打法 = MCP 端点(已收录官方 Registry) + 结构化数据(JSON-LD) + 法兰/接口知识库 + OSS 社群引用 + sitemap；3D 集成路线图（转接件生成器 P0 / 零件3D预览 P1 / OSS URDF摄取 P1 / 转接件开源库 P2 / 服务端参数化 P2）。
- **MVP 落地 `adapter-generator.html`（已部署 https://roboparts.cc/adapter-generator）**：选两侧 ISO 9409-1 法兰（或自定义），浏览器内 three.js 实时 3D 预览（无后端依赖），导出 STL（3D 打印）与 OpenSCAD 源文件。差异化于只卖模型的 TraceParts/CADENAS——把"不兼容"变"兼容"。参考生态 OpenSCAD/CadQuery/build123d/FreeCAD。
- GEO 接入：llms.txt 加专题页条目、agent-discovery.json 加 landing page、iso-9409-flange.html 加 CTA、sitemap.xml 加双页（已验证线上 HTTP 200 + sitemap 含新页）。
- 待用户验证：浏览器打开 adapter-generator 实测预览/下载；转接件开源库(P2)与外链建设仍是最该补的缺口。
