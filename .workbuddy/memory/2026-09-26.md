# 2026-09-26 工作日志

## Smithery 上架完成 ✅

**用户 22:13 确认完成。**

### 流程回顾
1. 用户进 `https://smithery.ai/servers/new`（不是文档里的 `/new`）
2. Step 1 Namespace/Server ID：初填 `61960005`（数字，违反"字母开头"规则），指导改为 `lm203688/roboparts`；最终生成 namespace `fm203688`（Smithery 自动生成）
3. Step 2 连接参数：直接 **Skip**（RoboParts 免鉴权免配置）
4. 扫描阶段：**SUCCESS**（<1 分钟）
   - `Server info retrieved: name: roboparts, version: 1.1.1`
   - `Capabilities found: 11 tools, 1 resource`
5. Settings 页：填 Description / Homepage / GitHub Repository / Unlisted 保持不勾选

### 最终 URL
- **Listing**: `https://smithery.ai/servers/fm203688/roboparts`
- `/server/...` 308 重定向到 `/servers/...`
- 从外部 curl 只能拿到 shell 页面（SSR 数据在 __NEXT_DATA__ 里，metadata 可能需登录后才可见）
- Smithery 搜索暂时没收录（索引延迟，非故障）

### 关键 URL 修正（记忆）
- ❌ `https://smithery.ai/new`（文档示例，实际不存在）
- ✅ `https://smithery.ai/servers/new`（真实入口）

## 分发上架进度总结（RoboParts Agent 生态）

| 平台 | 状态 | 说明 |
|---|---|---|
| Official MCP Registry | ✅ | v1.1.1 isLatest=True（2026-09-25 已发布） |
| Smithery | ✅ | fm203688/roboparts（2026-09-26 22:13 完成） |
| Glama | ⏳ | 自动索引失败；备选方案：发邮件 frank@glama.ai 或补 smithery.yaml/mcp.server.json |

## Agent 生态核心资产
- **MCP Server**: `https://roboparts.cc/mcp` v1.1.1, 11 tools, 1 resource
- **REST**: `https://roboparts.cc/api/urdf/lint`
- **差异化**: `lint_urdf` — URDF 静态兼容性检查（生态稀缺）
- **实体**: 852 条，20 品类
- **数据同步**: fill_pct 5.69% → 15.34%（50 台工业机械臂已同步到 entities.json）

## 部署链路备忘
```python
python -c "
import subprocess, os
env = os.environ.copy()
for line in open('.env.local', encoding='utf-8'):
    line = line.strip()
    if not line or line.startswith('#') or '=' not in line: continue
    k, v = line.split('=', 1)
    env.setdefault(k.strip(), v.strip().strip('\"\''))
wrangler = r'C:\Users\xing\.workbuddy\binaries\node\workspace\node_modules\wrangler\bin\wrangler.js'
node = r'C:\Users\xing\.workbuddy\binaries\node\versions\22.22.2-3\node.exe'
cmd = [node, wrangler, 'pages', 'deploy', '.', '--project-name=robotparts']
subprocess.run(cmd, env=env, capture_output=True, text=True, timeout=300)
"
```
