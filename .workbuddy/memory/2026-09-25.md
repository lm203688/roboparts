
# 2026-09-25 日报

## Agent 生态接入：P1 数据引导（commit 2355176 → 远端 9163b087）

**用户决策**：论文放一放，先做 agent 生态接入。验证通过后选 ① + ③ 组合拳
（数据补齐 + 分发上架）。

### 需求验证（4 个信息源）

| 信号 | 结果 |
|---|---|
| ROS Answers #357888 | UR3 + 法兰 + 抓手安装报错，答得含糊 |
| Robotics Center AI 指南 | 手工维护的 "Robot Arm \| Flange \| Gripper" 对照表——**这就是我们要做的事** |
| ISO 9409-1 表（graspmonkey.de） | UR3e/5e/10e/16e 都用 ISO 9409-1-50-4-M6，UR20/30 用 -80-6-M8 |
| ROS-Industrial 官方 Wiki | 标准帧 `flange` / `tool0` / `base` 是行业通用名词 |

**结论**：需求真实，现有方案都是手工表格，自动化 + 三态诚实是差异化。

### 关键发现：MCP 基础设施早就存在

`functions/mcp.js` 1373 行 + 12+ 工具，`POST https://roboparts.cc/mcp` 已经上线
（实测 initialize 返回 `serverInfo: roboparts@1.1.0`）。`.well-known/mcp.json`
`.well-known/glama.json` `glama.json` 三个 manifest 都在。

**真正缺的三件事**：
1. **数据缺口**：5.69% 声明率，UR5e/UR10e/KUKA/ABB/FANUC 都不在库
2. **分发缺口**：RoboParts 尚未在 Glama（`https://glama.ai/mcp/servers?search=roboparts` 空）
3. **场景工具缺口**：有 `check_compatibility` 但**没有面向 ROS 的专用 URDF 工具**

### 执行 ①：跑 backfill 脚本

`scripts/backfill_platforms_hub.py --apply`（脚本已在仓里，dry-run 验证 50 条数据干净）：

- **来源**：Industrial Robotics Hub《Robot Tool Flange Sizes by Brand》2026-07-25，tier B（OEM datasheets）
- **新增 50 台**：KUKA 19 / ABB 10 / UR 9 / FANUC 6 / Yaskawa 4 / Doosan 1 / Techman 1
- **platforms.json**: 43 → 93，declared 0 → 46（+4 partial）
- **commit 2355176** → 远端 9163b087

不影响：
- `entities.json`（802 条，25 declared）—— 事实真相源（`onboarding_block.facts()`）不变
- `robot_fleet.json`（90 条，46 declared）—— 已含 HUB- 数据，本变更不冲突
- 声明率统计（读 entities.json 非本文件）

### 部署被卡住

- `deploy_api.py` 用错 API schema（传 `{"branch":"main"}`，实际 API 要 `{"manifest":[...]}`）
- `wrangler` 未全局安装，`npx wrangler` 尝试安装但网络/权限失败
- **实测**：live `https://roboparts.cc/api/platforms.json` 仍是 43 条（旧数据）
- **需要用户手动部署**：装 wrangler 后 `npx wrangler pages deploy .deploy-staging --project robotparts`

### Glama 上架（未做）

`glama.json` + `.well-known/glama.json` 都在仓里，但 `https://glama.ai/mcp/servers?search=roboparts`
搜索返回空——**RoboParts 还没被 Glama 收录**。Glama 的收录需要主动提工单或提交
GitHub PR，本会话没走通。

### 下一步（等用户拍板）

1. **手动部署**：本地 `npm i -g wrangler` 后 `wrangler pages deploy .deploy-staging --project robotparts`
   （或改 deploy_api.py 用正确的 manifest 格式）
2. **Glama 提工单**：去 https://glama.ai 提交 RoboParts MCP 服务器
3. **Smithery 提工单**：https://smithery.ai 提交
4. **数据同步到 entities.json**（选项 ②）：让 `check_compatibility` 工具真的能查 UR5e + Robotiq
