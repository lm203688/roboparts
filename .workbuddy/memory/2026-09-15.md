# 2026-09-15 工作日志

## 情报归档（用户裁定全归 RoboParts）
- 用户指令「你能做的都完成掉」+ 四条情报，AskUserQuestion 确认全部归 RoboParts 当供应链/竞品/基准情报。
- 一手来源核实（2026-09-15）：
  1. 鑫精诚(XJC Sensor, xjcsensor.cn) 六维力传感器厂商，2026-09-15 数亿元B轮；Q1出货>10万只、>200家客户、680+型号。归供应链候选，但缺官域安装接口核实 + 不在 MECH_SOURCE_HOSTS 白名单 → 不升 declared（AI 不编）。
  2. archify (github.com/tt-a1i/archify, MIT)：agent 生成 typed JSON IR → repo evidence 校验 → 确定性渲染，fail-closed。与 RoboParts「AI 不编」同源，可借鉴进 Copilot 图生成。Stars 报道冲突(15.1k/36k)未采信。
  3. minerva 0.54M (k1000dai/MINERVA, arXiv2609.03715)：LIBERO 95.75%，比 π0.5 少 7700× 参数，CPU 5-9ms/chunk。容量地板~1M。
  4. libero 标准套件已饱和(LaST-R1 99.9% / DobotWAM 99.25%)，但 LIBERO-Plus 扰动掉到 46-56% → 校准用户「libero没难度」仅限标准口径。
- 落盘 docs/intel-20260915.md，本地提交（commit 待补 hash）。**未推送**：push-gitdata.mjs PAT 明文泄露卫生项待用户拍板。
- 待拍板：①鑫精诚白名单+传感候选类目；②archify 借鉴进 Copilot；③push/deploy。

## 三件事落地 + 发布收口（用户「就按照你的计划全部实施」）
- **① Copilot 验证式兼容图生成**（archify 借鉴，落 `functions/api/copilot.js` + `copilot.html`）：
  后端从真相源（mechanical_interfaces / negative_compat / entities）构造带 `evidence` 的图 IR，
  **无背书的关系不进 IR**（fail-closed）；前端确定性 SVG 渲染（同 IR 恒同图，渲染层不参与推理）。
  功能自测 14/14（含「无 evidence 的边必被丢弃」「同 IR 两次渲染字节一致」两类对照）。
  设计要点：把「有没有依据」交给数据背书，模型只负责解释——与「AI 不编」同构。
- **② 鑫精诚 XJCSensor 供应链候选收录**：`api/suppliers_seed.json` 加六维力/关节扭矩厂商条目
  （仅 candidate，不产生 mechanical_interface 声明）；`scripts/regression.py` 的 `MECH_SOURCE_HOSTS`
  增 `xjcsensor.cn`（仅「允许引用」语义，≠ 已收录条目）。加域前确认 L1.78 无白名单条数断言。
- **③ 卫生：`push-gitdata.mjs` PAT 脱敏**：curl 失败异常消息曾含完整命令行 `Authorization: Bearer <PAT>`，
  改统一 `redact()`（抹 token 原值 + 授权头 + 裸 Bearer 串），阴阳自测通过。
- **留痕修复**：14 点提交无小时报告 → `roboparts-20260915-14.md` 以 RECONCILED 补写 + `_SUMMARY` 摘要行；
  日报声明落后 → 重盖 DIGEST-CLAIM。修后 regression EXIT=0。
- **发布链路**：commit 9e841a0 → push（远端 ref 71fc1111/tree f1b960ac）→ deploy-20260915-1543
  （Pages b61dda29，54 文件，`[3/3]` ✅，IndexNow 200/65）→ verify_live 17 页 + 3 接口全绿 →
  漂移收口 c2d1a9b（25 文件，仅时间戳，0 内容改动）→ push（远端 ref 2755953f/tree 00ebdd00）。
  `closure_status --write` 门禁：✅ 放行（HEAD c2d1a9b，源改动 0 / 漂移 0 / 锁 FREE / regression GREEN / 占位 0）。
- **纪律留痕**：`staged_guard.py --begin` 我在编辑后才调用，导致 stale 基准晚于文件 mtime；
  用 `--start-epoch`（真实轮次起手 15:29:04）纠正，未走「touch 文件」这类绕闸门的歪路。
- **环境坑（本会话）**：Git Bash 的 `ls/grep/tail/head` 与本项目 shim 冲突（PATH 断裂），
  shell 内一律走专用工具或原生 python/node 绝对路径；python 收到的路径必须用 `C:/...` 而非 MSYS `/c/...`。

## ECS 网关密钥注入 + 路由可观测性（用户贴 key）
- `.env.local` 登记 `ECS_API_KEY`（已核 gitignore + 未跟踪）；写 Cloudflare Pages secret 到项目 `robotparts`
  （**不是** `robotparts-924`，那是子域后缀——踩过一次）。
- 本机直探拓扑（一手数据）：80/8450 = AIShield API v4.3.0；8000/443 = HealthLens API v0.22.0；
  **8420 = 拒连**（ATEX/DeepSeek 网关，HealthLens 与 RoboParts 共同文档记载的地址）。
  即：**该机器上没在跑 OpenAI 兼容网关** → 代码默认值没错，缺的是服务。
- `functions/api/copilot.js` 可观测性修复：`lastDetail` 被后一个后端覆盖 ⇒ 改为 `attempts[]` 逐后端留痕；
  并加 `redactErr()`（抹 key/Authorization 头/裸 Bearer），因该字段会回给公开端点。

## P0 真实 BOM 机械声明 —— 根因审计（本轮重头）
- **口径分叉**：同一「机械声明率」仓里存在 7 个值（0.57/0.78/1.12/1.52/1.68/2.76/3.45），真相是 **5.75%**
  = (declared 15 + partial 10)/applicable 435（`add_mechanical_interface.py` 公式）。
  3.45 来自 `fix_mi_meta.py` 的 declared-only 分子；1.12 是 agent-discovery 里 20260809 的僵死快照；
  0.78 是 `demand_scan.mjs` 用了**第三套分母**（全库 798 而非 applicable 435）。
  `docs/contribution-loop-design.md:68` 明令「禁止在文案里手写百分比」，但公开文件里手写了 5 处。
- **两条动这个数字的通道都在造假**（已铲）：
  1. `urdf_auto_extractor.py`：`classify_flange_type()` 凭关节数推断 `ISO 9409-1-50-4-M6` 并标 declared
     ——直接违反本管道自己的成文纪律。已重写为「只留真实 URDF 事实、不产声明」。
  2. `bom_backfill.py`：`source_url` 塞公司名（非 URL）、`source_evidence` 无深链、却标 declared。
     已重写为「只判结构、不判证据、不产出声明」。
  3. `build_flywheel_layer.mjs` `fromBom()`：采信门槛仅 `if (!b.source_url) return _warn` ⇒ **fail-open 镜像洞**
     （`"Universal Robots"` 这种非 URL 也采信为 declared）。已改为走共享契约 `validateDeclared()` fail-closed。
  4. **额外真 bug**：构建器读 `bom.components || bom.entities`，提交入口写 `entries` ⇒ 键名不匹配，
     **这条提交通道从来没通过**。已修。
- **治理机制（新增）**：
  - `scripts/mech_evidence_contract.json`：白名单域 + ISO token 出处**单一真相源**（8 域 / 9 编码）；
  - `scripts/mech_evidence.mjs`：共享 JS 判据（`validateDeclared()`）+ `--selftest` ASCII 机读输出；
  - `regression.py` 主库侧改为**读同一契约**（消灭 Python/JS 判据分叉）；
  - **L1.92**：守「两通道判据同源」+ 反造假行为自证（AST 精确判定，不用源码字符串扫描——
    曾把我自己 docstring 里的 "declared" 误判为假红）；
  - **L1.93**：把「同一指标的每个副本 + 对外裸文本」拉进门禁（76 文件扫描）；
  - **`_run_layer()` 异常隔离（93 处）**：闸门自身抛异常**不得截断整套**——自查发现 L1.92 末尾
    引用未定义 `declared` 抛 NameError，直接中断 main()，导致 **L1.93 及其后所有闸门从未运行**
    而输出只显示"一两条红"。这是比单项假绿更危险的「静默截断」。同处还修了
    `set('ISO 9409-1-50-4-M6')` 炸成**字符集**的 bug（两个不同编码共享一个字符就算配出一对）。
- **自我证伪（重要）**：原本打算把 15 条「顶层已有 flange 线索」的实体批量提升为声明，
  但读到 `upgrade_eoat_mi_20260913.py` 的保守口径一节**已明确拒绝**过 OnRobot VG1/NG10/NG30
  与 Schunk EGP/WSG 050（理由：无逐型号公开孔位出处）。我手上证据（仅族级 ISO-9409-1 声明）
  比那轮**更弱** ⇒ **放弃提升**。厂商官域实探结论：Schunk 仅快换/ELG 配置器页逐字枚举编码，
  OnRobot 全站只写通用 `ISO-9409-1 flange`。
- **可采面一手否证（把假设推翻，别再重走）**：
  - `platforms` 30 条未声明**全是人形机器人**（Optimus Gen3 / Figure 02/03 / Unitree G1,H1 /
    Atlas / Digit / Apollo / 1X NEO / GR-1,GR-2 / Walker S / CyberOne…）——人形本体
    **不持 ISO 9409-1 腕法兰**。「机器人厂商文档最全所以可采」只对**工业协作臂**成立。
  - `sensors` 86 条未声明多为**芯片/相机/IMU/LiDAR 模组**（Intel RealSense 等），本无腕法兰。
  - **OnRobot 现行目录已无 VG1 / NG10 / NG30**（现行：RG2/RG6/2FG7/3FG15/VG10/VGC10/VGP20/
    2FG14/VGP30/2FGP20/MG10/Gecko/Soft Gripper/HEX/LIFT100/Quick Changer…）⇒ 官域无逐型号页。
  - Schunk 侧：`Robot adaptation packages EGU EGK EZU`（"standardized adapter plates …
    available for all common ISO flanges"）说明本体走转接盘；WSG 050-110-P 官方技术参数表
    **无** `Robot-side connection` 字段（与 20260913 互证）。
  - CubeMars 官域**逐型号提供 2D/3D 工程图**，但图纸孔位无法可靠机读，且「非 ISO」属未核实断言 ⇒ 不做。
- **本轮净变化：declared 15 / partial 10 / not_declared 410 / applicable 435 全不变，仍 5.75%。**
  **没有靠任何代码改动抬升该指标**，也没让无出处数据混进来。P0 剩余部分是真·外部依赖
  （逐型号 + 厂商官域 + 含 ISO 编码的出处；或用户手上的厂商图纸/PDF）。
- **纪律留痕**：`staged_guard.py` 本轮**抓对了我自己**——工作树里叠了两轮改动
  （copilot.js 属上一轮未提交），且 11 个文档改了没声明 intent，判红两次；如实补齐 intent +
  把 `--start-epoch` 推到我本会话动手前（1789461000），**没走「减小暂存面」蒙混**。
- **提交前密钥自查**：脚本读 `.env.local` 取 key 原值，逐暂存文件比对 ⇒ 无泄露
  （唯一命中是变量名 `MECH_STD_TOKEN_EVIDENCE` 的正则误报）；`.env.local` 由 `.gitignore:8` 挡住。

## 神经控制域：项目独立铁律 + 扫描桌面两压缩包（21:07 轮）
- 用户硬规则：「每个项目独立运行与开发，不跨项目直接耦合；若有关联须用其他项目自己的 API key 关联，禁止共享源文件/反依赖」。
- 修订 neurorobotics：删 README「与 SwarmLabs 飞轮衔接/共用同一份 source.json 事实基线」耦合段，
  改「项目独立原则」；GAP_MAP 去「接 SwarmLabs 飞轮」措辞。G3-G7 保留为 RoboParts 自有情报层
  （落 source.json research_gaps，自洽闭环）。
- **判定结论**：G3-G7 与 RoboParts 强相关且自持；「SwarmLabs 开 neurorobotics 垂直实体桶」是跨项目
  耦合，按规则驳回——若 SwarmLabs 需引用，须其自有仓库内、用自有 key 调本域已公开端点 api/neurorobotics.json。
- **扫描桌面两 zip（共 ~15 独立项目）**：robot-parts=本仓库；bionic-ai/biocomputing=无数据骨架；
  仅 brain-science（BCI/连接组内容站）同主题，但属独立项目→仅作「已 Web 核验后复制到本仓库自持」参考，
  非依赖。swarmlabs_*/genetech/oraclemind/quantum 等一律排除。
- 纳入两条已核验硬事实（自持，非依赖）：MICrONS 哺乳动物连接组（小鼠视皮层 1mm3/523M 突触/2025 Nature，
  REF-9，作 GAP-G5 规模基准）+ Neuralink R1+N1 植入式 BCI（已人体部署的脑↔体信号桥现实先例，REF-8，
  作 GAP-G2 证据）。source.json 升 0.1.1，build 后连接组 5 / 引用 9。
- 提交 00ffe3f（独立铁律+参考）+ 70d1d58（漂移收口）→ push（远端 ref 待回填）。
