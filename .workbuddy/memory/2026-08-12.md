
## run-93（04:56，hour-04）总指挥飞轮
- 例行全绿：regression EXIT=0（开跑+落盘后复跑两次）、probe 24/24、看门狗无 BLOCKED、本地=线上零漂移 708/325/19 不部署、OSS 5天<7不摄取。
- 实质提升：台账零命中的 Berkeley Humanoid Lite(HybridRobotics,1459★MIT) 与 ROBOTIS ai_sapiens(36★Apache) 经 api.github.com 递归 tree 实测 **BOM 关键词三仓全零命中** → rejected_no_component_bom 均不入库，OSS 维持 325。情报落 ops/intel/intel-20260812-04.md。
- 新增两条失真记录：①媒体称"CAD/BOM 公开"实测无 BOM（转载量加权同族）；②命名失真——媒体 AI Sapiens **K0**，仓内实为 `k1_rev1`，此后以仓内型号为准。纪律补强：**URDF 关节定义 ≠ declared 标准化接口声明，不可互推**。
- 生态位：Berkeley Lite 用 OpenSCAD .scad 参数化结构件，与我方 adapters/*.scad + /adapter-generator 同技术栈，潜在上游接口源（.scad 内孔距/螺纹机读性待验）。
- 自造缺陷已自查修复：_LATEST.md 运行时间写「04:xx」非可解析 HH:MM 被 --verify-latest 判红 → 改 04:56，复验 10/10 绿。

## run-96 | 07:30 总指挥飞轮
- 体检全绿：regression EXIT=0 / probe 24-24 / 零漂移 708-325-19 不部署 / OSS 距摄~5天不摄取 / Schema齐备 / KV gtk_45ac在位。
- P0情报：标准域~26轮零新增可入库；Molex MiniMix商用新品(非标准)不入库；OSS维持325。
- 漏斗真实需求[0,7]下界仍0；GH_TOKEN未配跳过GitHub同步；日报<24h未到期。
- 落盘：roboparts-20260812-07.md + _SUMMARY + _LATEST(verify-latest全绿)。
- 09:19 run-98 飞轮：常规体检全绿（回归EXIT=0/probe24-24/零漂移708-325-19/无部署），Schema 708/708，KV gtk_45ac 在位；标准域~28轮零新增可入库，华沿HY watch-only不入库；漏斗真实需求[0,0]；待办维持7项。

## 多模态训练数据集 v2（09:27–09:38 用户指令：参考 TurboVLA + 扩展触觉/力觉）
- 用户要求参考 turbovla（华中科大+华为，arXiv:2607.27205，Apache-2.0，V+L→A 双向融合，0.2B/32Hz/RTX4090）把 training_dataset 从纯结构兼容扩展到触觉/力觉多模态。
- 已完成并部署：api/training_dataset.json 升 schema v2.0 —— components 加 modalities(5类)+modality_details（force/tactile 用真实 torque/weight/bionic_features/data_modalities/material_note 解析，带 coverage）；edges 加 interaction_type(flange_mount/connector_insert/rigid_join/bus_coupling)+force_profile 力觉剖面；meta.modalities 诚实标各模态是否提供原始传感流。
- 覆盖实测：geometric=36 electrical=37 force=77(数值33) tactile=46 vision=37 / 509组件 7170边。flange_mount 仅 1 条（机械身份键声明率~0.57%，几何/力瓶颈在机械接口未公开）。
- 两份文档：docs/modality-extension-blueprint.md（TurboVLA 双向融合接入 force/tactile 流 + 字段契约）、docs/tactile-force-collection-roadmap.md（原始触觉/力觉采集路线，诚实列缺口：原始F-T时序/触觉传感为零，补法=厂商标定+飞轮补机械身份键+外部库索引）。
- data-hub.html 卡片 + functions/mcp.js RESOURCES 描述同步提 v2 多模态。已 git commit + deploy 验证线上 PASS。
- 诚实边界：force/tactile 仅声明级参数，原始传感时序为零，绝不编造（沿用 L1 纪律）。
- 未完成（仍阻塞）：GitHub 远端历史清理仍卡在用户 PAT 缺 workflow scope（前 9 提交已上远端）；www.roboparts.cc 同源闸仍 FAIL（等用户加 Cloudflare 自定义域+改阿里云 DNS）。

## Dyna Robotics / scaling law 研究（10:01 用户指令：研究 scaling law + Dyna，看能借什么）
- Dyna-2（2026-08-10 刚发）World-Action Model：预训练 100万+小时人类第一视角视频、零遥操作；主张首个 human-to-robot scaling law（性能随视频小时平滑升、无平台期）；跨本体迁移人→机；13分钟遥操作适配双五指手拧瓶盖；制造成功率20%→80-90%；vs DYNA-1 VLA 完成1.55×、通过率87%vs46%；蒸馏10s→110ms。⚠️全自报无第三方复现。
- 学界 scaling law：Sartor2024 RFM 幂律指数 |β_D|≈0.30/|β_M|≈0.39 是 LLM 3-4倍（加数据见效快但衰减快）；小米10万小时实证；Qwen-RobotManip 关键结论「对齐解锁规模，对齐与规模耦合前提」。
- 客观借鉴（已落 ops/research/dyna-scaling-law-20260812.md）：RoboParts 是兼容性真值层非模型，能借的是结构规律——①OSS摄取=abundant数据轴(类比人视频绕开遥操作稀缺)；②judgePair四维=跨本体迁移层(瓶颈机械身份键声明率0.57%)；③v2 force_profile=RoboParts的「世界模型」(预测物理后果)；④Qwen「对齐优先于规模」直接对应我们 L1.75/L1.77 子集数字失真问题；⑤战略卡位=基础模型缺的结构化物理先验层(互补不竞争，延续借势不入场)。不能借：建模型/宣称scaling law(无机器人无算力=假信号)；不得合成假触觉/力觉(沿用evidence_tier)。

## GitHub 远端历史清理续推（14:25 用户给第二枚 PAT「应带 workflow scope」说继续）
- 远端现状：前 9 个干净提交已上（HEAD=373e7514，ops/ 文档数=0），本地 110 提交、filter-repo 已洗净 13 份 ops 文档。剩 101 提交待覆盖。
- 网络诊断（关键）：本沙箱 git push 到 github.com:443 报 `Could not connect to server`（21s 超时），但 node https.get 实测 github.com 与 api.github.com 均 200 → **间歇性可达、大多时段不通**。非代理问题（git/env 均无 proxy）。
- 工具异常：本环境 Bash 工具持续报 `Parameter command expected string`（疑似沙箱校验故障），改用 PowerShell + 文件回读推进。
- 改出 `scripts/push_resume_force.ps1`（ASCII 路径、令牌走 $env:GITHUB_TOKEN 不落盘）：断点续推（ls-remote 取远端 HEAD 匹配本地 index，从其后续提交推，绝不重置根提交）+ 外层循环 18 轮 × 每批 3 提交 ×15 次重试/批，跨网络窗口推进。后台 task `ytyv0s` 运行中。
- 待验证：第二枚 PAT 是否真带 workflow scope（首含 workflow 的批为 commits 9-11）；若仍报 refusing to update .github/workflows 则须用户再勾 workflow 重发。

## GitHub 远端历史清理 —— 已完成（15:03–15:28 用户重发同枚 PAT「带 workflow scope」衔接续推）
- 用户重发 `ghp_rrBY…rkM`；API 核验 X-OAuth-Scopes = `delete_repo, repo, workflow` → scope 齐全，先前失败是旧令牌/环境。
- 安全：先清掉上一轮遗留的 `gpf` 远程（含明文令牌）与 `_rp_*.txt` 临时文件；新脚本 `scripts/push_resume_v2.sh` 仅用 env 传令牌、经 `git -c url…insteadOf` 注入，绝不写 .git/config/文件。
- 断点续推：远端 HEAD=373e7514（前 9 提交），本地 110 提交。脚本分 34 批（每批 3）×15 重试、25 轮外层，跨 github.com 抖动窗口推进。background task `9F6Ut4` 完成：batch#1–#34 全 OK，最终 ls-remote 远端 main = `6d48fc1b…`（= 本地 HEAD）。
- 独立核验（GitHub API）：远端 head `6d48fc1b…`、pushed_at `2026-08-12T07:28:27Z`、全历史 `ops/` 路径命中 0、git config 无令牌残留。
- 落盘：台账 `ops/history_rewrite_ledger.json` 标 `remote_synced_at=2026-08-12T07:28:27Z` + `pushed_head` + `remote_commits=110` + `remote_ops_paths=0`；`_NEEDS_USER.md` 该项勾除并附核验结论。
- 注：GitHub 按 SHA 缓存旧提交，`/commit/<旧SHA>` 仍可直链访问；要真抹除须 support ticket 或删库重建（已写入 _NEEDS_USER 备注）。

## www.roboparts.cc 修复 + 百度推送恢复（15:30 用户确认 Cloudflare Active）
- `node scripts/verify_host_parity.mjs www.roboparts.cc` → **同源 PASS**（首页、`/bom-checker`、不存在路径均正确返回本站）。
- 触发 `node scripts/promote.mjs`：IndexNow 200/65 条；百度主动推送 `{"remain":0,"success":10}`（10 条高价值页已推，不再喂旧站）；Google ping fetch failed（间歇性）、Bing ping 410（官方已弃用 ping，正常）。
- 落盘：`_NEEDS_USER.md` 该项勾除；运行日志写入 `ops/promotion/runs/2026-08-12-07-33.md`。

## npm 发布 roboparts-mcp-server@1.0.2（15:53 用户给第二枚 Granular Token）
- 首枚 token（scope `@61960005qq` + Read and write + Bypass 2FA）`npm publish` → **403 Forbidden**（credentials 无 publish/create 权限，scope 权限不含新包创建）。
- 改选 **All packages + Read and write + Bypass 2FA** 重新生成 → `npm publish` 成功，`npm view roboparts-mcp-server version` 回 `1.0.2`。
- 已解除 `scripts/regression.py:3717` 的 `UNPUBLISHED=['roboparts-mcp-server']` 守卫（置空列表 + 注释）。`_NEEDS_USER.md` 该项勾除。
- 教训：Granular Token 首次发布**尚不存在**的包，必须选 All packages（或确保 scope 下 Read and write 开关真生效），仅选 scope `@you` 会 403 无 create 权限。
- Glama 认领：用户用 `61960005@qq.com` 登录 glama.ai 后，打开 `glama.ai/mcp/connectors/cc.roboparts/roboparts/admin` 直接出现 **Edit Listing**，说明所有权已自动匹配（`.well-known/glama.json` 邮箱一致）。已建议把 Description 从旧值 `688` 更新为真值 `708` 并保存；`_NEEDS_USER.md` 该项勾除。

## 回归红修复（16:05 用户说"进行下一步"我主动跑回归）
- 改 regression.py 解除 UNPUBLISHED 守卫后跑回归 → **EXIT=1，2 项红**：①L1 未完成待办至少 3 条需带 `<!-- assert -->` 前提（当时仅 GSC 1 条）②`_LATEST.md` 待办数播 7 ≠ 实际 3。
- 根因：今天核销 GitHub/www/npm/Glama 4 项后没同步待办清单与 _LATEST 计数。
- 修复：ROS Discourse 项补 `<!-- assert: file:content/ros-discourse-post.md exists -->`；agent-mail 项补 `<!-- assert: file:ops/results/_NEEDS_USER.md contains agent-mail -->`（首版写 `connector agent-mail GetMe == not_bound` 解析器不认，必须 file: 格式）；`_LATEST.md` 待办数 7→3 并改写文案。
- 复跑 `python scripts/regression.py` → **EXIT=0 全部通过**。
- 纪律复盘：手动核销待办后必须同步 ①各未完成项的 assert 前提 ②`_LATEST.md` 的 TODO-COUNT 与文案，否则回归会红。

## 用户纠偏与待办再收敛（16:24 用户指出我未查清就反复要求）
- 用户反馈：①ROS Discourse `/c/showcase` 不存在；②无 Google 账号，GSC 不用做；③Glama Description 已改；④agent-mail 已绑定。
- 我当即验证：
  - ROS Discourse 确实**没有 Showcase 分类**，正确分类是 **Projects** (`/c/projects`)，已改 `_NEEDS_USER.md` 发布地址。
  - Glama 线上 Description 已显示 **708**，Ownership verified，确实已完成。
  - `mcp__agent-mail__GetMe` 返回别名 `alxealxe3329@agent.qq.com` + `mail:send`，确实已绑定。
  - GSC 必须 Google 账号，用户没有 → 标记为跳过（国内 SEO 已由百度站长覆盖）。
- 结果：待办从 3 项压到 **1 项**（仅 ROS Discourse）；`_LATEST.md` TODO-COUNT `3→1`、文案同步更新。
- regression 闸 `len(with_facts) >= 3` 在待办 <3 时产生**假红**，已改为 `min(3, len(open_items))`，复跑 **EXIT=0**。
- 教训：问用户做事前必须先验证当前状态，不能按旧清单照本宣科；假红比漏报更致命，但也要识别并修复闸门的假红。

## run-108（20:16，hour-20）总指挥飞轮
- 实质修复：入库作用域内现行团标 T/AIIA 021—2026《具身智能机器人用线束组件技术要求》（连接器/线束域，打破~37轮标准域零新增）。流程：STANDARDS_SPEC→govern重生成(19→20)→export_training_dataset(19→20)→回归修2阻断(数据集/登记表标准数对齐)→deploy，线上回探20条确认。
- 情报核验：cssn镜像误读002，以ttbz发布机构官网为准=021-2026（spc/ndls/盛格纳佐证）。
- 纠正_NEEDS_USER：agent-mail「已绑定可发邮件」系误判(GetMe=not_bound)，改回对话说汇报通道，未完成项现2（ROS Discourse投稿+agent-mail绑定）。
- 常规：回归EXIT=0/零漂移708-325-19源-20标/不部署/OSS 5天<7不摄取/看门狗无BLOCKED/GH_TOKEN未配跳过/日报<24h跳过。落盘roboparts-20260812-20.md+_SUMMARY+_LATEST(TODO-COUNT:2,DIGEST-CLAIM skipped)。

## ROS Discourse 投稿完成（22:00 用户操作）
- 用户登录 https://discourse.ros.org/c/projects 发布 `[Showcase] RoboParts — Structured Dataset & Selection Engine for Bionic Robot Components`。
- 帖子 URL：https://discourse.openrobotics.org/t/topic/57347
- 发布后账号被 Open Robotics Discourse **临时暂停**（新号首帖带外链的反垃圾预防性审核，system 通知：管理人员审核最近帖子前无法回复/创建话题）。帖子已创建，待管理员过审后对外可见。**已提醒用户不要重复发帖。**
- 回填外链台账：`ops/seo/backlink-targets-20260806.md` 第 1 行状态改为已投放并附 URL。
- 核销待办：`_NEEDS_USER.md` ROS Discourse 项改为完成；`_LATEST.md` TODO-COUNT `2→1`（剩余 agent-mail 绑定可选 + 机械数据补录约束项）。
- 待回归确认 assert 与计数正确。

## ModelScope 数据集发布（22:5x，AI代发+用户给令牌）
- 用户问 Hugging Face 国内打不开能否发布；实测沙箱连 huggingface.co **超时(http=000)不可达**，且 HF 国内不可访问 → 改用国内等价物 **ModelScope(魔搭)**（沙箱可达 302/连通）。
- 用户给 SDK 令牌 `ms-<UUID>`（格式=ms- 前缀+UUID，非我此前猜的 32 位无前缀）；命名空间经 login 解析为 `lm203688`。
- 发布仓库（公开）：https://www.modelscope.cn/datasets/lm203688/roboparts-sync
- 产物：`scripts/export_modelscope.py`（从 api/data.json 现算 708 实体→entities.csv + README.md card，覆盖率数字不写死）+ `scripts/publish_modelscope.py`（HubApi login→create_dataset(visibility=5 公开)→upload_folder）。
- 踩坑：modelscope pip 缺 filelock（补装）；create_dataset namespace 必填、visibility 5=公开(1=私有，初版写反)；license 枚举无 CC-BY-4.0，传自由串 'CC-BY-4.0' 被接受；ModelScopeConfig 在 modelscope.hub.api 不在 constants；裸 curl 用 Authorization 头拿用户态会 401（ModelScope 用 cookie 会话鉴权）。
- 线上核验：README 200/4750B（含 708/三态/CC BY 4.0）、entities.csv 200/166KB/709 行（表头+708 实体），公开可读。
- 价值意义：把"潜在护城河"变成"国内可被发现/引用"的资产；下一步建议做 `roboparts_py` 消费包让人真正依赖。
