# 2026-08-11 工作日志

## 00:00 第 72 次飞轮运行

**修复 1 · sitemap 提交了自带 noindex 的页面（线上挂了两周）**
`supplier-admin.html` 第 14 行 `<meta name="robots" content="noindex,nofollow">`（管理后台，
意图正确），而 `sitemap.xml` 自 2026-07-27 起把 `/supplier-admin` 作为待抓 URL 提交（priority 0.6）。
GSC 记为「已提交的网址被标记为 noindex」——扣的不是这一页，是整份 sitemap 的可信度与全站抓取预算。
剔除该条 + 部署 + 回探（线上 65 条 0 冲突，页面仍 noindex）。**不用 robots.txt Disallow 兜底**：
Disallow 会挡住爬虫读到 noindex，反而更易被收录。

新增 **L1.84**（16 项）：三层索引指令并入同一判据（页面 meta robots / HTTP 头 X-Robots-Tag /
robots.txt Disallow），4 阴性 + 4 阳性 + 冻结原文自证 + 覆盖面断言。

**修复 2 · 留痕唯一入口把「用错」翻译成「静默抹历史」（写留痕时当场撞上）**
用不存在的 `--summary` 旗标调 `digest_due.py --append-summary` → 静默忽略 → 三字段回落 `无`
→ 写出格式完美的 `修复:无 | 提升:无 | 待办:无` → 按档位幂等**替换**掉本轮真实内容 →
`--verify-latest` 13 项全绿（只校验格式不校验内容）。加三道闸：拒未知旗标 / 三段全空须
`--allow-empty` / 棘轮禁止空行替换已有内容；并入 L1.81 补 5 项对照。

**产出**：回归 EXIT=0（1005 项）｜probe 24/24｜线上 17 页+3 接口 0 未核验｜708/325/15 无漂移｜
IndexNow 65 条 200｜提交 bfe13f1 + 184057b（主仓）、b7ab8eb（ops 仓）｜情报 `intel-20260811-00.md`
（标准域连续第 8 轮零可入库；机因工场补实体细节，判定不变：人工撮合非数据层，已写死三条升级触发条件）｜
日报未到期（8.05h）盖 skipped。待办仍 8 项，P0=ROS Discourse。GitHub 推送仍因凭据阻塞。

## 03:00 飞轮第 73 次运行
- **修复兼容性引擎机械维度「侧别」缺陷**：`standard`/`flange` 描述的永远是**安装侧**，同侧一致只能证明
  「可互换（装同一法兰）」，不能证明「可对接」。新增 `tool_side` 识别与 `relation` 两态
  （`mateable` / `interchangeable`），缺一侧判 null 非 false。`functions/_lib/compat_engine.js`、
  `functions/api/bom/check.js`（并查集排除仅 mateable 的配对）。
- **新增 L1.85 + `scripts/verify_mech_sides.mjs`**（阳性 3 + 阴性 3 + 覆盖面 + 端到端，含冻结的修复前实现自证）。
- **关键教训**：`judgePair()` 按白名单拷贝维度字段，新增的 `relation` 被静默丢弃 —— notes（给人/LLM）与
  字段（给程序）结论不一致 = 等于没修。**凡新增语义必须断言它活到最外层响应，不是函数返回值。**
- 收口：FINAL_REG_EXIT=0（1014 项）、probe 24/24、verify_live_numbers EXIT=0（17 页+3 接口）、部署并回探
  线上 `relation=interchangeable`、提交 669ec84（本地）。

## 06:00 / 06:30 飞轮第 75 次运行（同小时并发双实例）

- **第一实例 06:00**：发现 `www.roboparts.cc` 在播三周前旧站 + 连续三天把 30 条 URL 误推百度（软 404）；根因 Pages 自定义域只注册裸域未注册 www；已加 `promote.mjs` 主机同源闸 fail-closed 止血，主机修复只能由用户做（P0，进 `_NEEDS_USER.md`）。详见 `ops/results/roboparts-20260811-06.md`。
- **第二实例 06:30（本记录）**：①新增 `scripts/run_lock.py` 单实例锁根治并发双写（三态 ALIVE/DEAD/UNKNOWN + Windows GBK 解码修复，常驻活进程拒抢占已阴阳验证，提交 d6b076d）；②量化 ICS 枚举盲区，纠正 run-74「团标召回闭合」过度乐观（T/CMAP 008-2026 ICS=None 未入表，机电一体化协会 153 条 35 空 118 MISSED），下轮补「按发布机构整表枚举」兜底。
- 待办仍 9 项（P0=www 主机 + ROS Discourse）。无内容改动不部署；`.tmp` 探针已清理。

## 07:45 第 76 次飞轮运行（hour-07）

- **修复枚举器假绿（本轮唯一主线）**：ndls(国家数字标准馆) 前置加速乐(Jiasule) JS 反爬，
  `POST /api/standard/list` 只回 `{"visitToken":<每次新uuid>}`（响应带 `__jsluid_s`），
  回填 header/body 均无效；站点 HTML 200 正常、robots 仍放行本 UA ⇒ 被挡的是**接口层**。
  枚举器照单全收且**绿着收**：`total=0`→年桶空→`0==0`→`complete=True`，
  对历史 436 条的 ICS 25.040.30 打印「穷举自证 实收 0/0」、写 `fully_enumerated:true`、
  并把 `scan_index[tuanbiao]` 的 **799 条扫描面覆盖成 0**。
- **根因（可推广，记进 MEMORY）**：三道既有自证全在「返回数据内部」查一致性，
  **没人验证过「返回的是一张表」这个前提**；前提不成立时自证退化成同义反复。
- 修复四件：`_ndls_assert_resultset`(认结果集/识别挑战页) + `count<=0` 中止 +
  缩水闸(<历史最大值×90% 拒写，基线取**最大值**不取最近值) + 空扫描面不得覆盖留痕。
- 新增回归闸 **L1.88**（11 项，行为对照**直接驱动生产函数、只换传输层**）：
  阳1 + 阴5 + 防误伤1(缩水5%须放行) + 基线最大值 + 留痕保护 + 非空转自证。
  真实端到端复测：同一调用 30 分钟前 `complete:True`，修后判红。
- **夹具坑**：首版把 420 条塞单年片，命中的是**另一道**闸门(深度上限250)——
  "测出的红与被测判据无关"是阴性对照最常见的自欺，已按年分片修正。
- **立场**：不绕过反爬（不解 JS 挑战/不伪造 token/不换 UA）。取证 `ops/intel/ndls-waf-blocked-20260811.md`。
- 常规：回归 EXIT=0 ｜ probe 24/24 ｜ 线上 17 页+3 接口 0 未核验 ｜ 708/325/登记表19 无漂移 ｜
  OSS 8-07 未满 7 天不摄取 ｜ 无内容改动**不部署** ｜ 本地提交 51ff849（未推送，远端历史清理仍待用户）。
- 待办仍 **9 项**（+0），P0=www 主机 + ROS 投稿。未新建对话框、未使用任何任务清单工具。
- 单实例锁本轮开工即 acquire（pid 36808），收尾 release。
- **发现 run_lock.py 的设计缺口（下轮修）**：飞轮每步是**独立 Bash 进程**，acquire 与 release
  永远不是同一个 pid ⇒ 按 pid 判归属的 `release` **永远失败**，每轮都得 `--force`。
  后果：干净释放路径实际是死的，全靠"死 pid 抢占/TTL"兜底；而 Windows **pid 可复用**，
  万一该 pid 被无关活进程占用，下轮会判 ALIVE 而拒绝启动＝卡死飞轮（正是上轮想根治的病换个入口复发）。
  修法：acquire 生成随机 token 落盘并回显，`release --token` 按 token 判归属，pid 只作存活参考。

## 08:22 用户问"闭环任务汇总在哪" → 建单一真相源 _STATUS.md
- 此前只有聊天版"统一口径"（8/10 17:17）+ 散在 _LATEST.md/_NEEDS_USER.md/日志，用户感觉没有统一入口。
- 新建 `ops/results/_STATUS.md`：人读的闭环任务统一视图（①任务本体 ②自主做了什么 ③修复了什么 ④需你操作 7 项含链接 ⑤风险 ⑥真值快照）。
- 关键：按用户 8/10 范围指令排除 ROS 投稿/GSC；把第 76 次运行新冒出的 **P0 www.roboparts.cc 播旧站+软404（已误推百度 30 条）** 列为头号紧急项；git 本地已清但远端仍有 13 份 + GitHub 凭据取不到 列为 P1 后续。
- 约定：本文件由对话线程维护，不与飞轮 _NEEDS_USER.md 强同步（避免与飞轮累积逻辑打架）；冲突以 _NEEDS_USER.md 为准。

## 08:4x credits.html 支付强制 API Key 校验修复 + www 方案B 指引
- 问题：startPayment() 强制先填 API Key 才能支付(9/29/199都拦)，但后端 create.js 早已支持留空自动创建 gtk_ 新key 并回传；pricing.html 是留空自动创建，两页漂移。
- 修复：去掉 startPayment 的 gtk_ 前缀拦截；订单成功时后端返回 api_key 则写入 localStorage+输入框+订单区显示"已自动创建并保存"；checkBalance 保留 key 校验。
- 部署 deploy.mjs EXIT=0；线上 /credits 验证通过(旧文案消失、占位符"留空将自动创建"、自动创建文案出现)。
- www 方案B：用户在 Cloudflare Pages 加 www.roboparts.cc 自定义域(硬阻塞在登录)。

## 08:5x 用户要"GitHub 推送凭据 + 其余待办"逐步指导
- 复核 push-gitdata.mjs：文档(_NEEDS_USER.md 远端历史清理 决策B)给的
  `node scripts/push-gitdata.mjs --rewrite-acknowledged` **实际跑不通**——第 161 行在线守卫
  不认该参数，且脚本只做增量快进重放、无法推重写的全量历史。已更正文档，注明需先补
  "重写已确认→从根全量重放+force 更新 ref"代码路径（待用户给 GITHUB_TOKEN 且确认覆盖后我做）。
- 给用户交付：GitHub 凭据逐步指引(repo scope classic PAT→粘贴给我→我补脚本+提交工作区+强推+填台账)
  + 其余待办逐步指引(www方案B / 真实支付¥9 / npm Granular / Glama 认领 / agent-mail可选 / ω-0文章建议)。
- 已按用户 8/10 范围剔除 ROS 投稿与 GSC（文档仍列，本对话不纳入）。

## 08:58 第 77 次飞轮
- 机械接口登记表 v1.1.0：修 `registry_ref` 断链（实体 `ISO 9409-1-50-4-M6` 无 A vs 登记表 id 有 A，707 处引用零命中）→ 每行补 aliases + grammar 归一化规则 + 阳性/阴性双对照；4 行法兰 + grammar 出处 tier A→B（abbpj.weisizhineng.com 非 abb.com）；新增 UR5e 工具法兰一手证据（UR 官方手册图 4.2）；新增 `designations_in_use` 现算缺口区块。新脚本 `scripts/curate_flange_registry_20260811.py`（幂等）。
- `credits.html` 取消下单前必填 API Key（后端 `functions/api/payment/create.js` 本就 auto-create）。
- 已部署 + 线上回探；回归 EXIT=0（1052 项）；probe 24/24；线上 17 页+3 接口 0 未核验。

## run-78（10:20）机械假红根治
- `compat_engine.js` 长期把同一法兰的不同书写形式（空格/大小写/全角空格/U+2011）判成"机械接口无交集"。修：`normalizeMechToken` 下沉到 `idValues`，自由文本 `matchVocab` 也归一化。
- 新增第三态 `undecided_designation_form`（A 前缀 vs 无 A，`compatible=null`）：未持 ISO 9409-1:2004 原文，不替标准裁决。
- **教训固化：查得到 ≠ 判得对。** run-77 补 aliases 只修了登记表 join，判定链路没动；半截修复会让下一轮以为已完工。凡"数据层修好"必须同时问一句「输出结论的那段代码改了吗」。
- 新增回归闸 L1.89 + `scripts/verify_mech_designation.mjs`（含冻结旧实现的非空转自证、阴性对照防归一化过宽）。
- 线上三例回探通过（true/interchangeable、null/undecided、真不同仍 false）。
- 情报新认识：关键词召回会按转载量给**错误信息**加权（已证伪软文再次排首位），不只是漏报。

## run-80（12:40，hour-12）
- **单实例锁 `scripts/run_lock.py` v1 从未生效**：`acquire` 写 `os.getpid()`，但它是打印完即退出的辅助命令，飞轮实例无长活进程 → PID 恒 dead → "PID 不存在即抢占"等于无条件放行。实测代价：12:42/12:55 两个并发实例提交（32b3343、300c653）。
- 改为**心跳判活**（`heartbeat_at` ≤30min 即在跑；PID 只加严不放宽；新增 `renew`）+ 回归闸 **L1.90**。
- **教训固化：测试可以给缺陷背书。** v1 阴性对照"伪造死 pid 期望能抢占"测的正是缺陷本身的表现。新增纪律：**闸门必须对冻结的修复前实现实跑，两版结论相反才算有鉴别力**（L1.90 已内置该自证）。
- 归类为「口径≠事实」第 18 变种：**代理指标失真**——用 A 的生命周期代理 B 的生命周期，而两者根本不同步。
- **行业标准枚举通道打通**（此前空白）：`scripts/enumerate_hb_standards.py` → `hbba.sacinfo.org.cn`（免登录 JSON，翻页参数 `current`）。机械整表 21023/21025，作用域内 4 条 JB/T。台账 `ops/intel/hb-standards-ledger.json`。
- **反假绿判据也会过严**：初版"未拉满即判红"把 99.99% 的数据整批丢弃。已拆分「数据可用性」与「穷举宣称」（覆盖率≥99% 写数据但 `complete=false`）；补探针 `channel_alive()` 让"确实没有"首次可证明。

## run-81（12:52，用户跟进）
- `credits.html` 支付 UX 再强化：订单信息区始终显式展示「充值账户」API Key + 「请保存此 API Key」黄色警告；保存 Key 模态框触发条件从「用户未填」放宽为「后端返回 Key 与用户输入不一致」（防止输入框被旧 Key 占用时跳过提示）。已部署验证。
- 用户提供 GitHub PAT（repo scope），启动 `scripts/push-gitdata-force-rewrite.mjs` 全量重放。
- **首跑失败根因**：脚本把子模块 `roboparts-dataset-github` 跳过，远端 tree SHA 与本地不一致。已修复为 `type: "commit"` 入树，mktree 复现验证一致。第二次后台运行 106 个提交重放中。
- `www.roboparts.cc` 方案 B 继续：Cloudflare Pages Custom domains 选「Begin CNAME setup」，等待用户提供 CNAME 目标后改阿里云 DNS。
