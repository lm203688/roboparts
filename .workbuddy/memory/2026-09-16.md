# 2026-09-16

## 竞品/前沿开源项目借鉴评估（九个）
用户给出一份拼写错乱的项目名清单，要求评估对 RoboParts 的借鉴价值。全部经 WebSearch 核实到一手来源，未编造。

**名称解码**：everos=EverMind-AI/EverOS（盛大，自进化记忆底座）· hypit=hypit-ai/hypit（视频 DSL）· weknora=Tencent/WeKnora（RAG，ima 开源版，MIT）· zvec-ai/zvec-grep=阿里的 zg 本地优先搜索层（Apache-2.0）· openopc=HKUDS/OpenOPC（AI 原生公司框架，MIT；注意另有 OPC 工业互联同名老项目）· ezl-keygrahph shannon=KeygraphHQ/shannon（AI 渗透测试，**AGPL-3.0**）· ecc-the full component tree=affaan-m/ECC（Everything Claude Code，MIT，190k+★）· map any ms2 spectrum across the known structural space=Cornell MS2KOSMOS（质谱，论文 bioRxiv 2026.08）· penguinharness=PrismShadow/penguin-harness（LlamaFactory 作者，Apache-2.0）。

**核心判断**：九个里七个是 agent/harness/infra 工具（关于"造 agent"），不是 RoboParts 竞品。只有 MS2KOSMOS 在**问题结构**上同构：观测特征 → 已知结构空间的检索+解释。借鉴=方法论，领域知识不可迁移。

**P0 借鉴（命中最大痛点：机械接口声明率 5.75%）**：
1. MS2KOSMOS 的「预测特征索引」——它对 10^5 分子**预先算出应有的谱图**再索引，所以未知观测也能被比对。RoboParts 缺的正是这一层：应**从 ISO 9409-1 标号推导期望 PCD 集合**并入库，而不是等厂商声明。纯靠厂商声明，声明率天花板就锁死在个位数。
2. Shannon「无漏洞利用，不报告」（Proof by Exploitation）+ `--resume` 可恢复工作区 → 升级为项目级信条"不验证不计数"；与已有门禁（阴阳自证/组合数对账）同构。

**P1（低成本）**：zg 的 context economy（结果按文件分组+行跨度+默认省略预览）、MCP 面只暴露 2 工具且 agent 禁止静默建索引、`fresh`/`possibly_stale` 新鲜度标记；PenguinHarness「恰好 6 个工具」原则（无 read_file/write_file，全走 shell，实测 3 次调用完成 CSV 清洗）+「错误收敛为消息，不抛异常」+ OmniMessage「流式/落盘/模型三处是字面同一对象」；ECC AgentShield 的配置面扫描（A–F 分级）+ 三条威胁假设（assume malicious text enters context / repo will be poisoned / memory will store wrong things；convenience layer must never run before isolation layer）。

**P2/P3**：WeKnora 自动 Wiki 图谱+答案来源标注+语义/BM25 混合检索；EverOS 多模态摄入（图纸 PDF/规格表锁住的信息是声明率低的另一半原因）+ 双轨记忆补"无行为日志"；OpenOPC 工作项状态机（规划→执行→Review→阻塞→完成）+ 复盘沉淀；hypit 词锚定非时间戳锚定 + 最小作用域重算（对应刚修的 demand-signal 冻结快照覆盖）。

**硬约束提醒**：
- **Shannon 是 AGPL-3.0**（网络使用传染）——只能借方法论，**不得抄代码进 RoboParts**。
- 按项目独立铁律：九个都不能接成 RoboParts 运行时依赖；只能"已核验后复制到本仓库自持"作参考。
- 抓取第三方厂商页面/图纸是不可信输入，进 agent 上下文存在提示注入风险（Shannon 与 ECC 都明确警告）——先隔离再便利。
- 本轮纯研究，未改动仓库、未提交、未部署。

## P0「应有特征推导层」落地（commit f11d88f → 漂移收口 0538df0 → 部署 5fbb4f84）

按昨日借鉴评估落实 P0：新增 `scripts/build_derived_features.py`→`api/derived_features.json`（571 行），从 `canonical_ladder_iso_9409_1`（13 档）为 9 种 ISO 9409-1 标号推导应有 bolt_count/thread/ISO 外径，形成与厂商声明层的独立交叉验证源。产物：ladder_rungs=13 / registry_designations=9 / matches_canonical=4 / deviations=3 / no_standard_rung=2 / entity_tokens_in_use=4。新鲜度摘要锚定源内容 sha256（非 wall clock、非 mtime），幂等无 per-deploy 抖动。

**同步修三处派生冻结快照病**（同族 bug 家族——生成一次后不再重算即对外宣称）：
1. `scripts/refresh_designations_in_use.py` 新（226 行）：从 entities 现算机械接口标号使用面，`TOK_RE` 三级形态；三态 registered / unregistered_gap / unparseable_bare_standard；BIONIC-HAND-002 的「ISO 9409-1」判粒度不足而非未登记缺口。**L1.74 陷阱复现**：单孔位条目必须写标量字符串、不能写成单元素数组。
2. `scripts/build_standard_audit.py` 死代码修复：原 `registry_gaps` 把 `entries`（list）当 dict 迭代，永不匹配⇒恒为 `[]`（虚假合规）；`entity_kind` 全链路穿透（否则触发全库对账漂移 29 项）；`source_anchor()` 走源内容时间戳字段；保留注入的 `meta.access`。
3. `scripts/inject_api_access.py` 加 `DOMAIN_OVERRIDES` + `access_for()`：neurorobotics.json 曾被打回通用零件文案（对外假陈述——神经控制端点宣称谈零件选型），改后 merge（非整块替换）保留 `mechanical_interface_declared_pct`/`cross_vendor_comparable_grade_a` 让另一条闸门仍通过。

**L1.95 独立重写闸门**：不 import 生成器，7 个独立 helper（`_l195_norm/_l195_join/_l195_tokens/_l195_status/_l195_classify_geo/_l195_sha16/_l195_note_check`）互查生成器；50 断言含 7 例阴性/阳性/空集/粒度不足自证。

**关键坑（note 复核假绿）**：`note_corroborates` 若做 `.upper()` 会把 `x`→`X`，与大小写敏感正则 `(\d+)\s*x\s*m(\d+)` 叠加恒空匹配——5 条含断言的 note 全被算成 0/0，闸门因此恒绿。**这是 generate-gate 早期假绿同型病第二次出现**，记进 memory。

**部署链顺序**：`deploy.mjs` 插入 0b5→0b6→0b7 三条（refresh→derive→audit），都在 0c inject 之前，保证新鲜度断言成立。

**留痕纪律**：
- 三份 AUTO-STUB（20260915-17/20/21）以 RECONCILED 补写（保留不删、附真实 commit 哈希、by 晚于占位小时）。
- `_DAILY_DIGEST-20260916-08.md` 补出（上次 09-14 16:42，间隔 39.7h）+ 同步 `_last_digest.txt` + `--stamp` 重盖 DIGEST-CLAIM/TODO-COUNT。
- **`--stamp` 依赖 `_last_digest.txt` 现算 due**：只写 digest 文件不改标记⇒stamp 判「该出而未出」拒盖。顺序必须是 写 digest 文件→改 `_last_digest.txt`→`--stamp`。

**发布链路**：f11d88f（2450+/135-，11 文件）→ 推送（远端 ref 落 06313b8b，tree b0f7716e）→ deploy-20260916-0832（5135 files，preflight PASS、`[3/3]` ✅、IndexNow 200/65 URL、百度 200/10）→ verify_live_numbers 17 页 + 3 接口全绿 → 漂移收口 0538df0（27 文件 / 30±，纯时间戳）→ 推送（远端 ref 落 1b71cc1d，tree 973250cf）。

**遗留红项**：`roboparts-20260916-08.md` 是部署时自动占位（未手填），下轮 RECONCILED 补写；`_STATUS.md` 有 2 条提示性幽灵哈希（31c5d40, c804f6c）非闸门。

## SwarmLabs 外部数据源接入尝试（17:15 用户提供 key）

用户给：`sk-swlm-rbprts-067998a515b2aaee` / `https://swarmlabs.tools` / project=`roboparts`。
**符合项目独立铁律**：只走 SwarmLabs 公开 API + 此 key，绝不同步源文件、绝不反向依赖。

**已落 `.env.local`**（已在 `.gitignore` 白名单外）：`SWARMLABS_API_KEY` + `SWARMLABS_BASE_URL`。

**首次调用实测——key 无效**（穷举尝试，全部 401，hint 均为 `Provide x-api-key header`）：
- header 变体：`x-api-key` / `X-Api-Key` / `X-API-KEY` / `Authorization: Bearer` / `Authorization: <plain>` / `api-key` / `key` / `token`（8 种）
- query 变体：`?x-api-key=` / `?api_key=` / `?api-key=` / `?key=` / `?token=`（5 种）
- 路径变体：`/api/stats`、`/api/v1/stats`、`/v1/stats`（返 HTML 200）、`/api/knowledge-graph`、`/api/entities`、`/api/trends`、`/api/bridge`、`/api/papers`、`/api/v1/entities`、`/api/v1/knowledge-graph`、`/api/labs`、`/api/lab`、`/api/insights`（13 个）

**结论**：不是 header 拼写或路径问题——所有 `/api/*` 路径都回同一 401 消息体。可能性只有三个：
1. key 未真正写入 SwarmLabs 后端（发放时只存了日志、没进 KV / secret store）；
2. SwarmLabs 认证中间件不读 `x-api-key`（读的是别的名字，但 401 提示却说 `x-api-key`——提示本身可能是模板写死）；
3. key 已发但项目白名单未放行 `roboparts`。

**给用户的动作项**（SwarmLabs 侧核实）：
- 检查 SwarmLabs 后端 `KV.get("API_KEY_SK_SWLM_RBPRTS_067998A515B2AAEE")` 或 secret store 里是否真的有这条 key；
- 若走 license.swarmlabs.tools 发 key，确认该子域 Cloudflare Tunnel 已生效（实测 CONNECT 502，Tunnel 未接通）；
- 确认 auth 中间件实际读的 header 名——若与 401 提示不符，把提示改对（现在的提示是误导性的）。

**下一步等 SwarmLabs 侧修复后**：
- RoboParts 侧无需改代码——key 存好、`.env.local` 就位、`.env.local.swarmlabs` 也符合 `.gitignore`；
- 首个消费者建议：neurorobotics 情报层跨域桥接（RoboParts 机械兼容 → SwarmLabs 微生物/材料域），走 `GET /api/bridge?q=...` 一次拉几十条候选做情报层 enrichment。
