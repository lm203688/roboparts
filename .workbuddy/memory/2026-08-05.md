# 2026-08-05 工作日志

## RoboParts 总指挥飞轮 · 14:00 首次运行

**做了什么**：整合运维+增长双飞轮职责，完成回归校验、数据漂移检查、Schema/密钥/看门狗巡检、情报搜集、漏斗采集，并建立 `ops/results/` 上报机制（本轮报告 + `_SUMMARY.md` 流水 + 首份 `_DAILY_DIGEST.md`）。

**关键结论**：
- 两块历史硬短板同日闭环 —— S-01 管理员密钥线上泄露（敏感路径已全部 404）、S-02 支付通道（CREEM 产品 ID 已真实化，收银台可生成链接）。
- RoboParts 现状：**数据/安全/收款三项能力均已就位，唯一瓶颈是没有真实用户**（0 注册 / 0 付费 / 0 元，07-27 后 10 天零新单）。战略重心应从「修数据」转向「拉流量」。
- 发现「飞轮自污染」：自动化自身的 E2E 验证订单在污染其监控的转化指标，需做测试写入隔离。

**技术要点（复用价值高）**：
1. Git Bash 环境下不可用 `timeout` 包裹命令 —— 会命中 Windows `TIMEOUT.EXE`，管道静默返回空，极易误判为服务不可用。
2. 验证支付链路可用性可用**静态代码取证**代替实际下单：`creem.js` 仅在取得 `checkout_url` 之后才写 KV，故 KV 中存在订单记录即端到端证明收银台可用 —— 零副作用，避免制造脏数据。

**产出文件**：`ops/results/roboparts-20260805-14.md`、`ops/results/_DAILY_DIGEST.md`、`ops/intel/P0-开源人形模型包管理器分发-20260805.md`、`ops/funnel/funnel-20260805-14.md`。

---

## 15:00 · 总指挥飞轮第 2 轮（自动化 automation-1785907133940）

**做了什么**：把上轮发现的「飞轮自污染」从认知变成修复，并顺带清了一个 SEO 层面的隐性问题。

**三项修复（均已上线）**：
1. **测试订单隔离**：`functions/api/payment/{create,creem}.js` 按邮箱判定分流写 KV —— 测试走 `testorder_`、真实走 `order_`；`notify.js` 双前缀回落查找，保证测试链路仍能端到端跑通。判定函数含 `@roboparts.cc` 自有域名规则与 contest/latest 反误伤白名单，16/16 用例通过。
2. **历史污染清理**：KV 里 10 笔 `order_*` 逐笔取回核对，**无一例外全是内部测试**（fullpay_/verify@example.com/paytest_/test@roboparts.cc/unknown），已带审计字段迁移到 `testorder_*` 并删除旧键。**真实订单基线从「10」修正为「0」——这才是真值。**
3. **软 404**：`/functions/**` 原本返回 200 + 首页 HTML（源码未泄露，但等于给搜索引擎喂大批重复内容页），加入中间件私有前缀改为硬 404，并在 `scripts/regression.py` L1.6 增加 `/functions` 断言防回退。

**认知升级**：此前「有 10 笔订单未支付」会诱导人去优化收银台，实际上一笔真实下单都没有过。**指标脏 = 决策错**，这比指标难看危险得多。

**新 P0 情报**：《人形机器人 3DOF 球肩驱控传一体化通用适配规范(2026版)》首次给出可直接建库的法兰参数（φ80/φ120/φ160、4×M5/6×M6/8×M8、孔位中心距 φ65/φ100/φ135、公差 ±0.02mm）。实测本库 **机械互换维度覆盖 0/688 = 0.0%** —— 一个叫「兼容性平台」的产品，判断两个执行器能否物理互换的第一性字段一个都没有。这是下一步最该补的资产缺口。

**踩坑记录**：`wrangler kv key delete` 无 `--force` 参数（误加会静默失败且退出码 0）；`while read` 读无末尾换行的清单会漏最后一行；wrangler OAuth token ~1h 过期后 `pages deploy` 在非交互环境不自动刷新，先跑 `wrangler whoami` 即可恢复。

**产出**：`ops/results/roboparts-20260805-15.md`、`ops/intel/P0-机械接口法兰适配规范-20260805.md`、`ops/funnel/funnel-20260805-15.md`、`ops/test/kv-migrate/`（10 份迁移审计凭据）。

---

## RoboParts 总指挥飞轮 · 16:00 第 3 轮

**闭环机械互换维度 0/688**：新建 `api/mechanical_interfaces.json` 标准登记表 —— ISO 9409-1:2004 法兰指定语法 `ISO 9409-1-A{d1}-{n}-M{thread}`（附 `parse_regex` 可直接程序解析）、4 个标准尺寸族（A50-4-M6 / A80-4-M8 / A100-6-M10 / A160-8-M12，含定位销与已知适配机型）、3DOF 球肩三级参数、5 类 `mount_type`、**6 条装配红线**（螺纹旋入深度顶破油封、同名 A80 参数不等价、销孔 H7/g6 配合、螺栓须 12.9 级等）。全库 688 条补 `mechanical_interface` 显式状态：350 适用 / 338 `n_a` / 2 `partial` / 348 `not_declared`，fill_pct 0.57%。脚本 `scripts/add_mechanical_interface.py` 幂等可重跑。

**设计原则**：不臆造尺寸。无出处一律留 `null`，用 status 显式表达缺口 —— 把「字段静默缺失」变成「可被 Agent 查询的显式缺口」，并在 llms.txt 明确警告 `not_declared ≠ 无机械接口`。

**又一处静默失效**：`agent-discovery.json`（Agent 直读的发现清单）categories 明细求和 **577，却宣称 total 688**，且 4 个端点仍指向废弃的 `robotparts-924.pages.dev`。与 api/data.json 漂移同型 —— 只校验总数、不校验明细就会一路全绿。已修正并加断言。

**闸门反噬自己**：首次部署被 deploy.mjs 的字段级指纹校验拦下（改了真相源 entities.json 却没重生成对外 api/data.json）。走 `normalize_categories.py` 重生成后二次部署通过。**闸门拦住了执行者本人的疏漏，这正是它的价值。**

**回归新增 9 条断言**：L1.7 机械维度不变式（字段全覆盖 / status 合法 / meta 与实际分布一致 / 已声明数棘轮基线 2 / 登记表存在且含 parse_regex）+ L2 三条 agent-discovery 一致性与端点断言。

**转化真相**：KV 96 键中 `order_*` = **0**、`testorder_*` = 10（15:00 隔离未回退）、39 个 `user:*` **全是测试账号**（swarmlabs.tools / test.com），真实用户 0。连续三轮真实转化恒为 0。

**自我反思**：三轮全在修数据与修闸门，都是真问题，但**没有一项直接作用于 P0 获客**。数据侧已足够好，继续找缺口来补是舒适区。下轮必须产出分发动作。切入点：ISO 9409-1 designation（"A50-4-M6 能装什么"）是中文互联网无结构化索引的长尾词，正好是数据资产与获客的交集。

**踩坑**：Git Bash 无 `sleep` 命令（`sleep: command not found`，且会因 `&&` 短路掉后续命令，应改用 `;` 分隔）。

**产出**：`ops/results/roboparts-20260805-16.md`、`ops/intel/P0-ISO9409机械接口标准化竞争-20260805.md`、`ops/funnel/funnel-20260805-16.md`、`api/mechanical_interfaces.json`、`scripts/add_mechanical_interface.py`。

## RoboParts 总指挥飞轮 · 17:00 第 4 轮（获客弹药 + SEO/GEO 基建上线）

**落实上轮反思「必须产出分发动作」**：不再修数据/修闸门，转向把已投入未上线的内容资产变获客弹药。

**软 404 根因闭环（P0 级 SEO 缺陷）**：此前 `/articles/*` 全部 200 回落首页、sitemap 宣称的 URL 全是重复内容，导致收录惩罚。新建 `404.html` + 实际生成 14 篇文章页 + `iso-9409-flange.html` 法兰速查落地页；本轮回测确认不存在路径现返回真 404。

**IndexNow 密钥文件命名错误修复**：仓库仅有 `indexnow-key.txt`，协议要求 `{key}.txt` 即 `roboparts2026indexnow.txt`，线上 404 导致提交静默失败。新建正确命名文件，线上核验 200。提交 Bing 端点 200，54 URL 受理。

**robots.txt 与 sitemap/llms.txt 三方矛盾修复**：旧 robots 禁抓 20 个数据端点却 sitemap 列它们为待抓、llms.txt 又邀请 AI 取用。改为放开公开数据端点、显式 `Allow` GPTBot/ClaudeBot/PerplexityBot/OAI-SearchBot/Google-Extended/Applebot-Extended/Bytespider/CCBot/cohere-ai/Amazonbot/YouBot 等，仅 `Disallow: /api/usage.json` 与蜜罐。

**首页内链孤岛修复**：导航新增 `/articles/`、hero CTA 加"技术文库（14 篇·10 万字）"、热门速查段落 3 处内链，共 5 处指向文库。

**回归闸门新增 L1.8（21 条内容分发不变式）**：404.html 存在、content/ 每篇有 slug 且已生成页面、sitemap 文章条目数与源一致且无幽灵 URL、iso-9409-flange 收录、每页单 h1+JSON-LD 可解析+canonical、llms.txt 收录技术文库、agent-discovery.content_library.count 一致、robots 与 sitemap 不矛盾、robots 显式允许 AI 爬虫、IndexNow 密钥文件 `{key}.txt` 存在且内容匹配。部署 3 次全通过。

**P0 竞争情报**：英孚机器人（总经理何川甫）上线业内首个在线关节选型平台（数百种零部件库 + 工程经验算法，填力矩/转速/体积/重量/刹车/力控/减速器方案秒级推荐机电组合），正面撞车「兼容无忧」。反制：不硬拼选型器，抢"选型前知识检索"GEO 长尾词 + "选型后兼容性校验"两端。落 `ops/intel/P0-英孚机器人关节选型平台上线-20260805.md`。

**转化真相**：真实用户 0 / 订单 0，连续四轮恒为 0。瓶颈确认在获客不在数据——本轮已补齐弹药与基建，下轮起需看流量/注册/转化信号。

**产出**：`scripts/build_articles.py`、`scripts/build_flange_page.py`、`scripts/submit_indexnow.py`、`404.html`、`articles/`(15 页)、`iso-9409-flange.html`、`roboparts2026indexnow.txt`、`ops/results/roboparts-20260805-17.md`、`ops/intel/P0-英孚机器人关节选型平台上线-20260805.md`。日报距上次 <24h，跳过。

## 推广专项 · 18:12（用户指令：不用出报告，推广出问题马上修，多渠道含开源，分析用户/可达性，加快速度）

**诊断「推广问题」= 开源资产建了却断链**：GitHub README 停旧数据(493/实际688)、Star History 指向错仓库、零回链；HuggingFace 同步失败(HF_TOKEN 缺失)；ModelScope 数据集已公开(200)但无回链；python-sdk 不能 `pip install`(无 pyproject)；14 篇文章零外链。

**立刻修复并 push 上线（最高速的"马上修"）**：重写 `roboparts-dataset-github/README.md`（688 实体正确分表 / 接 ModelScope 活链 / 加 `pip install` / 修 Star History 为 `lm203688/roboparts` / 加差异化定位"不只是另一个选型器"）；修 `huggingface_dataset_card.md`（493→688、链接修正）；`python-sdk` 加 `pyproject.toml` + README 加 `pip install roboparts`。**已 `git add -f`（主仓 .gitignore 第59行忽略该目录）+ commit + push 至 lm203688/roboparts main（DNS 抖动 443 reset，第4次重试成功），raw README 已验证含 688/ModelScope/pip**。git 身份已设（support@roboparts.cc / RoboParts Flywheel）。

**多渠道分析与可发素材（用户要的"用户在哪里/可达性在哪里"）**：`ops/promotion/REACH_ANALYSIS.md`（6 类用户画像分平台 + 可达性矩阵 + 优先级打分，前5=GitHub·ModelScope·PyPI·HF·知乎）、`OUTREACH_DRAFTS.md`（9 份可直接发文案，均带回链）、`STATUS.md`（渠道状态+解阻塞清单）。

**解阻塞杠杆**：补 HF_TOKEN(secrets→HF自动同步)、PyPI token(发 `roboparts` 包)、ModelScope 账号 token(改简介)。本机 git 推送已可用。用户明确不出 ops/results 报告，推广产出落 ops/promotion/。

**竞争主线**：英孚关节选型平台(撞车兼容无忧)定位为"选完来 RoboParts 验兼容 + 开放可引用数据层"，已成文案主线。

## 18:00 总指挥飞轮（第 5 次运行）· 边缘遥测上线

**核心**：站点此前无任何流量埋点，导致"验证获客效果"是不可能任务——连续四轮把"转化 0"当结论，其实是在没有仪表的情况下反复宣布读数为零，分不清"没人来"还是"来了没转化"（两者处方相反）。

- 新增 `functions/_middleware.js` 边缘遥测：isolate 级累加 + 16 分片落盘，覆盖 18 类 AI 爬虫 / 9 类搜索爬虫 / 页面 / referer；配套 `scripts/read_metrics.py`。零成本，不开公开端点。
- 自造并自捕全站 404 事故：`Math.random()` 写在 Workers 模块顶层 → worker 启动失败 → 配合 `_routes.json` 的 `/*` 造成整站 404。部署闸门在预览环境拦下，生产零影响。
- 抓到"假绿"断言：用「括号深度==0」判 JS 顶层，实测顶层深度恒为 1，条件永不成立。改列锚定判据并注入事故代码反向验证。
- 修零流量丢数（爬虫命中改立即落盘）+ 两处自污染（伪造探针、deploy.mjs 校验请求，均加 `X-RoboParts-Selftest` 头隔离）。
- 回归新增 L1.9（25 条断言），部署 4 次全通过。真实流量基线清洗归零。
- P0 情报：英孚从"选型器"升级为"开放供应链数据库"；我方唯一不可复制优势是**中立**。产出 14 条 GEO 长尾词排期。

**经验（可复用）**：①Cloudflare Workers 顶层禁用随机数/时间调用，否则全站挂 ②新断言必须注入违规代码反向验证，假绿比无断言更危险 ③任何自检流量必须与真实指标分账，否则下一轮会把自己的动作读成市场反馈。

## 19:00 飞轮第 6 次运行 · 参数口径中立牌

- 主线：把竞品英孚（关节卖家）自认的痛点「参数存在水分、参数定义又存在区别」转为我方不可复制的中立牌——卖家同时是裁判与运动员，无资格定义参数口径。
- 产出 `/api/parameter_semantics.json`（6 条物理红线 + 5 组单位换算 + 4 级可比性判据 + 7 条采购问询清单，CC BY 4.0）+ `/robot-joint-parameter-spec` 落地页（TechArticle+FAQPage+Dataset 三重 Schema）。
- 用自家 688 条实体作行业证据且不美化：torque 63 值 19 种口径、speed 101 值 37 种且混装 Gbps 与 rad/s（量纲互逆）。公开登记自身同类缺陷 KD-01 并用断言锁死不许删。
- 修 2 处沉默失效：① OSS 摄取新鲜度检查（`ingest_oss.mjs` 只写 generated_at，飞轮读 last_updated 恒 null，7 天阈值永不触发）；② 本轮第 2 条假绿断言（裸子串 "63" 被 CSS 色值 `#30363d` 误命中 → 改 25 个 data-src 语义锚点）。
- 新增回归 L1.10（中立性资产 + 认知诚实）与 L1.11（运维判据字段存在性），两者均做反向验证，6+1 类违规注入全部确认可阻断。
- 遥测：真实爬虫命中 1 小时内 9→20，GPTBot/OAI-SearchBot/ClaudeBot 三大 AI 爬虫到齐；但 Googlebot/Bingbot/Baiduspider 全为 0。**判读：内容与技术基建已不是瓶颈，瓶颈是域名权重与外链；IndexNow 受理≠抓取。**
- 新增人工待办：Google Search Console / 百度站长 / Bing Webmaster 人工提交（需 DNS 验证，飞轮无法自动化），写入 `ops/手动接入待办-20260805.md`。

## 20:00 总指挥飞轮（第 7 次运行）· 把 AI 通道接到转化上

**用户指令**：想办法推动真实转化 + 遵循自动化原则（不产生等人工的待办）。

- **核心断点**：唯一走通的流量通道是 AI 爬虫（四大 AI bot 已确认抓取，真人 0），但 `llms.txt` / `agent-discovery.json` 里 `curl`、`/api/register`、`Bearer` 出现 0 次——爬虫抓走的内容中没有任何可执行的下一步。自助注册端点实测一直是通的（200、即时发 key、100 免费额度）。**门开着，但没人知道门在哪。** 已补零摩擦接入段与机读 onboarding 段。
- **转化基线第一次可信**：KV 中 46 个 `gtk_` 键全为历史健康检查/部署校验/支付测试产物，与潜在真实用户共用命名空间，"有没有真实用户"在原数据结构下无法回答。新建 `scripts/audit_users.py`（默认只读）用邮箱明文特征 + SHA-256 哈希反查甄别，清 25 个确证测试键，`stat:users:total` 校正为 0。
- **险些虚报**：首版判据把"无明文邮箱"当真人，一度报出 23 个真实用户。改为 real/unverified 分离——**无法证明是测试 ≠ 是真人**。最终诚实基线：已确证 0、存疑 21（不计入也不删）。
- 注册补来源归因（`attribute()` + `stat:src:*` + `stat:first_signup`），自检注册用 `selftest_` 前缀分账。
- **本轮第 3 次假绿**：`curl` 与 `/api/register` 裸子串各自存在即通过，删掉领 key 命令仍绿。收紧为"须在同一条命令内"，4 类违规注入全部阻断。
- GitHub README 加「30 秒接入」+「不隐瞒的三件事」并 push；修 ModelScope 路径不一致与 HF 死链徽章；`origin` 由失效 SSH 永久改为 HTTPS。

**经验（可复用）**：①验证 git push 结果用 GitHub API contents 端点，不要用 raw.githubusercontent（CDN 缓存会读到旧版，本轮误判推送失败）②甄别测试数据用确定性哈希反查，不要用"缺少某字段"这类间接特征③报出虚假的好数字比承认 0 更危险，存疑必须单列一档。

## 21:00 飞轮第 8 次运行 · 接入入口下沉到 AI 实际抓取的页面

- 核心修复：可执行接入入口此前只在 llms.txt（占 AI 抓取 3.3%），已下沉到 18 个实际被抓页面（/articles/* 占 30%）。入口覆盖率 3.3% → 约 47%。
- 新增 `scripts/onboarding_block.py`（真相源，额度从 register.js 解析）+ `scripts/inject_onboarding.py`（幂等注入），三个 build 脚本挂自动重注入。
- 修复 `build_articles.py` sitemap 整段覆盖吞掉其他写者注册的 URL（参数页曾被静默抹掉）→ 改保留式合并。
- regression 新增 L1.13（9 条），6 类违规反向验证全部阻断。部署全绿，IndexNow 56 URL，push c9b97cc。
- P0 情报：真正对手是占满中文 SERP 的 SEO 站群软文农场，不是英孚。真人层已被截流，AI 引用层是唯一有胜算的战场 → 停止产内容，资源转向 AI 可引用数据 + 外链。
- 教训：① 多写者共享文件禁止区间整体覆盖；② 发布统计数字前先打印字段实际取值分布（差点发布错误的 0.0% 声明率）。

## 22:00 飞轮第 9 轮

- **JSON 接入声明**：新建 `scripts/inject_api_access.py`（幂等，注入 `meta.access` 到 25 个对外 JSON，额度/限速从 `functions/api/register.js` 现场解析，蜜罐排除），`scripts/regression.py` 新增 L1.14 六条不变式（7/7 违规反向验证阻断），`scripts/normalize_categories.py` 挂自动重注入。已部署，线上 3 接口实证含 access。
- **MCP server 修复**：`mcp-server/index.js` 数据源由本地 `../api` 改远程 `https://roboparts.cc/api`（专属 UA + 本地回退 + 失败明确抛错 + 启动预载为 0 拒启），品类 7→10 类 688 条；新增 `mcp-server/selftest.mjs`（临时目录无 `../api` 跑完整握手）；`package.json` 1.0.1、README 重写。**npm publish 被平台 403（legacy token 被 2FA 策略限制），待人工换 Granular Access Token。**
- **情报**：`ops/intel/P0-MCP生态同名包被第三方占位且我方指标被污染-20260805.md` —— npm `roboparts-mcp` 属第三方 Crawde（robopartsai.com），此前周报「npm 下载 5 次」是对方数据，我方真值 0。
- 报告：`ops/results/roboparts-20260805-22.md`。

## 23:00 总指挥飞轮（第 10 次运行）

- 修 MCP 分发包两处：预载无重试（本机 3 次自检失败 1 次 ≈ 33% 装后起不来）→ 加 3 次退避重试、4xx 不重试；自检闸门用固定 sleep(6s) 等就绪属计时竞态 → 改事件驱动等真实就绪信号，改前 10 次坏 2 次、改后 6 次全绿且真故障仍被快速拦下。
- 新增回归 L1.15（10 条断言）锁死 MCP 分发包不变式，8 类违规注入全部可阻断。git push 687c3e1。
- npm publish 再次 403（legacy Publish token 被 npm 2FA 策略禁止直接发布，须人工换发 Granular Access Token，步骤见 ops/手动接入待办-20260805.md 3-bis）。
- P0 情报：同赛道（ResolveMesh / FoundryNet）走 hosted Streamable HTTP 端点 + 官方 MCP Registry + Smithery/LobeHub，不走 npm；我方四目录全缺席。→ npm 降 P2，下一步建 roboparts.cc/mcp 托管端点（零人工依赖）。
- 周报 npm 口径显式勘误：周下载 6 是竞品 Crawde 的包，我方真值 0。
- 数据无漂移（688/325），本轮无需部署，无 BLOCKED 项。
