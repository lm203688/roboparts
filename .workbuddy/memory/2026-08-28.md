# 2026-08-28 RoboParts 工作日志

## regression 32→0 全绿（放行发布）

- **provenance 生成器补全**：`provenance_coverage.tier_rule`/`note` 被手改抹成 null（L1.73/L1.83 红因），重跑 `enrich_provenance.py` 恢复；entities.json 补换行结尾（L1.55 幂等族）。教训：**meta 字段禁止手改 entities.json，必须走生成器唯一源**。
- **L1.79 宽限阴性测试修正**：原测试只喂 gb 的 run，tuanbiao 按 8-10 declared_at 已超 14 天宽限 → 判红是正确行为，测试构造不出"宽限期内"形态。给 `enumerate_standards.source_states()` 增加台账 `led['declared'][sk]` 覆盖（新来源入台账即从当天起算宽限），测试改用 declared 覆盖复现真实场景。
- **L1.89 覆盖面修正**：裸标准号 `ISO 9409-1`（非设计编码，parseIsoFlange 正确地返回 null）被计入"必须可解析"集合 → harness 改为只收 `9409-1-\d` 完整编码。行为对照 20 项全绿。
- **占位报告清剿**：删除 20260820-16 / 20260821-14 / 20260826-17/21 / 20260827-09/12（6 份 AUTO-STUB）+ 补建 20260827-13 孤儿报告（该时段 git 有活动）。教训：**删占位前先查该时段 git 是否活动，有则补 RECONCILED/AUTO-FILLED 报告而非删除**。
- **来源枚举实跑成功**：gb（std.samr.gov.cn）拉到 437 条、ndls（ndls.org.cn）拉到 800 条团标 —— 此前记忆认为"团标渠道 WAF 中断"已失效，ndls 现在可直连！台账 `standards-enum-ledger.json` 更新，L1.79 来源枚举红清零。
- **日报补发**：`_last_digest.txt` 停在 8-18（欠 9 天），生成 `_DAILY_DIGEST-20260828.md` + 更新标记 + `--stamp` 盖 `DIGEST-CLAIM: issued @17:06` + `TODO-COUNT: 12`。
- 提交 `16c18d6`（28 files, +43278/-1294），部署 + 推送后台执行中。

## 发布闭环完成（17:20）
- 部署 `26e9b998.robotparts-924.pages.dev`（中间件校验 + 推广脉冲 IndexNow 65 条全过）✅
- 推送 lm203688/roboparts @ `16c18d6`（Git Data API）✅
- verify_live_numbers：线上 17 页 + 3 接口与真相源 767/20/325 全一致 ✅
- _LATEST 重写为发布终态 + 机械盖章（TODO-COUNT 12 / DIGEST-CLAIM issued@17:16）+ ops 私有仓 `cc521ff`
- MEMORY.md 更正：团标渠道 ndls（ndls.org.cn）已恢复直连 800 条（8-28 实测），删去"WAF 中断"过期结论

## 外部具身智能技术借鉴评估（23:12–23:30）
用户列举五项技术求深挖借鉴。**先做事实核对，纠正三处命名错误**：
- 「MHS AI 控制实验仪器 99%」→ 实为 **NVIDIA ENPIRE**（GEAR/Jim Fan，8 Codex Agent 驱动机器人舰队，真实灵巧操作 99%）
- 「Dyna-22」→ **Dyna-2**（Dyna Robotics，100 万小时人类视频 WAM，首证 human→robot 迁移 scaling law）
- 「PSI」→ **灵初智能 PsiBot Psi-R2/Psi-W0**（10 万小时人类数据，MolmoSpaces 登顶 46.4%）
- CMU 那条是 **VLLR**（CMU+Amazon Robotics，LLM 分解子任务+VLM 估进度+self-certainty+PPO，+56%）
- Lyra 2.0 核实无误（NVIDIA，单图→可漫游 3DGS/mesh，Apache 2.0，导入 Isaac Sim）

**核心判断：五项里四项是模型/算法层，RoboParts 是数据基础设施层，不同层。借鉴算法是错的，只能借鉴其暴露出的需求与指标方法论。**

### 已落地（零采集成本）
- `scripts/build_negative_compat.py` → `api/negative_compat.json`（81 条裁决，幂等已验证 md5 一致）
  借鉴 Psi-W0「30% 失败样本」：兼容性引擎此前**只有正面裁决，无负面知识**
- **浮现关键结论**：9 种已登记 ISO 9409-1 标号 PCD = 20/31.5/40/50/63/80/100/160/250 **两两皆不同 ⇒ direct=0，任意两种不同标号互换 100% 需转接盘**。⇒ 适配器不是附属玩具，是法兰互操作唯一通路
- 新增 `ops/external-tech-benchmark-20260828.md`（核实表 + 借鉴矩阵 + 落地清单）

### 采纳/不做的判断
- 采纳 ENPIRE：①MDU 指标（把 1.52% 声明率做成对外靶心）②「重置环境比完成任务容易」→ 让厂商确认 AI 预填 draft，而非从零填写（冷启动正解）
- 采纳 PsiBot 信噪比判据（任务多样性>物体多样性>>场景多样性）→ 反向论证**补 1 条 power 数据的边际价值 > 再补 10 条 actuators**（当前 actuators 217 vs power/pcb/cables/connectors 各 2）
- 战略级待议：Embodiment Descriptor（跨具身缩放定律缺统一本体描述 schema，RoboParts 最接近；且能自洽解决「缺一等品类」）
- **明确不做**：VLLR/RMTL（训练配方层，零挂载点）、Lyra 2.0（P2 观察，前置条件未满足）

### 口径更正（重要，又踩一次口径坑）
`_NEEDS_USER` 写声明率 **1.68%（分母 358）是过期数字**。唯一真相源 `meta.mechanical_interface_coverage`：
declared 2 / partial 4 / not_declared 388 / not_applicable 373 → 应声明 = 767−373 = **394 → 1.52%**，
与 `platforms.json honest_limits` 一致。**对外一律 1.52%**；`coupled_count=371`（6/371=1.62%）是第三口径，不得对外。

### 收尾
回归 63/63 全绿 → 提交 `？（见 git log）` + ops 仓 → 部署 → 推送
