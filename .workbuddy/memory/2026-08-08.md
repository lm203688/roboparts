# 2026-08-08

## 00:32-00:55 · 飞轮第 34 次运行
- 清除幽灵待办：`_NEEDS_USER.md` 里「Copilot 接本地 Ollama 需放开跨域」已随 8/7 Agnes 代理上线
  彻底作废（copilot.html 中 11434 本地线上均为 0），用户照做属白费力气。待办 10→9。
  另更正「pricing 改积分切换仍在进行中」过时描述（线上 Creem 0/虎皮椒 2）。
- 新增回归闸门 **L1.49 幽灵待办闸门**（scripts/regression.py）：待办可用
  `<!-- assert: file:X contains|not-contains|exists Y -->` 声明所依赖事实前提，
  被代码变更推翻即判红；含 5 项阴阳自测 + `_LATEST` 播报待办数与清单实际数对账。
  **自测哨兵串必须运行时拼接**，否则源码字面量会被自己的 contains 命中（已踩）。
- 提交：主仓 `1696bbb`（push-gitdata 推送，tree 一致）；ops 仓 `98ab7ab`。本轮无站点变更、未部署。

## 飞轮第 35 轮（01:40–01:58）：飞轮自己给自己刷流量，已根治

- **实证**（阳性对照，非推测）：巡检用的裸 `curl` 不带 `x-roboparts-selftest`，
  被 `functions/_middleware.js` 记成**真实流量**。哨兵路径裸打 → 进真实 404 归因；
  带头打 → 只进 selftest。后果：真实请求数灌水 + 自造 404 被下轮读成"站点有断链、实伤 GEO"。
- **修复**：新增 `scripts/probe.mjs`（19 项探活，统一出口合并隔离头）；
  回归闸门 **L1.50**（三处头名一致 / 无裸请求 / 覆盖面 / 3 条阴阳自测）；
  `read_metrics.py` 加历史口径警告（8/8 01:45 前数据须扣除自造部分）。
- **纪律（长期）**：巡检一律 `node scripts/probe.mjs`，**禁止裸 curl 打 roboparts.cc**；
  临时探路径用 `node scripts/probe.mjs /path` 或手动加 `-H "x-roboparts-selftest: 1"`。
- 提交：主仓 `92b6bc5`（push-gitdata → 远端 `ef33677`，tree 一致）；ops 仓 `3adc6e2`。
- 状态：回归全绿、探活 19/19、688/688、OSS 325/325、无 BLOCKED、待办仍 9 项、本轮未部署。

## 飞轮第 37 次（04:12–04:30）· OSS 数据层误诊纠正

- 上一轮报的「OSS 实时抓取连续失败，325→136」**是误诊**。实测 9/9 上游源 200 可达。
  真因：`ingest_oss.mjs` 的 `LIVE = process.env.LIVE === '1'` 默认关闭，而任务书/文档/
  历次报告写的调用方式都是裸 `node scripts/ingest_oss.mjs` → 每次"照文档执行"都静默
  退化成种子层，覆盖 189 条 LIVE 实体，还打印 ✅ 以 0 码退出。
- 修复：LIVE 默认开启；LIVE=0 改为继承既有 LIVE 实体标 stale；缩水>10% 拒写+exit 1；
  ingest 自行重注入 meta.access；deploy.mjs 真正调用 inject_api_access.py
  （其 docstring 自称已挂载，实际 0 处引用）。新增回归闸门 L1.52（11 项含阴阳自测）。
- 回归 4 红 → 全绿（修根因，非改数字）。线上回探 /api/oss = 325/189/access 齐。
- 提交：主仓 be817dd（远端 914fb62a，tree 一致）；ops 仓 074fe3d。

## 05:23–05:40 飞轮第 38 次运行 · 探针传输层重试（L1.53）

- `/pricing` 探活单次 `fetch failed`（10.3s）→ 真发请求复核 4 次全 200/0.6~0.75s，**站点正常，是本机网络抖动**。
- 修 `scripts/probe.mjs`：`status===0` 重试 3 次带退避；拿到 HTTP 状态立即返回（不掩盖站点真故障）；
  输出/`--json` 分列 `site_failures`/`transport_failures`；瞬断标注「第N次才通」。`PROBE_RETRIES` 可调。
- 新增回归闸门 **L1.53**（9 项，3 阴性 + 1 阳性），并做**真实行为对照**（不可达域名 vs 真 404），
  不只做源码正则匹配。
- 闸门再次抓住自己：`_LATEST.md` 缺显式 `运行时间：` 前缀被判红。
- 未部署（仅 scripts 变更）。主仓 `f2241fe` → 远端 `2458cb19`（tree 一致）；ops 仓 `7f70b2e`。待办仍 9 项。

## 07:45–08:07 飞轮第 39 次运行 · 接管 06:45 断尾 + 生成器抹件根治（L1.54）

- **接住"干了活但没上线"的一轮**：06:45 那轮把 `api/entities.json` 688→706（新增 18 条
  传动/轴承/润滑/腱绳件），只手改 5 个页面就被切断，未提交/未部署/未报告。本轮开局回归判红 3 项接管。
- **先验后采**：18 条溯源逐条真发请求实测（17×200 + 1×403，与 `scripts/expand_drivetrain_20260808.py`
  记录的状态表吻合）→ 判为可信，采纳；计数一律由生成器重算（禁止手改数字），
  删掉写死 5 文件清单的一次性脚本 `propagate_counts_20260808.py`。
- **同一病根三个新位置**：①`build_param_page.py` 第 315 行用了 `os` 却从未 import →
  每次崩，链式补注入**从未跑过一次**；②`build_parameter_semantics.py` 整份重写抹掉 `meta.access`；
  ③页面模板漏 favicon/theme-color。再加 `inject_onboarding.py` **从未挂进 deploy** → 14 篇文章页集体陈旧。
- **修复**：三处生成器均补"真调注入器 + 校验 returncode"；`deploy.mjs` 新增 0d) 步调用
  `inject_onboarding.py`；新增回归闸门 **L1.54**（21 项，4 阴性 + 1 阳性），
  用 **AST 检测模块级未定义名**（专抓"语法没错、一执行就崩"），拿 git 里带 bug 的原版实测精确命中第 315 行。
- **自身翻车与纠正**：抽检 1 条 THK 就推断 18 条全部声称 200 → 误判 `TDN-sava-cable` 为伪造，
  已回滚该编辑（原记录本就诚实写 403）。**抽样一条 ≠ 推断全体，也是「口径≠事实」。**
- 线上回探：706/706、OSS 325(实时 189)、`meta.access=True`、三类页面锚点均 706、探活 19/19、IndexNow 65×200。
- 提交：主仓 `5b86625` → 远端 `ea4d0503`（tree `b8521faf` 一致）；ops 仓 `4daf5e4`。待办仍 9 项，无新增。

## 08:25-08:33 用户任务 · Copilot 改走 Agnes（.cn 端点 + 给密钥）
- 用户要求把 Agnes 端点从 `apihub.agnes-ai.com` 改为 `apihub.agnes-ai.cn`，并提供了 `sk-...` 密钥。
- 改 `functions/api/copilot.js` 默认 BASE_URL=`https://apihub.agnes-ai.cn/v1`；用 `wrangler pages secret put`（stdin 传值不回显，已 sed 脱敏）写入
  `AGNES_API_KEY` / `AGNES_BASE_URL` / `AGNES_MODEL` 三个加密 secret（project=robotparts）。
- 部署后首次实测 `/api/copilot` 仍报 `503 agnes_not_configured` → **再部署一次后 secret 才绑定到活动生产部署**，复测返回真实解释（model=agnes-2.0-flash）。
- 已标记 `_NEEDS_USER.md` 第 74 项 AGNES_API_KEY 为完成；MEMORY.md AI 推理段同步更新（含"put 后需再部署"提醒）。
- 结论：Copilot「用 Agnes AI 解释」按钮端到端可用，密钥不进前端/不进 git。

## 09:02–09:25 · 飞轮第 40 次运行

- **修复：全站 17 页积压 107 份重复 CSS（已在线上），根因是接入区块注入器从来就不幂等。**
  `inject_onboarding.py` 的 `STYLE` 常量发在 `MARK_START/END` 幂等标记**之外**，
  `strip_marked` 只剥标记块剥不掉它，每跑一次净增一份。08:33 那次 main 发起的部署
  又把 index.html 推到 12 份。上一轮把注入器挂进 deploy 后，变成每小时自动膨胀。
- 治法：按签名剥离全部历史副本（含无标记 legacy）+ 前后空白对称剥离 → 注入器自愈，
  未写一次性清理脚本。实测 124→17 份，index.html 22.7KB→14.3KB（-37%），全站约减 90KB。
- 新增闸门 **L1.55**（8 项，2 阴性 + 1 阳性），以真实行为对照守幂等；
  修复首版仍每轮多 2 字节（真实页面恰好掩盖），被该闸门当场判红后补对称剥离才转绿。
- 已部署并线上回探：三处页面均 1 份、文章页体积与本地逐字节相同；
  部署后工作区 0 改动（修复前每次部署必脏 17 个）= 幂等的端到端证据。
- 待办 9→8：`POST /api/copilot` 探针实测 200，AGNES_API_KEY 经独立回探确认生效后核销。
- 提交：主仓 `46e3e17` → 远端 `3bfc55cb`（tree 比对一致）；ops 仓 `7fe06b0`。

## 11:35–12:15 飞轮第 42 次运行 —— 补齐两个「宣称了但不存在」的 API 端点 + 两道新闸门

- **主线缺陷（口径≠事实第 10 次）**：`GET /api/search?q=`、`GET /api/entity/{id}` 在
  `llms.txt`/`README.md`/`api/openapi.json` 三处都写着，线上**双双 404**——从未实现。
  catch-all `functions/api/[[path]].js` 只兜底 404，"目录里有东西在接"是错觉。
  杀伤点：`llms.txt` 是写给 AI Agent 的，Agent 按第一条指引就撞墙且不会追问。
- 顺带查出 `api/openapi.json`（机读、会被目录站抓）三处陈旧：412 实体（真 706）、
  漏 3 品类、宣称扣 50 积分（实扣 1，会让 Agent 因"太贵"而不调）。
- **实现要点**：付费字段既不参与匹配也不返回；数据集失败返 **503 而非空 200**
  （不把故障伪装成"没搜到"）；404 带同前缀 `did_you_mean`；显式列 `locked_fields`；
  `PREMIUM_FIELDS` 抽为单一真相源 `functions/_lib/paywall.js`，catch-all 改 import。
- **新闸门 L1.57**：三份对外文档的端点声明逐条对账到真实 handler，
  **catch-all 显式不算 handler**（否则任何 `/api/*` 都"解析成功"，闸门恒绿）。
  实测：临时挪走实现文件 → 精确判红 4 条声明 + 首页 schema.org SearchAction。
- 自查抓到自己两处：漏 `onRequestHead`（Pages 不自动 HEAD→GET）；
  隔离计数字段歧义（拆成 `quarantine_matched_withheld` / `quarantine_in_index`）。
- **自纠 + 新闸门 L1.58**：起手把"跨了自然日"当成"日报过期"，实算仅 20.76h，
  且已把「本轮已出日报」写进 `_LATEST.md`，写完顺手一算才发现。
  → `scripts/digest_due.py` 成为到期判定唯一入口；L1.58 含"跨天但仅 20.8h 必判未到期"
  对照用例 + "声称已出日报必须有物证/标记刚更新"两条对账；实测能判红。
- 新立 P0 情报：ISO 22166 模块化系列（22166-1:2021 待修订 / 22166-201:2024 模块公共信息模型），
  实测全库 706 条命中 0 次 = 继 GB/T 之后第二个标准维度缺口；媒体"10 项 ISO"无标准号，
  标记 `unverified_claim` 不进数据。
- 提交：主仓 `19638fd`、`67f5fcc`（均已推远端且 tree 校验一致）；ops 仓 `196c4ec`、`51060ab`。
  回归全绿、探活 19/19、706/706、OSS 325(189 live)、无 BLOCKED、IndexNow 65 条 200。待办仍 8 项。
- **追加教训（本轮第二次自纠）**：L1.58 第一版用子串匹配散文判断"是否声称出了日报"，
  结果把复盘里的**引用**当成**主张**，判了假红。危害不在误报，在激励方向——
  不改的话，如实复盘会被自己的闸门判红，最省事的规避就是别写复盘。
  **通则：闸门要读文档里的自然语言，必须用机读标记（如 `<!-- DIGEST-CLAIM: ... -->`），
  禁止子串匹配散文**，否则闸门会与"如实记录"对立。改后 L1.58 11 项全绿且仍能判谎报。

## 16:18-16:25 用户任务 · 百度主动推送打通
- 用户在百度站长「普通收录→资源提交→API提交」拿到 token（站点 `www.roboparts.cc`）。首跑 `promote.mjs` 返回 `not_same_site`（sitemap URL 是裸域 roboparts.cc，与已验证 www 主机不匹配）。
- 改 `scripts/promote.mjs`：①`BAIDU_SITE` 支持 env 覆盖（默认 HOST）②推送前把 URL 主机改写为已验证主机 ③加 `BAIDU_LIMIT=10` 避开百度每日配额。复跑 `{"remain":0,"success":10}`，10 条送百度、今日配额满。
- ⚠️ 注意：百度验证的是 www 子域，我们推送的也是 www URL；规范域(canonical)其实是裸域 roboparts.cc（sitemap/llms.txt 全用裸域）。若想百度索引裸域、避免双版本分权，应另把 `roboparts.cc` 加进百度验证，或在 Cloudflare 设 www→裸域重定向。此点已向用户说明，列为可选后续。
- 已标记 `_NEEDS_USER.md` 百度项完成、GSC 项保留（仅 Google 待验）。

## 16:18–16:45 飞轮第 44 次运行

- 修两处**闸门误伤**（同族第 3 次）：L1.21 只认 commit 把如实记录的 tree SHA 判成幽灵；
  L1.58 拿"现在"审判上一轮当时正确的 skipped 声明。改类型感知 + 按声明时刻现算（标记带 @时刻并锁死取值范围）。
- 通则再记：闸门要惩罚**错误行为**，不能惩罚**时间流逝**或**如实记录**——否则最省事的合规是闭嘴。
- 对照用例基准必须钉死在用例内部，不许读被测流程自己会改的状态（`digest_due.evaluate` 加 `last_raw` 注入）。
- Windows Git Bash 无 `md5sum`，空值比较恒真＝假绿；比哈希一律用 Python。
- 出日报 `_DAILY_DIGEST-20260808-1618.md`；`_NEEDS_USER.md` 真实计数 **7 项**（此前报告口径 8 有误）。
- 主仓 `73bf304`→远端（tree `bd69e4c6` 一致）；ops 仓 `d86fbf0`。

## 17:33–17:50 飞轮第 45 次运行

- 新增 `scripts/staged_guard.py` + 闸门 **L1.60**（16 项）：提交前必须 `--intent` 显式声明
  本轮改哪些文件，未声明的暂存文件判红；`scripts/ functions/ mcp-server/ adapters/`
  代码目录**禁通配**；mtime 早于本轮起点判 stale。兑现上一轮"把提交清单核对做成闸门"。
  真实场景实测非空转（故意漏声明 → 精确判红 exit=1）。
- 新立 P0 情报：**YD/T 6770—2026 具身智能基准测试方法**（工信部 2026 年第 3 号，6/1 已实施，
  信通院牵头，ITU-T SG21 推国际立项；gov.cn/人民日报/CAICT 三处一手互证）。全库 0 命中
  ＝第三个标准维度缺口。明确记下**不要**逐件打标（它评系统不评零件）。
- 巡检：回归全绿、探活 19/19、706/706 无漂移、OSS 325(189 live)、无 BLOCKED、未部署（无漂移）。
- 提交：主仓 `f97254e`→远端 `dd381c44`（tree `fb9c62c0` 一致）；ops 仓 `305e219`。
- **教训**：写时间戳前先 `date`。本轮两处判红全是"凭感觉写了未来时刻/错档位"自伤造成。

## 20:16 飞轮第 47 次运行

- **上一轮"已修复"是假的：线上一个字没变。** 实测线上 11/17 页仍展示
  `493实体/155执行器/103芯片/62传感器/7品类`（真值 706/217/108/90/10）。
  两个从未验证的假设同时塌了：①「推 GitHub ＝ 会自动上线」——Pages 根本没接 GitHub 构建；
  ②「本机无 CF 凭据、deploy 跑不了」——**wrangler OAuth 令牌虽过期但带 refresh_token，
  实跑即自动续期**。第 ② 条是上一轮写进 memory 的结论，纯属没跑就下判断。
  → **凡是"跑不了/做不到"，先真跑一次再说。** 这是「口径≠事实」第 10 次。
- 已 `node scripts/deploy.mjs` 真部署，并用新核验实测：线上 **17/17 页一致、0 页未核验**。
- 新增 `scripts/verify_live_numbers.py` + 闸门 **L1.62**，补上系统性盲区：
  regression 只读本地文件、probe 只看状态码，**没有任何东西在看线上正文写了什么**。
  设计上纠了一错：不能直接套 L1.61（它管"源文件禁字面量"，而线上是渲染产物，
  本就该出现 706）；线上该问的是"数字等不等于真相源现算值"。口径边界仍复用 L1.61 正则。
- **自己造过一次假绿并当场修掉**：首版在 17 页里 16 页没抓到时照样打印「✅ 一致」。
  改为 GREEN/UNKNOWN/RED 三态 + 传输层退避重试，UNKNOWN 退出码非 0。
  会撒谎的闸门比没有闸门更坏——下一轮的我会拿它当"线上已验证"。
- 巡检：回归全绿（含 L1.62）、探活 19/19、706/706、OSS 325(189 live)、无 BLOCKED；
  日报实算 3.81h 未到期（下次 8/9 16:27）。
- 提交：主仓 `3fbb88c` → 远端 `f9697510`（tree `bfb64f0b` 一致）；ops 仓 `4c803eb`。
- `staged_guard.py --intent` 是 **append 型参数**，多文件要写多个 `--intent`，不能逗号分隔。
- git commit -m 含中文引号/括号会被 shell 拆碎，一律写 `.git/COMMIT_MSG_TMP` 后 `-F` 提交。

## 21:18–21:32 飞轮第 48 次运行

- 修复回归闸门假红（锁死整小时部署）：`_LATEST.md` 如实写时间区间 `19:58–20:20`，
  旧解析只抓区间起点小时(19) → 判"落后于报告 slot 20"。改为区间感知（末刻生效、
  跨零点进位、跨度 ≤2h 否则退回起点防后门）。「惩罚如实记录」族第 4 次。
- 新增闸门 **L1.63**：数据内嵌站内路径必须真敲得开。entities.json 707 处
  `registry_ref: /api/mechanical_interfaces.json` 此前无任何自动校验
  （probe 清单没有 / regression 没查 registry_ref / L1.57 只管文档声明）。
  复用 L1.57 的 `_endpoint_handler`，不另造判定。
- 巡检全绿：探活 19/19、706/706 无漂移、OSS 325(189 live)、线上 17/17 页数字一致、无 BLOCKED。
  数据无漂移故未部署。P0 情报 6 关键词全部已在库，无新增。
- 提交 `4fb1cfc` → 远端 `5676bfc4`（tree 校验一致）；ops 仓 `3be0d12`。

## 22:05 自动化提升（用户授权"围绕自动化原则自主修复提升"）
- probe.mjs GET_PATHS 补 agent-architecture/build-planner/geo-dashboard/skills/manifest.json；POST_CHECKS 补 /api/adapter-feedback(ok=200/400)。飞轮每小时监控盲区消除，实跑 24 条全正常。
- promote.mjs 自动读 .env.local 的 BAIDU_PUSH_TOKEN/BAIDU_SITE + 每日节流戳(.baidu-push-stamp)，百度推送从手输升级为飞轮自动每日一次；实跑第1次推、第2次跳过。
- .env.local 写百度 token(www.roboparts.cc)，gitignored 不进仓库/前端；.gitignore 加 .baidu-push-stamp。
- 纯本地脚本改动，未部署(飞轮直接读本地)；.gitignore 改动待飞轮推 GitHub 同步。

## 22:30 飞轮第 49 次运行

- 修两条闸门误判（均为「惩罚如实记录」族）：L1.49 待办数对账改强制机读标记
  `<!-- TODO-COUNT: N -->`（顺带堵住 `if m:` 的静默放行假绿洞，改后更严）；
  日报声明档位 `==` 改 `>=`（只防回填旧时刻，声明比报告新是自罚不是逃逸）。
- 三种作弊真文件实测全判红；巡检全绿（24/24 探活、17/17 线上数字、无漂移未部署）。
- 主仓 `2772215`+`43938b0` → 远端 `a9e309d6`；ops 仓 `8fb6945`。待办仍 7 项。
- **踩坑记**：`git commit -F /tmp/x.txt` 在本机必失败（git 是 Windows 程序、读不到 bash 的 /tmp，
  且无 cygpath）——改用 `C:/Users/xing/AppData/Local/Temp/x.txt`。

## 23:40–23:55 · 飞轮第 50 次运行

- **补线上核验盲区**：`scripts/verify_live_numbers.py` 新增「对外 JSON 接口总数」段
  （`/api/data.json`、`/api/oss?stats=1`）。此前 OSS 这根轴**三道绿灯全够不着**：
  回归只读本地、probe 只看状态码、页面核验的口径名词表里根本没有 OSS。
  刻意不新建脚本 —— 本项目反复栽在「同一口径有第二个来源」。
  端到端实测：假站点只让 OSS 返 136 → 页面失配 0/17、接口失配 1/2，精确隔离成立。
- **情报纠错**：T/BTIAIRI 0001-2025《人形机器人电驱动一体化关节接口要求》
  实为 2025-12-25 已实施，我方 8/7 误记为「征求意见稿」。
  新纪律：**判断标准阶段只认题录库/发布机构公告，不认新闻稿时间戳**。
- 主仓 `a1a1fa1`→远端 `d1112c23`；ops 仓 `5adac89`。无部署（无站点文件变更）。待办维持 7 项。
