# 2026-08-16 工作日志

## 自动化运营补强（用户令："按你的计划实施 + 严格自动化运营补强"）
在 P0/P1 引擎（20260815-11 轮已上线）之上，把"监听→判别→度量→回流→展示→自检"接线成常态化闭环：

- **T1 只读监听器** `scripts/demand_scan.mjs`：扫 GitHub/ROS/Stack（Reddit 沙箱不可达自动降级），带 `channel_alive` 探测（不可达绝不判零需求，堵空表假绿）；现算机械声明率=0.85%（status∈{declared,partial} 才计）。输出 `ops/demand-signal-YYYYMMDD.json`。绝不发帖。
- **T2 端点+回流** `functions/api/demand-signal.js`（GET/HEAD，读静态 `api/demand-signal.json`，缺文件诚实 404）；`deploy.mjs` 0b3 部署前把最新 `ops/demand-signal-*.json` 复制到 `api/`。初版误读项目根→改读 `ops/` 后校验全绿。
- **T3 计分板回流** `scoreboard.html` 新增"真实需求信号（自动监听回流）"卡片，fetch 端点取代人工维护单元格；② 用户真实提问行改"线上·自动"。
- **T4 三个常态化自动化**（ACTIVE）：周五需求扫描改确定性脚本版（run_lock→demand_scan→deploy→release）；每日 06:00 数字保鲜（verify_live_numbers.py）；每日 07:00 站点巡检（probe.mjs）。

## 验证
- 部署校验全绿；`/api/demand-signal` 线上 200（real_query_count=10, mech_decl_rate=0.0085, channels_alive=3/4）；计分板新卡片就位。
- `verify_live_numbers.py`：线上 17 页 + 3 接口数字与真相源一致（708/11/325），零漂移。
- 锁：旧实例心跳停 742min 被判崩溃抢占，新 owner 343ae86c，收尾已 release。

## 仍待办（未做）
- P2 社区需求信号加权：先决条件 waitlist 加"零件级需求"字段（当前仅有 role+note）。
- H5 抬声明率：需真实 BOM 反喂 ingestion。
- ① 资本/生态兴趣仍零信号（Open Collective 待开通、Open Robotics 直邮待回复）。

## 修复所有问题（用户令，09 时）
- 真实盘点：www.roboparts.cc 旧站问题已不存在（主域/www 均返新站，5 端点全 200，8-11 P0 已随部署解决）；线上数字一致。
- **P2 已自主落地**：waitlist.js 加 `need_categories`（白名单校验+聚合 `stat:waitlist:need:<cat>`，自检不计）；waitlist.html 加 12 类多选；scoreboard 加"社区需求热度（waitlist 反喂）"卡片；顺手把计分板硬编 1.68%→真实 0.85%。部署校验全绿，线上自检 POST 带 need_categories 成功且不污染统计。
- 硬阻塞（不假装修，列给用户）：① 资本/生态（Open Collective 开通、Open Robotics 回复、agent-mail 绑定邮箱）；H5（真实 BOM 反喂）；公开仓 force-push 历史清理（不可逆，需用户决策）；git 14 文件未提交（推送走 gh_push.py，未动）。
- 运行报告：`ops/results/roboparts-20260816-09.md`。

## 修复收口（10 时续，用户令"继续替我完成修复"）
- **git 本地提交收口**：43 文件全提交，HEAD=`46b17b0`，工作树干净。CRLF 仅为换行符警告。
- **推送仍硬阻塞**：`git push` 实测 `could not read Username for github.com: terminal prompts disabled`（本会话无 PAT）；记忆所述 `scripts/gh_push.py` 实际未落地（仓库只有 `push_resume_force.ps1`/`push_resume_v2.sh`，非 Contents API 推送器）。→ 需用户提供 GITHUB_TOKEN(PAT) 走 API 推送，或用户本地推。
- 线上健康快照刷新：`verify_live_numbers.py` 全绿（17页+3接口，708/11/325 零漂移）。
- 运行报告：`ops/results/roboparts-20260816-10.md`。
- **诚实结论**：所有可自主修复项（P0/P1/P2/自动化闭环/H1/H2/H4/H6/H7）均已上线验证；剩余 4 项均跨账号/不可逆/缺凭据，AI 不假装修。
