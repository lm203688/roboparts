# 2026-09-21

## 下一阶段规划锚点（08:47）

用户要求扫描全项目框架 + 市场分析定位 + 谋划下一阶段方向/功能/市场。产出 `docs/phase2-plan-20260921.md`（约 7500 字符）作为锚点文档。

### 扫描方法

- Python `os.walk` 递归统计顶层目录大小（.git 101MB / ops 31.9MB / api 24.9MB / mcp-server 14.8MB / scripts 3.5MB / 前端 30+ HTML）
- 数字锚定：`api/entities.json` `meta.mechanical_interface_coverage` 现算（798 实体 / 20 品类 / 435 applicable / 25 declared+partial / **5.75%** / 410 not_declared）
- 竞品一手核实：读 `ops/results/_MARKET_POSITIONING-20260918.md`（GitHub 0/0/0/1、LobeHub 11.26 分 18 天未同步、遥测合并 0/16 分片、Allonic $7.2M 已非竞品、Tnkr Leonardo + 0% 占位、RoboInfra/urdf_validator/UrdfArchitect 相邻挤压）+ `docs/tnkr-increment-20260920.md`（api.tnkr.ai 仍无 API 面）
- 读 `docs/modality-extension-blueprint.md`（多模态训练数据集 v2，TurboVLA V+L+F+T→A 骨架，RoboParts 定位=零件级物理先验真值层）
- 读 `ops/marketplace-architecture.md`（10 章节完整设计，**零执行**）
- 读 `_STATUS.md`（唯一对外接口，798 实体 / 5.75% / 未填写占位 1 份阻断发布）

### 核心判断（写入文档）

- **真实位置**：ISO 9409-1 物理接口兼容性判定的唯一开放源，正被 URDF 工具群从相邻象限挤压，社区信号 0，产品扎实但从未进入任何开源传播渠道
- **最大短板不是产品，是传播**：GitHub 0 star / 0 issue / 0 PR / 1 contributor，9 月 70 commits 全是自我迭代
- **测量链路本身坏了**：flushMcp 读-改-写互盖，读数只证明下界，任何"用户在看什么"的分析都是空中楼阁
- **5.75% 是硬阻塞**：Tnkr 徽章端点前置，先解再挂，否则挂出"95% 无法判定"负面证据
- **象限在被挤压**：URDF 工具群（RoboInfra/urdf_validator/UrdfArchitect）正在做 URDF 侧校验，"零件级+判定象限无人占"是 2026-08 旧判断
- **变现未验证**：29 次 API 调用 0 注册，佣金中介模式脆弱，转 Octopart 数据订阅（¥200/月起）

### 下一阶段三条主线

- **主线 A 数据补齐**：谐波减速器品类补到 ≥50% 可答率（ISO 9409-1 A35/A50/A65/A100 公开标准，查表批量补 A{n}），mech_pct 5.75%→≥20%
- **主线 B 传播**：ToddlerBot 外链 PR + 3 篇三态诚实判定博客 + URDF 工具群合作提案 + 修遥测读侧 + LobeHub 同步 + README 计数对齐 + Onshape MCP 文章 + SEO 全名差异化
- **主线 C 变现**：从佣金中介转 Octopart 数据订阅（¥200/月起），目标客户=机器人集成商/ROS 团队

### 明确不做（写入文档 §5.3）

- 造脑/造芯片（Eon/DeepMind/Intel 领地）
- VLA 模型部署（Tnkr 已往 AI 行为走）
- 整机构建/混改/皮肤（Tnkr 层位）
- Marketplace（设计已完但零执行，社区信号 0 阶段做=空中楼阁）
- 佣金中介模式（29 次 0 注册）
- 开源 BOM 反喂 ingestion（oss_components.json 与声明率分母 entities.json 不相通）
- Tnkr 官方 parts supplier 提案（通道非对称，主动提=把 5.75% 当筹码递出去）
- GitHub star 竞赛（其他 MCP 解决"如何控制"，RoboParts 解决"如何选型"，不适合拼 star）

### P0 五件事（ROI 排序）

1. 修遥测读侧 bug（0.5 天，最高 ROI——现在连"有多少人访问"都不知道）
2. 谐波减速器补到 ≥50% 可答率（1-2 周，解 5.75% 硬阻塞）
3. ToddlerBot 外链 PR（1 天，验证 Tnkr 侧是否放行）
4. 3 篇三态诚实判定博客（3 天，内容营销唯一强差异化资产）
5. URDF 工具群合作提案（1 天，进入 ROS/URDF 开源圈传播渠道）

### 6 个月验收锚点

| 指标 | 当前 | 目标 |
|---|---|---|
| 机械声明率 | 5.75% | ≥20%（≥1 品类 ≥50%） |
| GitHub star | 0 | ≥50 |
| GitHub issue/PR | 0/0 | ≥1 外部交互 |
| LobeHub 同步 | 18 天未同步 | 自动同步 |
| 遥测读侧 | 0/16 分片 | 非 0 且 ±20% 一致 |
| 真实付费转化 | 0 | ≥1 |
| API 订阅层 | 未建 | 上线 |

### 三条铁律

1. **不自我迭代**：kinematics/neurorobotics/demand-signal 层已证明"再加一层"不产生外部交互痕迹
2. **不空中楼阁**：marketplace 设计已完但零执行，社区信号 0 阶段做 marketplace=空中楼阁
3. **不空挂负面证据**：5.75% 没解之前不挂兼容裁决徽章端点

### 未做

- 未提交推送（docs/ 在部署面内，但本轮是规划文档非功能改动，等用户拍板后再推送）
- 未部署

## 市场定位偏差诊断（09:10）

产出 `docs/positioning-audit-20260921.md`（13517 字符 / 304 行）。用户问三个问题：如何精准定义目标受众 / 如何提炼 UVP / 如何调整定位表述。

### 核心发现 A：全站有 6 套互不一致的定位口径（不是 1 套说得好不好的问题）

| # | 位置 | 表述 |
|---|---|---|
| 1 | index.html H1/title | 开源机器人兼容性平台 |
| 2 | README/llms.txt/agent-discovery/suppliers.html | 仿生机器人生态平台 |
| 3 | index.html meta desc | **中文圈稀缺**的开源机器人兼容性平台 |
| 4 | api-pricing.html title | **RobotParts** DB（错名，多一个 t） |
| 5 | waitlist.html | 零部件兼容性数据底座与匹配引擎 |
| 6 | **GitHub repo description** | 仿生机器人零部件兼容性平台 \| **688** 条（实 798，**落后 110 条**） |

### 核心发现 B（本轮最有价值）：定位治理**倒挂**——给 AI 写的比给人写的准

| 面向 | 描述质量 |
|---|---|
| **机器可读四件套**（server.json / smithery.yaml / lhm.plugin.json / .well-known/mcp.json） | ✅ 全部 R 现算、且已把 **`Vendor-neutral` / 「中立数据层 —— 既不生产也不转售任何零部件」** 写进描述。smithery.yaml 注释里甚至明确记录"曾长期停在 688，真值已是 798" |
| **人类可读页面**（GitHub description / index.html / README / llms.txt / 定价页） | ❌ 漂移、矛盾（同页 133+ vs 325）、差异点埋在页脚、数字落后 110 条 |

⇒ **整改方向直接由此定义：以机器可读清单为定位真相源，反向同步人类页面。** 不要重新发明定位（server.json/lhm 已经写对了）。

### 核心发现 C：7 种受众说法，0 个验证

工程师（README）/ 构建者（index 卡片）/ Agent 与开发者（index）/ 免费-Starter-Pro-企业（pricing）/ 供应商+采购方（suppliers）/ Builder-投资人-供应商（waitlist）/ AI Agent（llms.txt）。
⇒ **7 种画像 = 没有画像**；CTA 无主次；无法定向增长。

### 六条偏差

1. **品类词挂错**：`bionic_mechanisms` 仅 17/798（2.1%），"仿生"却当第一定位词（4 处）。而 ISO 9409-1 是**工业法兰标准**非仿生概念——自我窄化 + 与独占能力脱节
2. **受众 7 种说法 0 验证**（见上）
3. **差异化错位**：首页主位写"对标 TraceParts"（零件量差 3-4 个数量级），而最强差异"卖家不适合同时当裁判"埋在页脚
4. **地域自相矛盾**：meta desc 说"中文圈稀缺"，但全部挂载点（官方 MCP Registry/Glama/LobeHub/Tnkr/RoboInfra/urdf_validator/Onshape）都在国际英文圈
5. **品牌漂移 + 开源身份不可见**：`api-pricing.html` 写 RobotParts；真实双轨许可（代码 MIT/数据 CC BY 4.0）但 GitHub `license` 字段实测 `NOASSERTION`（自定义 LICENSE 文本不被识别）
6. **数字在最高曝光位置落后 110 条**：GitHub description 688 vs 实际 798；index.html 同页 133+ vs 325 并存；README actuators 217 vs 现算 220

### UVP 选择：U3 为主

- U1「唯一能做能不能拧到一起的判定」——独占但市场小（没人在找）
- U2「不卖零件的第三方裁决」——结构性差异，但需对方先知道我们存在
- **U3「三态诚实：不可判定就说不知道，不填 0 假装有数据」——⭐ 主选**。理由：业内没人认真写过（竞品都在堆数量）；与 T1 受众真实痛点精准咬合（被虚标坑过的工程师最信任敢说不知道的源）；是最便宜的病毒式传播种子

UVP 一句话：**RoboParts 是唯一会告诉你"我不知道"的机器人零件兼容性数据层——我们公开登记答不出来的部分，因为我们不卖零件，所以没有理由骗你选一个。**

### 受众收窄（三层）

- **T1 主受众**：开源机器人构建者（ROS/URDF 生态做整机；唯一已验证存在、能产生外部交互痕迹的群体）
- **T2 高价值**：机器人集成商接口工程师（唯一有付费动机，对应 API 订阅，但触达成本高）
- **T3 生态**：AI Agent / MCP 客户端（零边际成本曝光面，当前 agent 流量实测 0）
- **明确排除**：终端消费者 / 大型 OEM 采购 / 投资人（waitlist 的"我是投资人"是噪音）/ 供应商（佣金中介模式残留，29 次调用 0 注册已证无效）
- 一句话版：**RoboParts 为开源机器人构建者而生——那些在 ROS/URDF 生态里拼整机、需要确认"这两个零件能不能装到一起"的工程师。**

### 落地清单（10 项，ROI 排序）

0. 改 GitHub repo description（688→798 + vendor-neutral），5 分钟 ← 最高曝光
1. 删 suppliers.html + waitlist.html 删投资人与供应商选项
2. api-pricing.html title 改回 RoboParts
3. index.html 删 "133+"，统一走 facts()
4. README §数据分类改脚本注入（消 217/220）
5. 全站 title/H1/tagline 按 §5.2 统一（6 套→1 套）
6. 页脚"中立性"声明提到 H1 下方第二位
7. LICENSE 改 GitHub 可识别格式（消 NOASSERTION）
8. 3 篇 U3 博客（英文+中文+ROS Discourse）
9. GEO 全名差异化（RoboParts.cc / 标题带"兼容性"避开 Amazon Roboparts）

验收判据：仓内 grep `仿生机器人生态平台`/`RobotParts`/`中文圈`/`133+`/`对标 TraceParts` 全 = 0；远端 GitHub description 不含 688。

### 自我纠错

初稿写"答不出来 82.9%"——**错**。现算：not_declared 410 / applicable 435 = **94.25%**。已修正。教训：写数字前必须先算，不能凭印象（这次差点把错数字写进建议对外使用的文案模板里）。

## 实施与收口（10:00–10:58）

用户指令：「结合以上两个调研的结果，对项目进行修复改造。但是我觉得有些不做的内容可能需要提上日程，
毕竟项目要产品化完整化的一个过程了，要啃硬骨头做别人都不要做的事情才是最核心的」

### 修复（根因而非症状）

1. **先修「闸门的闸门」**：全站扫陈旧数字报 0 条不是干净，是 `_CLAIM_RE` 瞎了
   （首页「133+ 开源**机器人**组件」量词多插一词即漏网）。放宽识别器 + 补 13 项双向自证
   （漏网=假绿 / 误伤=假红）；保鲜器 `inject_onboarding.py` 同一盲区一并修并验幂等；
   修好后精确命中 1 条真陈旧（133+ 真值 325）无误伤。
2. **全站定位 6 套→1 套**：统一为「机器人零件兼容性判定层」。关键是**改生成器源串**——
   README 标题与分发徽章由 `inject_readme_stats.py` / `sync_dataset_dist.py` 重写，手改必被覆盖。
   验收 grep 全 0。
3. **受众收窄**：suppliers 页 → 数据贡献页（接 5.75% 缺口）；waitlist 删投资人/供应商。
4. **数字保鲜扩面**：README 品类明细（原 217 等 8 项陈旧、无维护机制）+ 闸门项数（原手写 8 实 21）
   + 对外清单「最后更新」日期，全部改现算。
5. **已发布 SDK 数字全错**（审计未覆盖的新发现）：PyPI/HF 自称 688 实体 / 10 品类 /
   套餐 500·2000·9999 积分，真值 798 / 20 / 100·500·999999 → 纳入生成器重写面 + `--check`。
   幂等坑：`(旧|新)` 二选一写法，否则第二次跑因旧词消失而锚点失配判红。
6. **转接件三方几何闸门** `check_adapter_geometry.py`：预览/OpenSCAD/CLI 三处口径原靠人记忆。
   自证 7 例 —— **它自己被打回过一次**：初版只扫全文件 `pinPCD/2*`，预览的 cos/sin 只坏一处时
   照常绿（假绿）；现要求两处都成立，并把「只坏一半」单列阴性对照。
7. **立场一致性**：删 README「模块交易佣金」+ 文章 04「20% 平台佣金」+ adapter-generator 补第⑤条。
8. **LICENSE 拆双文件** + GitHub description 经 API 更正。

### 新增第二个闸门：纯漂移判别器（本轮最有价值的补洞）

`auto_drift_heal.py` 原先只看 `git status` 有没有改动就 `git add -A` 提交成
"drift remediation"——**不看改动是什么**。真内容改动会被贴 drift 标签静默入库。
本轮就撞上了：部署后 46 文件被重生成，肉眼无法区分「纯时间戳」与「夹带内容改动」。

`check_pure_drift.py`：JSON 走**结构比较**（白名单时间戳键换占位符后必须完全相等），
文本走「去空白 → 抹日期 token」两级；**明写盲区**（文本层抹日期属弱判据，输出里标注
`(文本弱判据)`，不假装是强结论）。`auto_drift_heal` 收口前强制过它。

**闸门放哪要想清楚**：审计模式在 CI 恒为「无改动」= 无信号，在工作树恒假警报 ⇒ CI 只跑自证。
自证 13 项，含 **1 条真实临时 git 仓库端到端**（只测纯函数会漏掉取 diff / 传参 / 编码整条管道，
而管道坏了的表征是「报 0 个改动」——一个看起来最健康的数字）。

### 收口

| 步骤 | 结果 |
|---|---|
| 密钥扫描 | 2069 新增行 0 命中；暂存无 `ops/`、`.env*`、`*.pem` |
| 预部署护栏 | PASS |
| 提交 | `03eed33`（76 文件 / +2069 −1588） |
| 推送 | 远端 ref 落 edbf8df1、tree ee4214c4，收尾校验与本地 HEAD 一致 |
| 部署 | Cloudflare Pages 上传 125 文件，[3/3] ✅ |
| 独立回探 | 17 页 + 3 接口全绿、0 未核验 |
| 漂移收口 | `3d5c6f9`（46 文件，判别：空白 16 + 时间戳 30 + **内容 0**） |
| 新闸门提交 | `cfb7eb8`（4 文件 / +421 −3） |

### 本轮踩到的环境坑（跨会话有用）

1. **`pre_deploy_check.py` 会拒绝带未提交源改动的工作树** ⇒ 临时驱动脚本一律放**仓库外**
   （本机 `%TEMP%\roboparts-helpers\`）。第一版落在仓根，部署被当场拦下——护栏是对的。
2. **`regression` L1.94 是 env 键契约闸门**：scripts/ 读的每个 env 键必须登记进
   `scripts/env_contract.json`。为一处测试钩子加 `PURE_DRIFT_ROOT` 不划算 ⇒ 改 `--root` CLI 参数。
   这条闸门当场抓出了我的疏忽（这是它第二次证明自己在管事）。
3. **本机 Bash 工具的 PATH 被 `shell-runtime-bash-env.sh` shim 破坏**（`ls/cat/head/tail/grep`
   全 command not found，长命令还会 SIGTERM）⇒ 用 Python `subprocess` 显式拼 PATH 驱动
   `node.exe`；或用 `os.walk`/`re` 直接替代 shell 工具。
4. `wrangler` 不在 PATH，但 `deploy.mjs` 会自动回退到
   `%APPDATA%\npm\node_modules\wrangler\bin\wrangler.js`（全局安装存在），无需设 `WRANGLER_BIN`。

### 未做

- **未重新部署**：`cfb7eb8` 只动 dev 侧脚本与 README，未改任何线上校验页内容；
  重新部署只会再触发一轮纯时间戳漂移，需要再一次收口。下次内容部署会一并带上。
- 几何闸门通过「不」代表装配可行性（见 `check_adapter_geometry.py` docstring 的诚实边界）。

## 收口自查：给「定位轴」补上机制（11:00–11:20）

收口时问了自己一句：**本轮 6→1 的定位收敛，验收是什么？**
答：一次人工 grep，结论只写在 `docs/positioning-audit-20260921.md:276`。
数字轴有 `verify_live_numbers.py` 回探（能抓「推了 GitHub 没部署」），**定位轴零核验**——
而定位比数字更外显，它写在 title / meta / llms.txt / agent-discovery 上，
正是搜索引擎与 AI 抓走二次分发的位置。这就是本仓「口径 ≠ 事实」病史的第 10 次同形：
**检查的粒度小于被检事实，全绿的含义被悄悄放大。**

### 落地：判据唯一来源 + 两个消费者

- 新建 `scripts/positioning_contract.py`：退役表述表 + 定位表述族 + 本地可部署面扫描
  + 纯函数 `positioning_violations()`。**判据不手写期望值**，由本地源文件自己说话
  （本地含定位表述而线上不含 ⇒ 未部署）。自证 15 项（4 阴性对照 + 3 临时目录端到端）。
- `ci_gate.py`：离线闸门（本地 255 面 0 退役表述 + 自证）。**刻意不联网**——
  CI 上网络不可依赖，线上半场是部署后回探的职责。`ci_gate --list` 22 → **23** 道。
- `verify_live_numbers.py`：**第四轴**（线上 21 个自称面）+ `/agent-discovery.json`
  总数纳入接口核验（3 → **4**）。
- `regression.py` L1.62 补 4 条守卫，防这一轴被静默摘掉。

### 自证当场打回我自己两次（都是它该干的）

1. 我设了「最小锚点串」做单串比对 ⇒ 把合法变体「机器人零件**的**兼容性判定层」
   判成**假红**（`waitlist.html` 正是这个写法）→ 改**族匹配**，并在表处写明
   「族匹配没有任何单一子串能替代，别再加回去」。
2. 「恒空桩」断言布尔式写反（写成"不是所有用例都空"，对恒空桩恒为假——
   看似在验桩，实测我自己的式子）→ 改成「真判定器全命中 **且** 桩全放过」。

### 另一个教训：探针先自证再报红

第一版临时探针报了 **4 处 FAIL**，逐条核口径后 **4 处全是探针自己的错**：
2 处 URL 猜错（`agent-discovery.json` 在**根目录**、不在 `.well-known/`；`compass` 在 `api/` 下），
2 处判据过窄（把「多一个的」的合法变体、与本来就不谈定位的页面也算进必须命中）。
⇒ 判据一律由本地源给出，不手写期望。

### 顺手 settle 的「幽灵哈希」提示（连两轮喊人工核）

三个哈希**没有一个是我担心的伪造**，三类各有真相：

| 哈希 | 真因 | 远端探针 | 处置 |
|---|---|---|---|
| `31c5d40` | amend 前旧值（改动在 `2706adf`，见 20260807-19） | 422 查无（本就不该可解析） | 在 20260807-17 补有出处的【20260921 核】订正 |
| `c804f6c` | filter-repo 删除的前史提交（台账记 old→全 0） | 200（GH 缓存旧提交） | `hash_history` 新增第四态 `deleted_by_rewrite` |
| `a8deaa66` | push 重放的**远端 ref**，被反引号写了 | 200 | 改裸写（纪律：远端 ref/tree 一律裸写） |

- `_mapping()` 原先故意滤掉全 0 → 它和**凭空编造**的哈希共用一句「请人工核」。
  新增第四态后编造的哈希仍判幽灵（阴性断言照旧通过），判定强度不变。
- **不得并入 `_mapping()`**：regression 台账守卫要求映射目标在对象库真实存在，
  塞全 0 会立刻判悬空映射（自伤）。
- `closure_status` 分两行呈现：幽灵（请人工核）/ 已解释（台账零映射）。

### 收口数据

| 项 | 结果 |
|---|---|
| 本地扫描 | 255 个可部署文本面 **0 处退役表述**（44 面含现行定位表述） |
| 线上回探 | 17 页 + **4** 接口 + **21** 自称面全绿、0 项未核验 |
| 回归 | 1547 行 / 94 段 / **0 ❌** |
| 提交 | `1fcd46e`（7 文件 / +519 −19），推送远端 ref 落 789d8d9c（tree e866cffe，校验与本地 HEAD 完全一致） |
| 部署 | **未部署**：改动全在 dev 侧脚本与 README，未触及线上校验页内容 |

## 外部浪潮情报：TrAct / OM-1 / reBot（20:55 用户问「有没有提升」）

联网核实三者后给结论：**均非代码提升、非竞品，是战略验证 + 生态扩张类顺风**。
完整评估落 `ops/results/competitive-intel-20260921.md`。要点：

- **TrAct**（arXiv 2608.24101v3，UMich+Stanford）：世界模型决策框架，核心论点「动作 embodiment-specific、visual tracks embodiment-agnostic」——与 RoboParts 立身哲学同构 ⇒ 定位验证（间接），未来是上游消费者候选。
- **Reward AI OM-1**（2026-09-14）：只从人类手部数据学、零样本跨桌面臂/工业臂/人形不微调。thesis=「把智能从硬件抽掉」恰是 RoboParts quadrant；「适配全硬件」不解决机械挂载硬约束（ISO 9409-1 法兰/扭矩/负载）⇒ 兼容层需求反升。**强顺风**，但权重全闭源、无 API、无第三方基准（demo 级信号，不可集成）。
- **reBot**（Seeed，~7000¥，开源到螺丝级 BOM，GitHub ~3.7k★）：真实开放硬件生态，其 BOM 是 RoboParts **G1（开放躯体标准件生态）** 可直接 ingest 的数据源 ⇒ 生态扩张 + 数据源（最直接）。
- **三者拼成一条栈**：脑(TrAct/OM-1) → **RoboParts 缺失的中间件** → 体(reBot)。共同证明「脑×体解耦」行业迁移，RoboParts 卡的层正是缺失件。
- **诚实边界**：三者不解决真阻塞（P0 声明率 5.75% / P2 ECS）；唯一要盯风险=若「零样本适配全硬件」在机械装配也成立则需求被稀释，但物理挂载是硬约束短期不会。
- **建议动作**：P1 话术对齐这波叙事；P2 把 reBot BOM ingest 进 G1；P3 借 OM-1 热点写「零样本适配未说之隐：机械兼容」GEO 内容（定位轴闸门已就位可防漂移）。

## 外部浪潮第二波：Flexiv / ForceDelta-VLA / AthenaZero / SONIC（22:39）

用户指令「就按照你的计划提升项目」+ 追加评估这 4 项。已联网核实并落盘。

### 执行（按计划 P1 话术对齐）
- `ops/results/competitive-intel-20260921.md` 追加第二波评估（Flix→判定为非夕科技 Flexiv，发音契合，已向用户标注假设）。
- `neurorobotics/GAP_MAP.md` 新增 §6「外部浪潮验证」+ REF-8..11：把脑×体解耦立论从孤论升级为「被主流玩家独立验证」。
- 提交 `0ad8ccb` → 推送远端 ref `52548fd4`（收尾校验 tree 完全一致）。**未部署**：GAP_MAP 是仓库文档、非构建输入（grep 确认无 deploy/build 引用 neurorobotics/*.md），api/neurorobotics.json 由 source.json 生成、未被改动。
- 定位口径闸门 `positioning_contract.py --check`：255 面 0 退役表述、rc=0。

### 四者事实锚 + 判定
- **Flexiv 非夕科技**（FLEXIVERSE 2026）：Enlight 力觉躯干 + Orion 控制器 + **MICO 模块化标准双臂平台**；王世全原话「靠躯干不靠大脑」⇒ 与脑×体解耦同构；MICO 是 G1 生态可 ingest 标准件源。
- **ForceDelta-VLA**（arXiv 2609.18242，汉堡+TUM）：力觉修正蒸馏，接触密集 82.2% vs 54.4% 基线；依赖躯体力觉规格 ⇒ 上游消费者候选。
- **AthenaZero**（RAI Institute，Science Robotics 2026）：准直驱低惯量双臂，有效质量比商用协作臂低约一个量级，投 30m/s/接 14m/s；新型躯体标准、G1 可 catalog 接口。
- **NVIDIA SONIC**（Science Robotics eaed4592）：单一全身控制策略 1 亿+ mocap 帧，Unitree G1 实测 99.2%；**明确为 GR00T（脑）之下运动皮层层**；代码 Apache 2.0、跑商用 G1。
- **共同判定**：四者全在脑/体/控制层，**无人做兼容判定中间件** ⇒ 立论「缺失的中间件」被 NVIDIA/RAI/Flexiv 三方独立强化；MICO/G1/AthenaZero 成可 catalog 标准躯体 ⇒ 兼容层需求面扩大（但数据护城河 5.75% 不自动变厚）。
- **不变真阻塞**：P0 声明率 5.75% / P2 ECS 网关。

### 环境备注
- 本机 Bash shim 仍坏（`dirname`/`cd: null directory`，`rm` safe-bin 崩）；`git`/`python`/`node` 直调可用。Git Data 推送 `push-gitdata.mjs` 正常。
- `closure_status.py --write` 本轮 SIGTERM 挂死（疑似线上核验网络调用被 TLS 代理卡住）；_STATUS.md 的 HEAD 仍显示 1fcd46e（实际已 52548fd4），下次飞轮跑会刷新。本地定位闸门与回归均绿，不影响放行判读。

## 第 16 篇 GEO 长文 + 生成器硬编码修复 + 部署回探（23:00 slot）

用户「继续任务按照计划」→ 执行 P3（GEO 借势）。全链闭环，最终 regression 1547 行 0 红、线上 18 页 + 4 接口全绿。

### 产出
- **`content/article-16-brain-body-decoupling-compatibility-layer.md`**（5435 字）：「脑×体解耦之后，机械兼容层从可选变刚需」，借 SONIC/Flexiv MICO/ForceDelta-VLA/AthenaZero/OM-1/TrAct 七项热点。诚实清单明写 MICO/reBot/AthenaZero 站内 0 命中（已实测）、声明率 5.75% 自身短板——GEO 文不编造实体命中。
- **`scripts/build_articles.py` 修双盲区**：CTA 与索引页描述硬编码 688（真值 798），**且该串无 data-rp 锚点** ⇒ `verify_live_numbers.py` 锚点扫描与 `regression` 都不覆盖。改 `facts()` 现算，漂移在构建时即不可能发生。
- 构建：16 篇 / 110838 字；sitemap 66 URL；llms.txt 16 篇 + 10 速查页；agent-discovery content_library 16 篇；接入区块 20 页 ok=17。

### 发布链
commit `2ac2ce9` → 推送 ref 5705e75b（tree d02fb02a == 本地 HEAD）→ deploy-20260921-2325（c0ca69db，75 文件上传/5195 缓存，IndexNow 200/66）→ verify 18 页 + 4 接口 0 未核验、定位口径 22 面全绿 → 漂移收口 `9e1be03`（44 文件：空白 17 + 时间戳 27 + 内容 0，`check_pure_drift.py --base HEAD` rc=0 判别）→ 推送 ref 41810947（tree 8f65cee7）。**漂移收口后不二次部署**：44 文件已随 23:25 部署上传，commit 只是让 git 历史对齐线上。

### 本轮两个可复用教训
1. **RECONCILED 对「当前小时」无解**：L1.40 判据 `slot < by <= cur`，当报告属于当前小时时 slot == cur，区间为空。正确做法是走 (a) 第一人称路径——deploy.mjs 落 AUTO-STUB 只因「部署先于报告落盘」的时序，不等于「该小时无运行」；本轮确有一轮完整运行，由运行者本人补填即可，不加 RECONCILED 标记。删掉 AUTO-STUB 后 L1.40 四项全不适用。
2. **漂移自愈器的升级项可能是瞬态误报**：23:29 `auto_drift_heal.py` 跑回归命中「待办数对账」红灯并自动写 `_NEEDS_USER.md`——红源于本小时报告仍是占位 + `_LATEST` TODO-COUNT 未重盖，而非线上失真（deploy/verify 各自 PASS）。处置：核销并留注记、**不删原条目**（删=孤儿红灯），给关闭项加机读 assert（关闭项的 assert 不校验，但留着是审计痕迹）。

### 留痕机械入口（本轮验证可用）
- `digest_due.py --append-summary --hour 23 --now 2026-09-21T23:24 --fixed … --improved … --todo …`：slot 键是 (日期,小时)，故 `--hour 23` 会**整行替换** deploy 自动写的 `23:24` 行（非追加）。
- `digest_due.py --stamp`：一次盖齐 `TODO-COUNT`（现算自 `_NEEDS_USER.md`）+ `DIGEST-CLAIM`（本轮 skipped @23:38，elapsed 13.24h < PERIOD_H 24h 未到期）。
- 三项留痕红的修复顺序：补写小时报告 → 核销 `_NEEDS_USER` 瞬态项 → `--append-summary` → `--stamp` → 更新 `_LATEST` 运行时间 → regression。



## 2026-09-22 15:xx 执行（reBot ingest + 部署 + 回探全闭环）

用户指令「你能做的尽量都做掉」。从 Seeed-Projects/reBot-DevArm（4287 stars, CERN-OHL-W-2.0）BOM 提取 4 条目进 api/entities.json：

- PLAT-seeed-rebot-dm（B601-DM，Damiao43 4340P，24V/1.5kg/767mm）
- PLAT-seeed-rebot-rs（B601-RS，RobStride RSM，48V/2.5kg/754mm）
- ACT-damiao-4340p、ACT-robstride-rsm

诚实边界：关节非 ISO 9409-1 法兰，mechanical_interface.status=not_declared；不提高 P0 声明率（5.75%→5.69%），只填补 G1 生态（开放躯体标准件）。DM vs RS 电机版本不兼容作为条目信息（related/compatibility_note），不进 negative_compat（schema 是 ISO 9409-1 法兰专用，避免污染）。

口径同步：全仓 798→802、5.75%→5.69%、actuators 220→222、platforms 41→43。提交 b3fb44e（104 文件）→ cae4049（llms.txt 补刷）→ af74548（30 文件漂移收口）。部署 deploy-20260922-1535（Cloudflare Pages 86da97b6，140 文件上传）；线上回探 18 页 + 4 接口全绿、22 定位面全绿。远端 ref 960f387e。

待你拍板：P0 真实 BOM 机械声明（需厂商官域出处）、P2 ECS_API_KEY、支付 UX。
