注意：.workbuddy/memory/2026-09-09.md 尚不存在，本次为创建。

## 2026-09-09 24 轮（四视角审查采纳修复，09 档）

- **审查核验先行**：用户下发技术/用户/投资人/领域四视角审查。按「先核口径再动手」核验 6 项技术主张：3 属实（FILE_MAP 缺 connectors、dialects 评分硬伤、重试无抖动）、2 部分失真（权重和实际都=1.0 但维度名错配更重；budget_note 实为单行）、1 与代码不符（middleware flush——recordMcp 每 event 即 waitUntil flush）。审查中「声明率 1.12%」为过期口径（现 2.76%）。
- **采纳 8 项**：①npm 端 FILE_MAP 补 connectors（P0，整类搜不到）+ ALL_CATEGORIES 派生 + 7 enum；②dialects.js 双硬伤修复：switch 维度名对齐模板实际维度名（joint_freedom 等，旧大部落 default 伪 50 分）+ spec() 兼容平铺/key_specs 两层（旧读错层伪 0 分）+ Σ(w·s)/Σw 归一化 + 无证据维度 null 不冒充 50 分；70 例回归 0 坏；③fetchJSON ±50% 抖动；④两端 overall_compatible 统一；⑤search 多词 AND + 0 命中 hint；⑥compare_components 两端（npm 端 ID 跨品类自动解析）；⑦dialects_list/evaluate 接入（npm 端 8 工具 stdio 端到端实测）；⑧onboarding_block「5 个工具」硬编码→从 mcp.js TOOLS 现算（_mcp_tool_names + data-rp=mcp_tool_count 锚点）。npm 1.0.2→1.1.0；skills 三件套 13 技能；BUSINESS_TOOLS 白名单补 compare_components。
- **不采纳（记录理由）**：reputation.js 接入（fs 依赖+Workers 不可复用+无贡献链=空转）；共享库大重构（风险>收益）；budget_note 压缩（诚实披露）。数据类主张（bus_class 731/798 unknown、interop 684 unknown、BOM 拆解）归 P0 BOM 待办。
- **发布闭环**：b426a4b（55 文件）→ 推送 ref 落 be22bc0c → deploy [3/3]✅（IndexNow 200/65）→ 漂移 1044204 → verify 17页+3接口全绿 → regression 全绿 → _STATUS 放行 → 锁释放。
- **踩坑（复用）**：①**同一消息内对同一文件发多个 Edit 会有部分编辑静默丢失**（本轮 dialects.js 与 index.js 各丢 1-2 处，工具却报成功）——同文件多处修改必须逐个发、发一个 grep 验一个；②`_DAILY_DIGEST` 正文须手写在 ops/results/（digest_due.py 只判定/盖章/摘要，不生成正文），出刊后手改 _last_digest.txt 再 --stamp 才会判 issued；③BUSINESS_TOOLS 白名单在 scripts/read_metrics.py（regression 的工具一致性闸门读它），新增 MCP 工具三处必同步：mcp.js TOOLS（真相源）→ skills.meta.json（散文）→ read_metrics.py（白名单），然后跑 gen_skills_manifest.mjs。

## 2026-09-09 25 轮（增长渠道归因基础设施 + 渠道/竞赛客观评估）

- **用户两问**：①"下一步你直接执行"；②"能否对接已有机器人需求平台做外溢流量起步，或通过竞赛让投资方/业内了解项目"。
- **先装量具再谈渠道**：核验发现"有没有外部流量"技术上答不出，三层断点全修：①`register.js` 的 `attribute()` 只把 source/detail 写进单条用户记录、无聚合键 ⇒ 补 `stat:via:<src>:<detail>`（仅非自检）；②渠道标记原只认 HTTP 头 `x-roboparts-via`，浏览器点链接不带自定义头 ⇒ 补 URL 参数 `?via=`（头优先参数兜底），两侧同规则清洗 `[^a-z0-9._-]`+截断40防键污染；③`_middleware.js` 补 `via:<ch>` 点击计数，与注册配成漏斗（否则分不清"没人来"vs"留不住"）。
- **新脚本 `scripts/channel_report.py`**（自测 16 例阴阳对照全绿）：渠道漏斗/转化率 + `--link <渠道>` 生成 4 条带 via 的落地页链接 + `--days`；**非渠道注册（referral/agent/web）单列不混入 ROI**。两侧埋点同在 USER_CREDITS（middleware 写 `metrics:<day>:s<shard>` JSON，register 写 `stat:via:` 单键）。
- **渠道结论（docs/growth-channels-20260909.md，含事实来源）**：撮合型平台（亦庄"机因工场" 2026-07-30 发布，自带选型选件中心+BOM 一站式选型+TURN-KEY）**是竞品不是上游**，且线下定向撮合、流量不经公网，采购需求不外溢 ⇒ 不采纳。能外溢的只有**内容型**（古月居 100 万+开发者/300+企业/400+高校）与**被集成型**（Octopart/KiCad-atopile/LCSC）。
- **竞赛结论**：窗口多数已关——创客中国具身智能专题赛截止 8/31、ITEC2026 截止 7/15、2026 杭州国际具身机器人大赛已结束（5/15-16，省经信厅主办，杭州本地，明年优先）；唯一时间吻合 **ROSCon China 2026（10/16-17 上海，古月居主办，开源项目路演+招商，799-999 元+差旅）→ 需用户拍板**。不建议现在主打竞赛：评分表对"数据+工具"不友好、与 SwarmLabs 竞赛线争精力、**瓶颈是价值未验证（可比参数 0 条、声明率 2.76%）而非知名度，顺序应是先补 P0 再曝光**。
- **发布闭环**：ea447e4（4 文件）→ 推送 ref 落 8b5785cc → deploy [3/3]✅ → 漂移 24 文件 → verify 17 页+3 接口全绿 → regression 全绿（修 deploy AUTO-STUB 两档 10/17 补写 + _LATEST 档位对齐）→ _STATUS 放行 → 锁释放。待办仍 2 项 + 1 项新增待拍板（ROSCon）。
- **踩坑（复用）**：①`_LATEST` 运行时间行**必须含标准 HH:MM**（如"2026-09-09 17:41"），写成"25 轮 10 档"会被 `_latest_slot()` 解析失败并静默退回上一条，导致同口径闸门红；②部署每次会在当前小时建 AUTO-STUB，一天多轮部署就要补写多档（本轮 10 与 17 两档）；③`ops/` 在 .gitignore，留痕提交常显示"nothing to commit"属正常，不要误判为未保存。
