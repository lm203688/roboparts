# 2026-09-05

## 闭环回探收口（03:33–03:55）

检查上一轮任务是否完成 + 复盘闭环缺陷并直接修完。

**上一轮遗留（全部已清）**
- 29 个文件改动未提交（api/*.json + data.js 仅 `updated` 时间戳漂移；5 篇文章的
  217→220 修复从未入库）→ commit `2732538`。
- 03-16 AUTO-STUB 已成过去小时占位 → RECONCILED 补写（commit `2732538`）。

**新增并实证：预部署护栏 `scripts/pre_deploy_check.py`**
- 复用 verify_live_numbers 的 number_mismatches/claim_mismatches，部署前扫本地待部署
  散文，对照 facts() 真相源；工作树脏或数字矛盾即 exit(1)。
- 首次部署即命中并拦下 3 处——其中 2 处是我自己的误改（见下），1 处是护栏自身缺陷。
- 目的达成：结构性封堵「脏部署 / 过期数字上线 → verify RED → 二次闭环」的复现路径。

**护栏自身两个缺陷（已修）**
1. **子集计数误报**：护栏把实体/品类数一律按全库总数判。`data-hub.html` 的 9 大品类
   （CATEGORIES 数组确有 9 项）、`iso-9409-flange.html` 的 589（实物子集）、
   `robot-joint` 的 708（entities_scanned 子集）都是合法子集计数，非过期数字。
   → 扫描面收窄至 verify_live_numbers 线上实际校验的页面集。
2. **被自身 regen 反噬**：deploy.mjs 在 preflight **之后**跑 regen_derived.py /
   inject_*.py / export_training_dataset.mjs，刷新 25 个派生产物各 1 行时间戳。
   原脏树闸门一刀切 → 上次部署自己造的漂移让下次部署永久被拦（实测命中）。
   → 改两级判据：源内容改动一律拒绝；派生产物仅纯时间戳 diff 放行，非时间戳仍拒绝。

**我自己的误改（已回退，`8c58083`）**
- 把 `robot-joint-parameter-spec.html` 的 entities_scanned `708` 改成 `798`，判为过期数字。
  实际 `api/parameter_semantics.json` 的 meta.entities_scanned=708 是「被参数语义扫过的
  实体数」，与全库 798 是两个口径。落地页锚点闸门 L1.29 立刻抓出脱钩（页面=798/JSON=708）。
  教训：护栏报的数先核真相源口径，再动手——护栏只是提醒，不是判决。

**其他 regression 修复（6 项归绿）**
- P0 待办的事实前提 `not_declared: 389` 已过期 → 现算 411（applicable 418 /
  not_applicable 380 / declared 2 / partial 5 / not_declared 411）。
- 03-16 RECONCILED 报告引用了远端 ref SHA（`3a3ceb59`/`2cb25b58`）= 幽灵哈希违规 → 清除。
- `20260904-10` 占位以 RECONCILED 补写；`_LATEST` + DIGEST-CLAIM 走 `digest_due.py` 机械重盖章。
- 当小时报告（`20260905-03`）不能用 RECONCILED（补写者须晚于被补写时段）→ 改第一人称活报告。

**最终状态**：HEAD `7a1be9f`，tree clean，regression `✅ 全部通过`，线上 [3/3] 校验全绿。

**遗留（用户侧，AI 不编）**
- P0 真实 BOM 机械接口声明（现算 not_declared:411）。
- P2 copilot 后端升级（点亮 ECS 网关 + 设 ECS_API_KEY + 定 ECS_MODEL）。

## 续（15:31，按计划继续）

- **计划下一步 = commit→push→deploy→verify 的 verify 段**：跑 `verify_live_numbers.py`，线上 17 页 + 3 接口数字全与真相源一致（798/20/325），✅ GREEN，无漂移。git 工作树 0 脏。
- **P0 真实 BOM 机械接口声明缺口量化（真实数据，未编造）**：418 个 applicable 实体中仅 2 declared + 5 partial，411 not_declared。按品类缺口：actuators 216 / sensors 92 / platforms 30 / grippers 23 / flexible_actuators 22 / reducers 14 / controllers 4 / structural 3 / cables 2 / power 2 / pcb 2 / connectors 1。已声明 2 个：Robotiq 2F-85 Gripper、Robotiq FT 300 F/T Sensor；partial 5 个含宇立 C025XX/C075XX、IKO CRBF、Molex MiniMix、Yeah Robotic Hand。
  - 最高价值优先项：`platforms`(30) 的 UR5e/Franka FR3/Kinova Gen3 等定义了下游所有 ISO 9409-1 法兰基准，应先填。
- **P2 ECS 后端升级仍阻塞**：agent 机 `curl 150.158.119.19:8420` 仍 `502 CONNECT tunnel failed`（exit 7，不可达）。路由代码已就绪（设 Cloudflare secret `ECS_API_KEY` 即零代码切自托管），但仍需用户确认 ECS 在线 + 给密钥。
- 结论：闭环/verify 段已绿，仅 P0（真实零件数据）、P2（ECS 密钥）两项属用户侧硬阻塞，AI 不替用户编造 BOM。

## 续（17:26，Tnkr 生态位调研 + 三项实施）

- **Tnkr 一手拆解**：api.tnkr.ai 存在（FastAPI）但 openapi/docs 关闭；用户 cfut_ token 以 4 种姿势打 /api/* 均 307/404，疑为网页会话邀请码（beta 走 tally.so），未入 git。项目页结构：Files/PRs/**Mods**/Skins/Builds/Leo AI/Stats。Manifesto 实锤商业模式=开发者工具→frontier labs 数据采集市场抽成，不碰零件⇒与 RoboParts 兼容层互补非竞争。
- **交付物**（commit f326fd58，已推送部署）：① docs/ecosystem-integration-tnkr-202609.md（生态位对照+拿来用/衔接清单）；② docs/contribution-loop-design.md（贡献闭环：GitHub PR 流→站内表单 AI 预填→厂商门户，evidence_tier 四级闸门 T0 拒收）；③ compatibility-viewer.html（three.js 法兰+转接盘 3D 查看器 PoC，9 档 canonical 镜像 build_negative_compat.py，direct/adapter_required 现算）。
- **P0 新杠杆**：开源整机官方 BOM 是 T1 证据源——AgiBot X1（github.com/AgibotTech/agibot_x1_hardware）/Fourier N1/OpenLoong/天工/XLeRobot（readthedocs 四区域价格 BOM）/reBot Arm，走 ingest_oss_bom.py 批量入声库比等社区提交快一个量级。
- **竞品动向**：orobot.io 已做 XLeRobot BOM 分销页（无兼容引擎），反向印证差异化。
