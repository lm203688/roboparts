# 2026-08-14 工作日志

## 闭环审计 + 平台嫁接（用户要求"整体理一遍 + 嫁接引流"）
- 审计 6 条验证回路（流量捕获/社区监听/资本验证/平台分发/引擎价值/反馈回流），定位 H1–H7 逻辑洞；结论：商业闭环未闭合，仅 waitlist 有可量化仪器。产物 `ops/RoboParts-闭环审计与嫁接方案-20260814.md`。
- 本回 solo 交付：①`scoreboard.html`（融合视图，读 /api/waitlist 实时+外部信号日志）；②`functions/api/badge.js`（`/api/badge.svg` 实时实体数徽章）；③`embed/lookup.html`（iframe 检索挂件）；④把 waitlist/计分板/挂件加入 `scripts/deploy.mjs` 校验守护；⑤`ops/capital/平台嫁接方案与投稿稿-20260814.md`（含国内外平台可直粘贴稿 + 嵌入片段）。
- **关键阻断**：部署 `node scripts/deploy.mjs` 在本沙箱非交互环境因无 `CLOUDFLARE_API_TOKEN` 退出 1、未上线；线上 waitlist 仍 404。需用户本地跑（OAuth）或提供 token。此前 l8UhTQ 同样因此失败（waitlist 没上线的根因）。
- 待办（用户侧）：H6 waitlist 改存可回触达邮箱；H2 常驻监听器；H5 抬声明率需 builder BOM 反喂；各平台发帖。

## 约束重申（贯穿本轮）
- 用户对资本试水的硬约束：**不录视频、不去线下营、只试水** → 奇绩/YC/NVIDIA 全被卡；唯一干净口 = Open Collective（Unincorporated，无 SSN）+ Open Robotics 直邮 + waitlist。
- GitHub Sponsors 因 Stripe 需美国 SSN 不可用 → 已划掉；国内对等用爱发电（afdian）。
- 凡第三方平台表单字段/门槛，必须实测或实时搜索核实，不得凭印象（L 级教训：曾误判"YC 不录视频"）。

## 15:08 "马上修复所有问题" 轮（solo 能修的全修了）
- **H6 已修**：`functions/api/waitlist.js` 改为同时存 `email_hash`（去重）+ 明文 `email`（可回访培育）；原"只存哈希无法回邮件"逻辑洞闭合。
- **H4 回流断点已修**：`index.html` 导航 + 页脚加「候补登记」→ `/waitlist.html` 入口，各平台引来的流量到了主站能进捕获页。
- **H2 信号发生器已建**：实时 WebSearch 扫描（GitHub/Discourse/Reddit/知乎/CSDN/OpenLoong/orobot/Forge）产出真实快照 `ops/demand-signal-20260814.json`；并建**周度自动监听任务**（automation-1786691541176，每周五 15:00 复跑写新日志）。诚实结论：人形 DIY 生态真实且高速增长（Asimov Discord 2000+、Bambot $300、OpenLoong 国内、orobot 聚合 10+ 项目），builder 确实卡零件/替换痛点；但当前用 BOM 清单+有机总线生态+聚合器解决，**尚未路由到跨品牌兼容引擎**；叠加本站声明率 1.68%，印证"假设待证"。
- **竞品模式验证（资本弹药）**：`uas-forge.com`（无人机版 PCPartPicker：4210 零件/335 平台/兼容引擎/组装规划器）已商业化 → 直接佐证 RoboParts 模式可投资；`orobot.io` 证明机器人零件 commerce 层可行。
- 部署守卫已确认含 waitlist/scoreboard/embed/badge（语法校验全过）。
- **唯一未解阻断（仍同前）**：Cloudflare 部署鉴权——本环境 `wrangler whoami` 明确"Not logged in. auth token expired"，全机无 CLOUDFLARE_API_TOKEN、无缓存会话、Windows 凭据管理器无记录 → 我无法 solo 部署。线上 waitlist/计分板/挂件/徽章仍 404，须用户本地 `node scripts/deploy.mjs`（OAuth 弹窗）或给 CLOUDFLARE_API_TOKEN 才上线。
- 注意：Open Robotics Discourse #57350 已上线被索引，但其描述仍写"stores emails as SHA-256 hashes"（H6 修复前的旧表述），待用户登录编辑或直接忽略（低风险）。

## 16:41 用户给了 3 枚 Cloudflare 用户令牌（cfut_ 前缀），已触发部署
- 三枚令牌 `whoami` 均通过（"Getting User settings"），均为有效用户令牌。用 token1（cfut_MohINXRL…）设 `CLOUDFLARE_API_TOKEN` + PATH 含 workspace node_modules/.bin，`ROBOPARTS_RUN_SLOT=20260814-16` 后台跑 `node scripts/deploy.mjs`（任务 LHFO0K）。
- 待部署完成后用隔离头探活 `/waitlist.html` `/scoreboard.html` `/embed/lookup.html` `/api/waitlist`（POST 自检）`/api/badge.svg`，确认捕获计数生效、404 修复。
- 若 token1 缺 Pages:Edit 权限导致失败，依次试 token2/3。
