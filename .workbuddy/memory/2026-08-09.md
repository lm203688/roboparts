# 2026-08-09 工作日志

## 01:01 总指挥飞轮第 51 次运行

- **补线上核验第三轴**：`GET /mcp` 的机读 `dataset.total_entities` 此前无人核验
  （regression 只读本地 / probe 对 /mcp 只发 POST 只看状态码 / 页面段 live_paths 无 /mcp）。
  它是目录站与 MCP 客户端直接采信的字段，mcp.js 有写死 688 被 Registry 抓走的旧账。
  收进 `scripts/verify_live_numbers.py`（第三条 `_API_TOTALS`），线上核验仍单一入口。
  阴阳对照 6 条 + 端到端实测（假站点仅 /mcp 返 null → 接口失配 1/3 exit=1；真值 exit=0）。
  副作用已先查：recordMcp 按 `x-roboparts-selftest` 分流 `selftest:mcp`，不污染遥测。
- **情报纠错 ×2**（`ops/intel/standards-correction-YNIPA033-20260809-00.md`）：
  ① 《人形机器人连接器》= **T/YNIPA 033-2026，2026-06-12 发布并实施【现行】**，
  我方 8/7 记成「CAEE 刚启动编制」；归属也错（CAEE 那份是 2026-268T-CAEE，7-24 立项）。
  ② 《人形机器人用连接器通用技术规范》《机器人连接器接口尺寸标准》两标题**题录库查无**，
  源自厂商软文，却被次日情报以「已记录」引用 —— 未证实项经内部引用提级成事实。
  原节保留 + 加证伪标注。
- 提交：主仓 `0c48760` → 远端 `1d6618d8`（tree `97613e32` 自校验一致）；ops 仓 `570bdb8`。
  两仓 0 改动。本轮无站点文件变更，未部署。待办仍 7 项，无新增。
- 巡检：回归绿 / 探活 24/24 / 线上 17 页 + 3 接口全一致（0 未核验）/ 706==706 /
  OSS 325（20h 新鲜）/ 无 BLOCKED / 日报未到期（下次 8/9 16:27）。未使用任何任务清单工具。

## 02:22 总指挥飞轮第 52 次运行

- **拆兼容引擎假绿雷**：`functions/_lib/compat_engine.js` 的 `mechanicalEvidence` 原把
  `mount_type` 与 `standard`/`flange` 并列当可比对特征，导致两个仅共享
  `mount_type=direct_mount`（standard/flange 均 null）实体被判 `compatible:true`
  且作为硬约束撑起 `overall=true`，其自身 gap 却写明「无法做孔位级互换判定」。
  改为只认身份键（standard/flange）进交集；仅声明 mount_type 无身份键时返 null，
  `inconclusiveNote` 第三分支点出「仅声明安装方式 X，未给法兰/标准规格」并转述厂商 gap。
- **新增 L1.64 九项阴阳对照闸门**（`scripts/regression.py`）：源码层禁 mount_type 入身份键
  + 行为层拿生产源码实跑（阳性 3：宇立真实配对判 null / 全库假绿 0 / 反事实填 100 条仍 0；
  阴性 3：同标准仍 true、异标准仍 false、partial+真法兰仍可比对；文案 1：点名缺法兰规格）。
  还原修复前原版实测 6 项转红、3 项阴性对照仍绿，证明精确命中非空转。
- **情报证伪 UL1604:2026 伪造（第三种失效模式：对抗性伪造监管新闻）**：该标准 2012-07-31
  已撤销，由 ANSI/ISA 12.12.01 取代、OSHA 不再承认；信源为机器生成 GEO 软文
  （`/news/GEO-Articles/`、文末自述 generated from、点名供应商导流）。
  记 `ops/intel/disproof-ul1604-2026-fabricated-20260809-02.md`：GEO/agent-native
  路线同渠道存在对抗面，摄入即成伪造放大器。
- 提交：主仓 `6397f8e` → 远端 `8db61368`（tree `6969295d` 自校验一致）；ops 仓 6 文件。
  已部署 + IndexNow 66 条 200；线上双路回探（REST `/api/compatibility` + MCP
  `check_compatibility`）均 `overall=null`/`score=null`，文案点名缺法兰规格。
- 待办 **7**（无新增）。节点纪律补遗：`rm` 用 `python -c "os.remove"`（safe-delete 钩子拦 rm）；
  `git commit -F` 用 `C:/Users/...` 形式；MCP 参数 `component1_id`/`component2_id`；
  `audit_data_quality.py` 有写副作用（推进 `data_quality.audited_at`）。
- 下轮起手：补 declared 机械数据现可安全推进（雷已拆），优先 standard/flange、逐条留 source_url。

## 06:52 第 55 次飞轮运行 · 上一轮成果未部署事故 + L1.69

- **发现**：上一轮（L1.68 口径 694→507）39 个文件只 `git add` 未提交、**也未部署**；
  线上 Schema.org 一直播「694 条零部件、3 条市场情报」（真值 507/17），
  而 regression / probe / verify_live_numbers **三道全绿**。已部署+补提交+推送
  （`6860b49`、`8b5430f` → 远端 `8eba5264`，tree `98c65bdb` 自校验）。
- **L1.69 新闸门**（核验器盲区，两个独立缺陷）：
  ① `_NOUN2KEY` 没有新增五档口径的名词 → 取到数字 `key is None` 静默 continue，
     而「未核验 N 项」统计的是**页面数**不是**断言数**，故显示"0 项未核验"；
  ② `html_to_lines` 把 `application/ld+json` 当代码随 `<script>` 一起剜掉 ——
     JSON-LD 是 GEO/AI 读者的主要事实源，从来没被核验过。
  名词表已并入 `regression._KIND_NOUNS`（L1.61/L1.62 单一来源）；
  L1.69 以 `facts()` 反查覆盖，`*_entities` 漏接即红。
- `staged_guard --begin` 起手脏改判红（return 3）；L1.62 的导入断言由字面写法改为断言性质。
- 情报 A 级：GB/T 47864-2026 工业移动机器人调度接口国标（发布 7-02、实施 2027-02-01、
  SAC/TC 591）。属 specification、面向 AMR 调度报文，与零件级兼容仅边缘交集，不改定位。
  落 `ops/intel/std-GBT47864-2026-*.md`。
- 待办仍 7 项，P0 仍 ROS Discourse 投稿（须本人发）。

## 09:17 用户指令"按自动化原则你自己处理"7 项待办

- 用户要求对 `_NEEDS_USER.md` 的 7 项本人操作事项按自动化原则自主推进。
- 诚实边界：**7 项全卡在用户凭证/资金/登录/社区禁爬，无法由 AI 代完成**；我做的是"无需凭证即可做的验证与预备"，并把每项的人的动作压到最小一步。未勾掉任何待办。
- 已做实：① ROS 稿件通读、数字与真相源一致可直接投；② 支付链路逐行核验 `create.js→/api/payment/notify→notify.js` 代码侧全闭合（验签+写KV+plan=pro+防重），只差真实扫码；③ Glama `/.well-known/glama.json` maintainer=61960005@qq.com 与登录邮箱一致（登录即认领）；④ npm 闸门 `regression.py:3548` 正确锁定 `roboparts-mcp-server`；⑤ 用 `git log --all --diff-filter=D` 精确定位 13 份运营文档路径（全 `ops/**`，在公开仓历史）。
- 仍硬阻塞（须用户）：ROS 登录发帖、真实扫码付款、GSC/CF DNS TXT、npm 生成 Granular token、Glama 登录认领、agent-mail 绑定；git 清洗因 github 被墙强推不可行 + 实时泄露口已堵，建议非合规不下。
- 已更新 `ops/results/_NEEDS_USER.md` 追加"AI 自动化预备"段。未部署（无站点文件改动）。

## 10:05 首页"商业模式"区块设计判断

- 用户截图问"这个需要放在首页上吗"。经核对，该区块已在 `index.html:127-142` 存在（"商业模式：免费工具→数据飞轮→三层变现"）。
- 给用户的诚实建议：它**已经在首页**；对以工程师为主的访问者来说，这个 investor-deck 语气的区块不是最有价值的首屏内容，但放在工具卡片之后、API 接入之前也不算大错。
- 若首页面向投资人/合作伙伴，保留；若面向构建者/工程师，建议要么下移到更底部、要么改写成"How it works"式的用户价值说明，避免"锁客""变现"这类商业措辞带来的侵略感。未擅自改动页面，等用户决策。

## 09:35 第 57 次飞轮运行 · Tier-A 注水治理收口

- **L1.70 落地（Tier 分级服从本仓 tier_definition）**：新建 `scripts/source_scope.py`
  （按 URL 形状判出处范围，零人工登记表，单一真相源）+ `scripts/govern_source_scope.py`。
  全库 88 条「Tier A + 仅厂商首页」注水，73 条 vendor-scope（含 2026-08-05 点名的
  maxongroup.com 反例 `MOT-maxon-re25`）封顶降 B；2 条编造 GitHub 来源
  （`XDA-007` puppeteer / `XDA-008` telekinesis，GitHub API 实证 404）隔离 quarantine。
- **trust KPI 归位**：traceable_pct 57.91%→**47.32%**（挤出 10.6pp 注水）；
  Tier A 410→335、B 223、C 150，实体总数 708 不变。
- **棘轮不误伤诚实治理（L1.71/72）**：L1.71 把"新增劣化"重定义为"新实体 ID 带脏"，
  存量查出脏须留 `quarantine_reason`；L1.72 要求朝松改基线必须留 `_adjustments`
  机器可复核记录（legacy_discovery / recalibration 两类自证）。基线诚实松动：
  max_quarantined 95→97、max_unverifiable_vendor 30→32、min_traceable_pct 55.2→47.32。
- **修派生漂移 + 闸门生效**：`meta.data_quality`（clean 609→607、quarantined 99→101、
  breakdown）与 `llms.txt` 披露行按真相源校正；`quality-baseline.json` 从 gitignored
  的 `ops/` 迁到 tracked 的 `scripts/`，**L1.72 闸门今后才真正生效**（此前因文件不被
  git 追踪，self-proof 永远 skip）。
- 回归**全绿 EXIT=0**（L1.70/71/72 三层含阴阳自测过）；已部署，线上 verify_live
  **17 页 + 3 接口一致（0 未核验）**、probe 24/24。提交 `bb7f125`→远端 `bdf91b6a`
  （Git Data API 自校验 tree 一致）。待办仍 **7 项**，P0 仍 ROS Discourse 投稿。
- 教训：治理脚本（`govern_source_scope.py`）只改实体字段、**不重算 `meta.data_quality`
  账目**，导致 clean/breakdown 漂移 2 个发布周期才被 regression 抓到；今后治理后须跑
  `normalize_categories.py` + 确认 `meta.data_quality` 与 llms.txt 同步。

## 11:21 第 58 次运行 · 留痕机制自造孤儿的根因修复（L1.73）

- **开局回归 EXIT=1，工作区却是干净的**（HEAD=bb7f125 就是上轮宣称"全绿"的代码）。
  根因：`deploy.mjs` 的 `ensureRunTrace()` 按**墙钟小时**决定占位落哪一格，隐含前提
  「部署时刻的小时 == 该轮报告的小时」——**对飞轮结构性不成立**（09:35 起跑、10:06 部署，
  报告在 09 格 → 10 格被落下一份永远没人填的占位）。它同时点燃 L1.32＋L1.21＋L1.63 三闸门。
- 修法：占位改按**运行轮次 slot** 归属（`ROBOPARTS_RUN_SLOT=YYYYMMDD-HH`）；未设变量行为
  完全不变（保住 L1.40 主线/人工部署留痕）；slot 只能向过去且 ≤6h（防丢进远古格当免检通道）。
  **飞轮今后调 deploy 必须带 `ROBOPARTS_ACTOR=flywheel ROBOPARTS_RUN_SLOT=<本轮slot>`。**
- 10 点孤儿以 RECONCILED 补写（据 git 痕迹重建、对账 bb7f125），未伪造第一人称。
- 新增 **L1.73**：harness 提取生产源码里 `ensureRunTrace` 的函数体原文执行（括号配对截取 +
  `new Function` 依赖注入 fs/path/spawnSync/ROOT/process），临时目录跑 7 种情形，九项对照全过；
  **拿修复前原版实测命中**（旧版落墙钟格、新版落本轮格）——非空转。
- 提交 `1d619b7` → 远端 `a0b6480f`（tree `ea128a7f` 自校验一致）。部署成功、线上校验通过、
  `verify_live_numbers.py` 17 页+3 接口全绿、`probe.mjs` 24/24、本地线上均 708 无漂移。
- 情报克制：《协作机器人模块化机械臂 通用接口要求》仅新闻稿（称计划 8 月发布），
  **题录库 cssn/ttbz 查无此标 → 不入库**；T/WEA 122-2026、T/CAEE 060-2026 均已在库无变化。
- 待办仍 7 项无新增，P0 仍 ROS Discourse 投稿；日报未到期（18.89h，下次 8-09 16:27）。

### 元教训（本轮最值得记的一条）
- 8/9 06:43 那次**同形态**孤儿已经被看见过，当时判语是「属误报噪声…若反复出现可考虑加判据」，
  记进备忘录就算交代完了。三小时后它把回归卡死、禁止发布。
  → **判定一个红灯为噪声之前，先确认它到底会不会阻断。**"看起来像噪声"和"确实无害"是两件事；
  归类为噪声必须附带"它的后果上限是什么"的判断，否则等于给自己发了一张缓期执行的假绿。
- 另一条：**监控机制自身会成为故障源**。留痕/占位这类"为了发现问题"的机制，若其时间模型与
  被监控对象的真实节奏不一致（墙钟 vs 运行轮次），就会稳定地生产假阳性，并训练下一轮去放宽它。

## 12:34 第 59 次飞轮 · 机械维度首条真实数据 + 引擎多值假红修复
- `compat_engine.mechanicalEvidence` 身份键用 `String(v)` 把数组塌缩成单伪 token → 集合内含对方孔位仍判不兼容。改 `idValues()` 展开+去重求交集；新增 L1.74（13 项阴阳对照，修复前原版实测命中，全库 248865 配对 0 假绿）。
- ACT-028 Robotiq 2F-85 录三值 ISO 9409-1 孔位集合（官方手册 §6.1.1，出处 B→A）；机械 declared 0→1，fill 1.12%→1.40%。脚本 `scripts/curate_mechanical_20260809.py`（幂等）。
- T/CAEE 060-2026 日期更正：仓库记 7/31 实为新闻稿时间戳，题录库载明 2026-06-10 发布并实施、7-24 公布、现行；补录同批漏记 073-2026。
- 两轮部署闭环：`7ad87bd`→远端 31421301、`97b8d2d`→远端 0f6db546；verify_live_numbers 17 页+3 接口 0 未核验；probe 24/24。
- 教训：已立的入库纪律不追溯清洗存量记录（060 是存量，第一次立纪律时没排查）；沉睡缺陷靠"没数据触发"活着，灌第一批数据前先审该维度比对逻辑能否吃多值。

## 14:03 第 60 次飞轮 · 标准登记表出处闸门 L1.75 + 存量清洗
- 新增 **L1.75**：登记表凡有日期/状态断言（issued_at/effective_at/announced_at/plan_released_at/last_reviewed_at/status）必须挂 `evidence` + `evidence_tier`，出处 host 限白名单（ttbz/std.samr/openstd.samr/cssn/iso.org）。13 项阴阳对照：拿修复前原版实测命中 4 条存量违规；阴性拒 toutiao/weixin/bzchaxun、拒"只有 tier 无链接"与"只有链接无 tier"。
- **存量清洗**：20262893-T-604、20261783-T-604、ISO 22166-201:2024、ISO 22166-1:2021 四条"事实正确但零出处"补齐引用。
- **选择性偏差发现**：原 6 条 = 2 团标 + 2 在研国标 + 2 ISO，**零条现行且直接管零部件接口的国标**。根因是入库触发器是"新闻提到人形机器人标准"而非"作用域命中零部件接口"。补录 GB/T 38560-2020（通用驱动模块接口）、GB/T 43200-2023（一体化关节性能试验），另立 registry_watchlist 7 条在研计划（仅记立项事实，不进判据）。
- 提交 `5c66efa` → 远端 `194070d6`。
- 教训：**凡是新立一条"今后必须如何"的规矩，同一轮就要问：按这条规矩，现存的东西有多少不合格？** 只堵新入口 = 给存量违规发永久豁免。

## 14:25 第 61 次飞轮 · 占位脚枪收尾 + 作用域驱动首条补录
- **deploy.mjs slot 解析脚枪**：只认 `YYYYMMDD-HH`，飞轮传裸 `13` 被拒后**静默回退墙钟 14**，跨小时部署自造孤儿 AUTO-STUB `14.md`，连锁点燃回归覆盖缺口、锁死发布。改为接受裸 HH（归一今日）+ **flywheel 传非法 slot 直接抛错中止**。`e2d40c2`。
- 覆盖缺口消除：`_SUMMARY` run-60 行由误标 14:00 归位 13:00，孤儿 14.md 落为本轮真实报告。
- **T/WEA 122-2026《轮足式机器人及直驱关节模组技术要求》**（现行 2026-07-16，中国智慧工程研究会）由题录库 cssn.net.cn 命中补录——上一轮点出偏差后**第一条作用域驱动进来的标准**，登记表 8→9。同批厂商软文重复的两"查无此标"连接器标题正确拒收。`87f9ca7` → 远端 `04f7b8ec`。
- **端点事实更正**：登记表 meta 由 `/api/entities.json` 承载（线上实测 200，9 条含 T/WEA）；`/api/data.json` 是**不带 meta 的另一投影**，勿再按"data.json 带登记表"口径写。
- 核验：回归 EXIT=0；probe 24/24；L1.62 线上 17 页 + 3 接口 0 项未核验、全部一致。
- 教训：L1.73 修了"占位按轮次归属"，漏了"裸 HH 解析不下"这一支。**修解析分支必须把输入形态（空/完整/裸/非法）列全做阴阳对照，少一种就留一个静默回退口，而静默回退口迟早变成孤儿占位。**

## 15:26 首页改写用户价值版 + 清理历史任务列表

- 用户指出首页「商业模式飞轮」区块是投资人/合伙人视角，网站应面向用户（工程师/构建者）。
- 改写 `index.html`：
  - Hero 副标题从"免费工具锁构建者，深度数据与供应商线索驱动增长"改为"一站式查清楚…能不能装到一起、能不能互相替换"。
  - 原"商业模式：免费工具→数据飞轮→三层变现"区块改为"三步找到能用的零件"：找零件 → 查能不能配 → 拿替代方案。
  - CTA 主次调整：主按钮"免费检查 BOM 兼容性"，次按钮"按场景选型 / 问 AI Copilot / 技术文库"。
- 部署验证：`node scripts/deploy.mjs` 通过；线上 `curl https://roboparts.cc/` 已返回新版本；IndexNow 200、Bing 410、Google ping fetch failed（非阻塞）。
- 任务列表瘦身：当前项目已约定"禁止定时任务用 TaskCreate/TaskList 生成侧边栏任务列表"，结果只走 `_NEEDS_USER.md` + 本对话。本次把 TaskList 中 27 条已完成历史任务全部 `deleted` 清理；未来总指挥飞轮运行不再新增侧边栏任务。
- 闭环任务（ROS 投稿 / 支付验证 / GSC / npm / Glama / git 清洗 / agent-mail）仍继续在 `_NEEDS_USER.md` 跟踪，不受任务列表清理影响。

## 15:00 第 62 次飞轮运行 · 全站数字保鲜（口径≠事实第 10 次）
- **发现**：L1.62 报「17 页全绿」，实际线上 16/16 页在播错数字（首页 700+/300+、16 篇文章 CTA 688、data-hub 435+ 低报 39%、articles/index 的 meta+og+twitter+JSON-LD 六处 688、credits 577）。**核验器只看 data-rp 锚点 → 锚点全绿被当成页面全绿。**
- **修**：`inject_onboarding.py` 三层保鲜（全页锚点 / 区块外裸文本含 meta 属性值 / 子集口径 SUBSET_RULES），均幂等、均已挂 `deploy.mjs`。识别器只留一份 `onboarding_block.stale_count_claims`，静态闸与线上核验共用。
- **闸门**：新增 L1.76（全站 43 页数量断言，18 项）、L1.77（子集口径，10 项）。
- **线上核验补盲**：`verify_live_numbers.py` 增 `claim_mismatches`（无口径标记的裸句照查）+ `html_to_lines` 抽 meta description/og/twitter（文字在属性里，抹标签会连内容删掉）。部署前实测 16/16 判红（非空转），部署后 17/17 绿。
- **子集数字新真值**：`facts().caee060_relevant = 469`（llms.txt 写死 451 已三周）。
- 提交 `c92c896` → 远端 `df501b7c`；ops 私有仓 `b413584`。回归 EXIT=0，probe 24/24，日报 `_DAILY_DIGEST-20260809-1630.md` 已出。
- **用户当日 15:26 另立长期约定**：RoboParts 官网面向**用户**而非投资人/合伙人；"锁客/变现/商业模式飞轮"类措辞不得出现在面向用户的页面，投资人版内容归计划书。首页已改为「三步找到能用的零件」。

## 16:40 竞赛渠道调研（用户明确需求：资金+算力，倾向模型公司/大厂主办）
- 筛选并逐一核实窗口期，固化 `ops/competition-channels-20260809.md`。
- **正开着、最对口**：
  ① **GOAI 世界人工智能开源大赛**（杭州市开源 AI 基金会主办，阿里云+蚂蚁+之江实验室承办；500万池；Token+算力补助、西湖区落地最高百万级云资源；TRACK 01 新智基座 Agent Infra = RoboParts 本体；作品提交 8/16(3赛道)/8/20(具身)，剩 1~1.5 周）→ **本周首选**。
  ② **Build with Gemini XPRIZE**（XPRIZE+Google，$2M，Google 全栈算力；构建期至 8/17；硬约束：须 5/19 后新项目+90天真实收入，RoboParts 已存在需做新垂直）。
- **滚动开放**：AI Grant（Nat Friedman/Daniel Gross，$250K + $600K+ 算力 Andromeda Cluster，最接近"无限算力"，但强筛选/英文/国内访问摩擦）。
- **已过期/不对口**：百度千帆杯(奖金小)、火山引擎(全截止)、智谱(京西4/30/算子7/20/大学生5/30)、腾讯(雄安/创业之星均截止)、Google.org Impact(4月)、Anthropic(8/2/已截止)。
- **诚实判断**：没有真正"无限算力"；最现实的大算力国内入口是 GOAI 的西湖区落地云资源补助；AI Grant 算力包最大但最难拿。用户处中国大陆，Google/OpenAI/Anthropic 云存在访问摩擦，GOAI 零摩擦。
- **GOAI 报名数量规则（8/9 核实）**：公开规则无限报条款；用户已报 1 个项目，第 2 个若不同项目+不同赛道大概率可报。Agent Infra 赛道硬门槛=AgentTeams 协同基点+≥3 职能 Agent+Skill 体系；Boundless Agents 仅要求真实产业闭环。规则已补进 competition-channels-20260809.md。
- **GOAI Agent Infra 初赛材料已备（17:0x）**：基于 RoboParts 真实能力（MCP Server 已上线/Glama收录/708实体/11品类/325 OSS/9标准/469 T-CAEE060域）包装成四职能 Agent 闭环，按赛道手册初赛清单写 `ops/goai-agent-infra-submission-20260809.md`（作品简介≤500字 + 13页PPT脚本 + 自检清单）。诚实标注：AgentTeams 仅初赛设计基点映射，复赛才补代码包。假设用户已报此赛道，待确认。
