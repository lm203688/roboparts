# 2026-08-17

## 完成 P0–P4 + rerun 可视化（参考 rerun.io 的有用部分全部纳入）

用户指令："参考 rerun 对我们项目有用的纳入；评估里 P0–P4 均未落地，需要全部完成。"

- **rerun 参考性结论（沿用前次判定）**：rerun.io 是机器人运行时多模态可观测性工具（Rust+wgpu+Arrow+WASM），兼容性匹配引擎——低/不对口；可视化呈现层——高（最划算）；ECS 数据模型——中；录制回放/训练闭环——低。本次只取其"可探索可视化"范式，纯 Canvas 2D 复刻（不引 Rust 全栈、不引外部 CDN，契合中国网络+无 GPU 约束）。

- **P0 数据飞轮**：已 commit(035e6f6) + 部署 + verify_live_numbers 三态回归通过（17 页 + 3 接口与 708 真值一致）。贡献层 142 条（开源 BOM 反喂），运行时合并不污染主库。
- **P1 本地语义层**：`scripts/build_semantic_index.mjs` 离线哈希 TF-IDF 向量索引（824 实体×256 维，零依赖零外发零模型——本机无 embedding 模型、HF 不可达）；`compat_engine` 加 ensureSemanticIndex/semanticSearch/neighborsOf；`judgePair` 仅在 overall=null 时附 semantic_hint（**绝不**把相似度写进 compatibility_score，避免假绿）；`/api/semantic-search` 端点。
- **P2 标准自校验**：`scripts/build_standard_audit.py`（登记表↔实体声明交叉校验）+ `api/standard-audit.json` + `standard-audit.html`。诚实结果：6 机械声明（2 核实/4 未核实），4 行标覆盖（hbba），4 条真实可核实性冲突。已修掉"非标准接口(PWM/TTL 等)误判冲突"的假红。
- **P3 行为回流**：`/api/recommend-feedback`（采纳/忽略埋点，isolate 缓冲+分片 flush 复用 metrics: 前缀；冷启动仅采集不调权）。
- **P4 MCP 护城河**：`mcp.js` 新增 `bom_compatibility_check` / `semantic_search` / `get_standard_audit` 三工具。
- **rerun 式可视化**：`compatibility-viz.html` 三面板——①兼容性判定过程（四维动画）②BOM 兼容图谱（力导向，绿/红/灰边）③声明率快照（实时算，基线标线）。

## 诚实口径（重要）
- 机械接口声明率真实基线 **0.28%**（仅 2 实体声明机械标准），贡献层 **0 条机械声明**。评估文档里的"0.85%→15%+"目标**当前无法诚实达成**——飞轮必须有真实 BOM 贡献（用户/开源项目提交带 ISO 法兰的零件）流入才能抬升该维度。已如实写入各页面说明。
- 新增 `ops/seed-bom.json` 入口尚未建（待用户提交真实 BOM 触发审核流）。

## 已知 drift（预存，非本次引入，待修）
- `inject_api_access.py` 模板写死 `mechanical_interface_declared_pct: 1.68`（真实 0.28%），且 `cross_vendor_comparable_grade_a: 0`。属注入器模板 stale 数字，每次部署都会注入 API JSON 的 `access` 块。不在 P0–P4 范围内，列为后续待修项。

## 部署（已完成）
- 全量 commit `e96bb24`（P1–P4 + viz + 页面互链）已落。
- `node scripts/deploy.mjs` 部署成功（exit 0，[3/3] 校验全过：私有目录 404 拦截、实体数+内容指纹一致、关键页面正常）；推广脉冲已发（IndexNow 200、百度推送 success 10）。
- `verify_live_numbers` 三态回归：17 页 + 3 接口全 GREEN（实体数稳定 708，未核验 0）。
- 新端点/页面经探针确认 200：`/standard-audit`、`/compatibility-viz`、`/bom-import`、`/api/standard-audit.json`、`/api/semantic-search`、`/mcp`（8 工具齐全，含新增 3 个）。
- 端到端功能实测：recommend-feedback 接受埋点、bom_compatibility_check 返回诚实 `overall_compatible:null`+semantic_hint、get_standard_audit 返回审计+access 元信息。
- 运行报告已落盘：`ops/results/_LATEST.md`（三段式覆盖）、`_NEEDS_USER.md`（新增 2 项真实待办）、`_SUMMARY.md`（追加一行）、`roboparts-20260817-09.md`（覆盖占位）。运行锁已 release。
- 本会话 git HEAD：`e96bb24`（P1–P4+viz），P0 单独 `035e6f6`。

## 17:45 需求修复轮（用户："需求修复的你全部帮我修复掉"）

- **核对 NEEDS_USER 3 项未完成待办**：① agent-mail 绑定（账号硬阻塞，AI 不可代劳）② 真实 BOM 机械声明（数据缺口）③ inject_api_access 1.68% stale（误判）。
- **关键纠正**：此前记 "mech_pct 0.28% / 1.68% 是 stale" 是**口径混淆误判**——0.28% = status=declared 2 / 全库 708；官方 mech_pct 1.68% = (declared 2 + partial 4) / applicable 358，由 onboarding_block.facts() 现算、regression.py 用 truth['mech_pct'] 校验、对外 JSON 已通过闸门。**1.68% 正确，非 stale**，该项核销。
- **已落地修复**：① `compatibility-viz.html` / `standard-audit.html` 改用官方 mech_pct（从 entities.json meta 实时取），与回归闸门一致；② 新增 `ops/seed-bom.json` 提交模板（含 schema 示例），`build_flywheel_layer.mjs` 验证可合并（空模板 0 条、管道无错），BOM 贡献入口端到端就绪。
- 部署 `831899f` 成功，verify_live_numbers 17 页+3 接口全 GREEN；线上 standard-audit.html 经 Node fetch 确认含 1.68% 且 0.28% 仅作子口径澄清。
- 锁：本轮回 acquire 后，部署时锁已归 owner=d6c7cc59（疑似小时飞轮），renew 被拒（正常），未强释他人锁。
- **诚实边界（向用户直说）**：第 ② 项"真实 BOM 机械零件"是数据缺口，AI 不编数据冒充已修复；需用户/社区提交带出处的真实零件（UR5e/Franka FR3/Kinova Gen3 的 ISO 9409-1 法兰），提供清单即补录重部署。
