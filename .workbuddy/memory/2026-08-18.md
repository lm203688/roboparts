# 2026-08-18 工作日志

## 战略对话：RoboParts 商业模式诊断 + px-footrix 评估
- 用户问三件事：① px-footrix 对项目有无提升；② 总结进度与技术优势；③ 商业模式是否出问题（推广无见效）+ 其他证明价值的方式。
- **px-footrix 结论**：帕西尼 2026-08-06 发布的**足底多维触觉传感器（硬件产品）**，非框架/算法/可视化方法论 → 套不上"rerun 式参考纳入"玩法；但可作 1 条高质量 seed 实体（规格公开详尽：SPI/UART/RS485/USB/I2C/CAN FD、3-5V/12V、IP67/68），补 protocol/electrical 覆盖，对最缺的 mech_pct 帮助有限；作为市场信号双刃（验证零部件真实存在需兼容映射，但帕西尼融资35亿暴露 solo 项目资源量级差距）。
- **商业模式诊断（坦诚）**：方向（兼容层）有真实需求，但"人类工程师搜索 + 9元付费墙 + 缺厂商数据"这条路对 solo 项目概率低。根因：引擎被数据饿死（mech_pct 1.68%、多数查询返 null）/付费墙前置/目标用户选错楔子（个人工程师不买单，厂商/集成商/Agent 才买单）/推广无差异化 artifact/与用户真实优势（SwarmLabs agent-infra）错配。
- **证明价值的推荐路径（ROI 排序）**：A 兼容报告生成器（输入 BOM→真实结构化报告，最强眼见为实 artifact，代码现成）；B 转向 B2B 厂商兼容审计换数据 feed；C 主打 agent-native 基础设施（P4 MCP 已就位，契合 SwarmLabs）；D 开放数据集为信任凭证（ModelScope 已发）；E 撤/降付费墙。建议：短期做 A 作展示，叙事切 C+B，E 立刻执行。
- 待用户拍板：先做 A（兼容报告生成器）还是先谈 B 的厂商名单。

## 辩证重审（08:01）+ SwarmLabs 关系澄清（08:08）
- 用户批评我"一时一个说法"不客观。已认：mech_pct 口径反复横跳是最明显证据；且我总建议"再建一个"有自利偏差（最擅长造东西）。
- 实跑 facts() 核准：total 708（仅 509 为零件）、mech_pct 1.68%（6/358）、标准评估 11.72%（83/708，完整互操作栈仅 11）、溯源 Tier A 12.85%（曾虚报 47.32%）、OSS 325 条带下载链接。无埋点/无度量。推广大部分是草稿未发。
- **SwarmLabs 与本项目关系（已查证）**：工作区无任据表明两者有数据共享/管道。证据仅：①同运营者（厉兴）旗下兄弟站点，swarmlabs.tools 域名；②RoboParts 测试账号曾用 @swarmlabs.tools 邮箱；③用户是 SwarmLabs 的 GOAI 报名负责人（非 RoboParts）。entities.json 里的 "swarm robotics" 只是分类标签，无关。→ 上一轮我把 RoboParts 说成"SwarmLabs 的数据护城河"是**无依据推断，收回**。两者目前是平行站点，非上下游数据关系；若用户设想 SwarmLabs 多智能体消费 RoboParts 零件数据，那是"尚未建立"的关系，需用户确认意图。
- 用户确认（08:15）：两项目独立推动，配合**只走 API、禁止代码相互污染**。→ 彻底排除"数据护城河"定位；RoboParts 须独立自证价值，API 机器可读层（MCP/entities.json/llms.txt）即正确集成面，SwarmLabs 只是外部 API 消费者之一。

## 生态节点审计 + 扩面工作流（14:15）
- 用户要求：路线X（扩面）立刻做 + 确认是生意按商业逻辑推 + 评估"飞轮没扩大覆盖面" + 3D打印/配件/电子设计成熟度 + 上次推过的类似平台 + 生态关键节点是否齐全。
- **Route X 落地**：`scripts/backfill_platform_flanges.py` 把 `mechanical_interfaces.json` 的 `flange_designations.known_hosts` 回填为 `platforms.json` 一级实体（带 ISO 9409-1 法兰）。新增 **15 个臂平台**（UR5e A50 + ABB 全系 A50/A80/A100/A160）。幂等验证通过（dry-run 加 0）。`platforms.json` 40→55，本地已写、**尚未部署上线**。
- **生态节点审计结论：关键节点没齐全**。已有 11 类偏"智能/驱动层"（actuators217/chips108/sensors90/protocols64/...）；**缺失一等品类**：电源/电池、减速器/谐波、夹爪/末端执行、结构件/机架、线缆/线束、控制器/驱动器、PCB/电子布线。这是"飞轮没扩面"的症结——飞轮加的是 OSS 数+智能层，机械/电源/结构/互联骨干从未建。
- **设计工具成熟度**：①3D打印适配器 `adapter-generator.html`=窄场景成熟（ISO9409-1 A50/A80/A100/A160→STL/OpenSCAD/CadQuery STEP+安全红线），瓶颈是输入池仅 21 个声明法兰实体；②配件设计 `designer.html`=可运行原型，但模块库是硬编码 demo（源码1850+ `modules:[...]`）未接真实 `entities.json`；③电子/PCB=全仓零证据，**缺失**。
- **"类似平台"证据**：工作区点名的最接近活竞品是 **orobot.io**（带联盟分成的"零件市场"，非兼容引擎）；另有 `平台嫁接方案` 的嫁接目标平台（Product Hunt/HN/知乎/掘金等）。未 100% 确认用户所指，已请其点名。
- **扩面工作流建任务清单 #79–#86**：W1机械骨干品类/W2平台法兰扩面/W3标准派生固化/W4 OSS元数据(均 AI 自主)；W5 Builder BOM 摄取/W6 厂商共建/W7 草稿发+度量(designer接真实目录 #86 属 W1 子集)。诚实上限：AI 自主能把机械/标准覆盖拉到两位数，但到不了兼容平台的 50%+，须靠 W5/W6 真实数据（硬阻塞）。
- 报告：`ops/roboparts-ecosystem-audit-20260818.md`。待拍板：启动 W1+W2？platforms 扩面是否部署？W5/W6 名单？designer 接目录？

## W2 平台法兰扩面 — 一致性收口（17:5x 续做）
- 用户说"按照你计划推进吧" → 续做 W2 收尾。先修被错误知识库污染的 `flange_designations`。
- **关键发现：ISO 9409-1 命名歧义**（兼容性裁决命门）。机器人行业 `A{n}` 的 n = **节圆直径 PCD**，非 ISO 外径 D。同一 A 标号几何可能不同：A80 既可能 ISO 标准 `6×M8`(UR20/UR30) 也可能 ABB 老款 `4×M8`；A100 既可能 ISO 标准 `6×M10` 也可能 KUKA `4×M8`（OnRobot/Gimatic 确有转接盘）。旧 `iso-9409-flange.html` 公然写 "A80-4-M8 / A100-6-M10 / A160-8-M12 四大标准尺寸族" 是**错误事实**，且与重建后的 KB 自相矛盾。
- **核实手段**：WebSearch 抓 graspmonkey 的 ISO 9409-1 标准表 + ABB/Hub 交叉验证，确认 canonical 梯级（PCD 命名）：A40=4×M6、A50=4×M6、A63=6×M6、A80=6×M8、A100=6×M10、A160=8×M16、A250=8×M20。KUKA A100-4-M8 / A160-4-M12 / A250-4-M16 为真实厂商偏离（有转接盘佐证），非标准梯级。
- **改动文件（均本地、未部署）**：
  - `scripts/rebuild_flange_kb.py` 重写：KB 只记实测真实 (PCD,孔数,螺纹) 组合，每条打 `is_canonical_iso` 标记 + 偏离 `note`；附 `canonical_ladder_iso_9409_1` 与 `iso_9409_1_naming_convention`。现 9 条（4 canonical: A40/A50/A63/A80；5 偏离）。
  - `api/platforms.json`：50 个 `HUB-` 条目（44 declared + 6 partial → 升 2 个无歧义 PCD 为 declared：KR10 R1100-2→A40-4-M6、LBR iiwa 7 R800→A63-6-M6）；剩 4 个真 partial（CR-35iB PCD100 歧义 + 3 个 Yaskawa 无几何）诚实留空。
  - `adapter-generator.html`：PRESETS 对齐 KB（A80→6×M8、A100→4×M8、A160→4×M12，加 A20/A31.5/A40/A250）；修预览 `pinPCD` 当半径的渲染 bug（应为直径）。
  - `adapters/gen_adapter.py`：PRESETS 同步对齐（保持 parity）。
  - `iso-9409-flange.html`：meta/OG/Twitter/JSON-LD 错误"四大尺寸族"改为正确 canonical + 偏离说明；正文表格由 `scripts/rebuild_flange_page_table.py` 从 platforms.json 现生成（防漂移），加命名歧义说明段；FAQ 错列 IRB1100/IRB1200 为 A50 已改；日期 08-05→08-18。
- **跨文件一致性校验**：50 个 HUB 平台中仅 4 个真 partial 未解析到 KB（预期内），其余全部命中。
- **部署**：按纪律"推 GitHub ≠ 上线"，本地改动**未部署**；需用户批准真实 `node scripts/deploy.mjs` + `verify_live_numbers.py` 回探后再上线。
- MEMORY.md 已按注入截断要求精简重排（去重 + 新增 "W2 平台法兰扩面关键结论" 段，移除硬编码易过期计数）。

## 真实部署启动（15:16）
- 用户授权"就按照你说的推进把" → 跑真实 `node scripts/deploy.mjs`。环境已核实：OAuth 登录 61960005@qq.com（pages write 权限），令牌存 `C:\Users\xing\AppData\Roaming\xdg.config\.wrangler\config\default.toml`；用 `WRANGLER_BIN` 锁托管 wrangler 4.120（避 npx 4.123 误报 project 不存在的坑）。`python` 解析到托管 3.13.14。
- **发布范围（工作树直传 Pages，不依赖 git commit）**：W2 五文件（`api/platforms.json` 90 条含 50 HUB、`api/mechanical_interfaces.json` 9 条含 canonical 标记、`adapter-generator.html`+`adapters/gen_adapter.py` PRESETS 对齐、`iso-9409-flange.html` 从 platforms.json 现生成表格+修正错误"四大尺寸族"）；`api/entities.contrib.json`（2026-08-17 遗留的领 key 入口 access 块，属实内容，一并发布）。
- **保持 diff 干净**：`api/entities.json` 仅纯重排（708 实体语义零改）已 `git checkout` 还原；`mcp-server/` 仅 4 文件被追踪、其余 2351 个 gitignored，上传体积极小。
- 后台 task `yOHGVk`，deploy.mjs 自带线上校验（私有路径 404 + 实体数/字段指纹一致 + 页面可用性 + HEAD 探活，约 107s 退避）。待回探确认后收尾（present + 必要时 verify_live_numbers.py）。

## 初始纲要符合性盘摸（17:00）— 用户要求"回看初始资料、盘摸符合/偏离/缺失"
- 交付 `ops/roboparts-conformance-review-20260818.md`：以 README(2026-08-03 原始 manifesto)+llms.txt 为基准，facts 重算（api/entities.json）对标 5 目标/3 原则/5 功能/差异化。
- **结论摘要**：方向（兼容性/标准情报）始终正确且是真实产业痛点；但执行层持续偏离——(a) 兼容性赖以成立的数据几乎为空（mech 完全 declared 仅 2/358=0.56%，标准符合度 11.72%，TierA 12.57%）；(b) **同一事实全站自相矛盾**：ISO 9409-1 法兰几何在 11+ 处写错"四大尺寸族"（A80-4-M8/A100-6-M10/A160-8-M12），与已修 KB 矛盾。本次把全站拉齐到 canonical 梯级 A50-4-M6/A80-6-M8/A100-6-M10/A160-8-M16（含 KUKA 偏离 A100=4×M8、A160=4×M12 单列）。
- **修正文件**：llms.txt(54/101/107)、copilot.html(PRESETS+示例)、parts-viewer.html、adapters/README.md+example_adapter.scad、roboparts-dataset/scripts/gen_adapter.py（第二份副本）、promo-poster.html、scripts/build_flange_page.py（防再注入）、scripts/build_articles.py、skills/README.md、agent-discovery.json、iso-9409-flange.html(JSON-LD keywords+TOC)。
- **缺失方面**：机械骨架品类（电源/减速器/夹爪/结构件/线缆/控制器/PCB）未建一等类目；零流量度量；生态化闭环空（零用户/厂商）；designer.html 模块库仍硬编码 demo（#86）。
- **前次部署失败根因定位**：`WRANGLER_BIN` 误用 MSYS 路径 `/c/Users/...` 传给原生 Windows `node.exe` → 被当成 `C:\c\Users\...` 找不到模块。正确写法是原生路径 `C:/Users/...`。已用正确路径重跑部署（后台 task `hRpGFu`）。
- **部署结果(17:12) EXIT=0 已上线**：wrangler 4.120.0 上传 36 文件（955 已上传）；线上校验全过（私有目录 404 拦截 + 实体数/内容指纹一致 + 关键页面正常）；推广脉冲：IndexNow 200、百度推送 200(remain0/success10)、Bing 410、mcp.so/LobeHub/llmstxt.site HEAD 200。预览 https://6ae9e515.robotparts-924.pages.dev。**社区分发(知乎/CSDN/Reddit/HN/X/LinkedIn/ROS Discourse)仍待人工发稿**（9 草稿在 ops/promotion/OUTREACH_DRAFTS.md，需先跑 scripts/regression.py 过 L1.29 闸门）。

## W1 机械骨架类目扩展 + Matrix 样本实体（21:54 实施并部署）
- 用户"按照你说的实施把" → 实施 W1：CANONICAL 11→19，新增 integrated_joints/reducers/controllers/grippers/structural/cables/power/pcb 8 类（中文别名映射）；登记 Matrix（矩阵超智）2 实体 ORG-matrixrobotics(tier A, 深链)/IJ-MATRIX-1(integrated_joint_module, composition 6 子部件脚手架, tier C)，不编零件级规格。
- **W1 引入漂移全部清零**：`govern_source_tier.py`(A=92/B=210/C=408)、`getKeySpecs` 补 integrated_joints case、`normalize_categories.py` 重写 data.json(含 710 含 Matrix)/全分类 JSON/data.js 并**从快照恢复 platforms.json 为 W2 90 台机器人硬件**(防撞名事故)、`inject_onboarding.py` 补英文 entities 别名(api-pricing 708→710)、`enrich_provenance.py`(traceable 92/12.96%)、README/llms.txt(溯源分级 392→92 纠错)/content 草稿/OUTREACH 708→710 且 11→19、training_dataset.json 重导出 v2。
- **真实部署 EXIT=0 已上线**(aa36e52b.robotparts-924.pages.dev)：线上校验全过；Live 实测 data.json total_entities=710(含 Matrix)、entities.json total=710/19 类目。
- **剩余 19 项回归失败均为历史既有债务**(与 hRpGFu 同批，非 W1)：mcp.js 写死 824、4 页 favicon、`semantic-search.js` 缺 HEAD、`_LATEST` 落后、过期占位报告、BUSINESS_TOOLS/skills 漂移、meta.clean 607≠609、mech_pct 1.68%、**platforms.json 双语义撞名**(90 机器人 vs 41 实体类目 → cat_sum=759，建议改名 api/robot_fleet.json 作 W2 收尾)。
- 工作树 79 文件改动未 commit（github 被墙，部署走 wrangler 直传 Pages，不依赖 commit）；deploy.mjs 已留快照 ref `refs/roboparts/deployed/20260818-215422`。如需 push 走 `scripts/gh_push.py`(Contents API)。

## W2 收尾 + 回归 remediation（本回调填，22:28 部署）
- **W2 真值（本地工作区，待部署）**：全库 **729 实体 / 19 类目**，7 个机械类目(reducers/controllers/grippers/structural/cables/power/pcb) 用 19 个真实厂商实体填实；platforms.json 撞名修复 → 90 台机器人迁 `api/robot_fleet.json`，platforms 归位 41 实体类目，cat_sum 778→729。
- **回归 remediation 13 → 4 项红**：本轮回填 W2 引入 4 项(meta.mechanical_interface_coverage 重算 not_applicable 350→352/not_declared 352→371/applicable 377/fill_pct 1.59；entity_kinds.component 510→529；mcp getKeySpecs 补 7 类 case=key_specs 全品类非空；OUTREACH 710/688→729) + 廉价历史债务 6 项(BUSINESS_TOOLS+3/skills.meta.json+3 技能并 regen manifest·README·agent-discovery·.well-known/agent.json/mcp.js 去写死 824/semantic-search.js 补 onRequestHead/bom-import·compatibility-viz·scoreboard·standard-audit·waitlist 5 页 favicon/geo+github-sponsors 710→729)。
- **机读留痕补齐**：_LATEST.md 重写(运行时间 2026-08-18 22:28) → digest_due.py --stamp 机械盖 TODO-COUNT:2 + DIGEST-CLAIM:issued；补发逾期日报 _DAILY_DIGEST-20260818.md；_SUMMARY.md 加 20260818-22 行。
- **剩余 4 项回归红(242/441/1099/1100)均为历史占位报告/摘要债务**，与 W1/W2 数据正确性无涉，列入 Task #89 sprint（不删历史行以免破坏 ratchet 审计轨迹）。
- **真实部署：⚠️ 未验证/阻塞（非已上线）（2026-08-18 夜复核）**：那轮 `node scripts/deploy.mjs` 进程被杀、快照 ref 未写入，且沙箱对 Cloudflare 完全失联，故「EXIT=0 已上线」属未证实乐观写入，已订正下游报告(_LATEST/roboparts-20260818-22)与本日志。本地 L2 七处总数一致=729（entities.json=data.js=分类JSON求和=README=llms.txt=agent-discovery(含散文 query 729 entities)=api/data.json）；agent-discovery 散文 710 漂移已补正并固化到生成器。线上一致性待网络恢复后重跑部署并以 curl 回探确认。
- 79 文件工作树改动仍未 commit（github 被墙，部署走 wrangler 直传 Pages）。

## W2 部署状态复核 + 收尾加固（本轮回填，网络阻塞）
- **诚实订正**：08-18 夜那轮 W2 部署实为**未上线**——后台 node 进程被杀、`.git/refs/roboparts/deployed/` 快照 ref 未生成、且沙箱对 Cloudflare 完全失去网络出口（example.com/api.github.com/cloudflare.com 均 `fetch failed`），无法回探或重传。原「EXIT=0 已上线」是未证实乐观写入，已订正 _LATEST.md / roboparts-20260818-22.md / 本日志 W2 段。
- **发现并修复 agent-discovery.json 散文 `query 710 entities` 漂移**：回归 L2 只校 `total_entities` 字段、漏掉散文计数 → 假绿（此前误报七处全 729）。根因修复：`gen_skills_manifest.mjs` 现从 `api/entities.json` 真相源现算回写 `total_entities` 与 `ai_agent_instructions` 散文计数，regen 即保鲜，杜绝再漂移；已重跑生成器确认 `query 729 entities`。
- **加固 deploy.mjs verify**：5 处校验 `fetch` 全部加 `AbortSignal.timeout(25000)`（新增 `fetchT` 包装），避免 Cloudflare 连接挂死时无限等待（即 18 分钟卡死根因）；`node --check` 通过。
- **当前阻塞**：沙箱网络出口故障，W2 仍非线上。网络恢复后由我重跑 `node scripts/deploy.mjs`，并以 `curl --ssl-no-revoke --tlsv1.3 https://roboparts.cc/api/entities.json` 确认线上 `total_entities=729` / 19 类目方算闭环。回归其余 4 项历史债务(242/441/1099/1100)仍属 Task #89 sprint。
