# 2026-08-10 工作日志

## 09:00 飞轮续跑（接 run-62 机械维度收尾）
- 修复上轮因新增 SENS-31 真实声明触发的 6 项回归漂移，回归 EXIT=0。
- **L1.74 探针退化修复**：原 `tt` 计全库所有兼容对，补 SENS-31 后断言 `==0` 变红（闸门退化成禁止本维度有数据）；改为只数「无身份键支撑的假绿」→ 0 假绿 / 1 合法对（ACT-028×SENS-31，生产兼容引擎实跑 compatible=true）。
- 派生接口重生成：normalize_categories（data.json+10 分类 JSON+data.js）、inject_api_access（26 JSON honest_limits+openapi）、inject_onboarding（19 页 mech_pct）→ 全部刷新至 1.68%。
- 新增 **L1.78** 闸门（机械 declared 的 standard 编码不得凭空造，14 项全绿）。
- 部署 DEPLOY_EXIT=0（线上校验通过）；提交 f66704a → 远端 ae44215e（tree 自校验一致，经 push-gitdata）。
- 上报：roboparts-20260810-09.md + _LATEST.md 覆盖 + _SUMMARY.md 替换占位行；无日报（<24h）；待办 7 项无新增，P0=ROS Discourse 投稿。临时 .tmp/mechpair.mjs 已清。
- 纪律：未用 TaskCreate/TaskList，未建新对话框。

## 10:00 飞轮第 63 次运行
- **DIGEST-CLAIM 由手打改机械盖章**：`scripts/digest_due.py` 新增 `claim_line` / `stamp_latest`（幂等）/ `check_claim`，`CLAIM_RE` 收为唯一识别器；`regression.py` L1.58 改为引用 `dd.CLAIM_RE` 不再另抄。新增 CLI `--stamp`（写完 _LATEST.md 后盖章）与 `--verify-latest`（同轮自查）。**飞轮流程变更：以后写完 `_LATEST.md` 必须跑 `python scripts/digest_due.py --stamp`，不要手打那行标记。**
- 6 项阴阳对照证明盖章不放水（到期盖 skipped 被拒 / 陈旧盖 issued 被拒 / auto 0.5h 严于校验 3h / 幂等）。
- **标准登记表 9→10**：补录 T/BTIAIRI 0001-2025《人形机器人电驱动一体化关节接口要求》（ttbz.org.cn 详情页为证据，2025-12-25 实施、现行）。
- 回归 EXIT=0 / 部署 EXIT=0 / 线上 17 页+3 接口 0 未核验；提交 f3eb458 → 远端 917c0ba3；ops 仓 2fec257。

## 11:00 用户问「飞捷科思（物理AI原生平台）对项目有帮助吗」
- **评估结论：不匹配，不投入。** 飞捷科思=Fysics AI（fysics.net），张立华（原英伟达PhysX奠基人）创立的物理智能基础设施公司，2024-08 成立，2026-08-06 刚完成 A1 轮数亿元融资；五大产品 Fysics/MoziSim/OmniFysics/Fysiverse/FysiData；牵头物理智能创新联合体 CPii（32 家）。
- **为何不匹配用户需求（资金+算力、大厂/模型公司主办）**：它是刚融资的"选手"不是"主办方"，不发奖金/算力、无公开竞赛通道；CPii 为邀请制联合体。
- **技术层**：与 RoboParts（兼容性数据层）相邻非互补；且 FysiData 方向是潜在数据层竞争者，需盯。
- **唯一非资金角度**：B2B 数据合作（喂兼容性数据给 MoziSim）属慢商务、不带资金；其投资人（云启资本等）可作独立 VC 线索。
- **待办**：用户要的"GOAI 出局后改找其他大厂/模型公司竞赛"清单未整理完（上轮被 429 打断：字节Coze/华为昇腾/阿里云通义/腾讯云启/AI Grant/Gemini XPRIZE 待核实窗口）。

## 12:15 用户问「AI物理世界训练机器人是一个方向，对项目有什么帮助」
- **评估**：physical AI / 具身智能 / 机器人基础模型是趋势，对 RoboParts 是**需求顺风 + 融资叙事顺风**，但**不是应自建的能力**（属 Fysics/NVIDIA Isaac 地盘，pivot 即失守数据层护城河）。
- 真实杠杆：①对外叙述贴"物理AI机器人构建者的数据底座"以命中物理AI主题竞赛（当前最肯砸钱）；②加 training-data-ready 导出（组件+兼容性图+标准标签结构化），让 MoziSim/Isaac 训练平台直接消费；不做仿真引擎/世界模型。
- 连接待办：下一轮查竞赛渠道优先筛"具身智能/物理AI/机器人"主题赛事（CAAI具身赛道含机器人零配件、HICOOL/摩尔线程具身赛道等）。

## 12:23 用户指示：竞赛渠道搜索暂搁置，先做「training-data-ready 导出」具体动作
- **用户原话**："这个事情暂时放弃。真正该做的（一个具体动作）这个先做" —— 指 12:15 提出的「把兼容性数据做成 training-data-ready 导出，让物理AI训练平台直接消费」。
- **已实现**：新建 `scripts/export_training_dataset.mjs`，复用 `functions/_lib/compat_engine.js` 的 `judgePair`（单一真相源，不另写判定），生成 `api/training_dataset.json` 三件套：
  ① components（509 实体，含 standard_conformance 标准标签 caee060_relevant/iso22166_relevant/bus_class/ros2 + evidence 等级）
  ② compatibility_edges（judgePair 现算、只保留 decided_dimensions>=1 的有证据边，剔除 25 万级 undecided 噪声）
  ③ standards 登记表（10 条，来自 entities.json meta）
- **实跑结果**：components=509 / selectable=453 / pairs=102378 / edges(decided)=7170（compatible=2591 / conflict=4579）/ 文件 ~10MB。已校验结构正确（样本边 ACT-001×ACT-002 兼容、ACT-001×ACT-003 冲突，符合预期）。
- **部署中**：后台跑 `deploy.mjs` 上线 `api/training_dataset.json`（静态资源，训练平台可按 https://roboparts.cc/api/training_dataset.json 直拉）。等待线上回探验证。
- 用户确认"都做吧"：①已加 MCP resource ②已在 data-hub.html + mcp-guide.html 提及训练数据集。
- **MCP resource（functions/mcp.js）**：新增 RESOURCES 常量（uri=https://roboparts.cc/api/training_dataset.json），initialize 能力声明加 `resources:{listChanged:false}`，`resources/list` 返回该资源、`resources/read` 拉取文件仅回传 meta 摘要+直链（不返 10MB 全文，避免撑爆响应）。GET /mcp 自描述文档加 `resources` 字段。语法校验通过。
- **页面提及**：data-hub.html 在「全量导出」后新增「物理 AI 训练数据集」卡片（下载/说明双链接，无写死数字，遵守页面数字纪律）；mcp-guide.html 新增 `#training-dataset` 锚点章节（含端点 + 经 resources/list 自动发现说明）。
- **部署完成并线上回探全通过**（zgSGJS，1m39s）：① `POST /mcp` resources/list 返回训练数据集资源 ✅ ② GET /mcp 自描述文档含 `resources` 字段 ✅ ③ /data-hub 与 /mcp-guide 均含新文案与锚点 ✅ ④ /api/training_dataset.json 仍 200（~10MB）✅。
- 旁注（未修，待用户定）：mcp-guide.html 的「5 个工具」仍列旧名（component_search/get_component_detail/check_compatibility/recommend_components/export_bom），与 mcp.js 真实工具名（search_components/get_component_detail/check_compatibility/recommend_for_application）漂移；真实以 mcp.js TOOLS 为准。
- **13:47 顺手修掉上述漂移（用户授权"你决定"）**：mcp-guide.html 工具清单按 mcp.js TOOLS 真相源对齐——`component_search`→`search_components`、删不存在的 `export_bom`→改真实第5工具 `get_parameter_semantics`、`recommend_components`→`recommend_for_application`；参数名同步真实 schema（search_components 用 category/keyword/limit/include_market_intelligence；check_compatibility 用 component1_id/component2_id；recommend_for_application 用 application/budget/count）；正文 "component_search"→"search_components"、"BOM 导出"→"参数口径规范"。部署 AyacGQ 通过，线上回探 5 正确名全在、3 错误名 0 残留 ✅。

## 14:00 飞轮 run-65（自动化 automation-1785907133940）

- **修**：训练数据集是「有出处的过期快照」——12:25 手工生成，12:26 那轮登记表 10→11，线上 `standards` 定格 10 条；已重生成+部署，线上 11 条。
- **修**：导出器整份重写会抹掉 `meta.access`（L1.55 复发）→ 收尾真调 `inject_api_access.py` 且校验 returncode，注入失败即导出失败。
- **修**：`entity_kind` 只在内层且 `|| 'component'` 凭空默认 → 提到记录顶层、逐字照抄真相源、删内层第二份。
- **入库**：data-hub/mcp-guide/functions/mcp.js + 生成器（此前只活在工作树，生产不可复现）→ 提交 afc99e8 → 远端 0d1f9bf7（tree 553098f7 自校验一致）。
- **新增闸门 L1.80**：对外数据集必须是真相源当期快照（实体总数/登记表条数/机械声明率现算比对 + 引用与产物成对 + 导出器必须挂部署链路），5 阳 1 阴对照。
- **约定**：`api/training_dataset.json`（10MB 纯派生物）已 gitignore —— **入库的是生成器不是快照**；部署由 `deploy.mjs` 0b2 无条件重生成，一致性由 L1.80 兜底。
- **团标召回缺口找到合规入口**：`ndls.org.cn`（国家数字标准馆）robots 仅禁 Bytespider/YisouSpider，详情页 200 且字段齐 → 可替代被 Disallow 的 ttbz。**尚未接进枚举器**；且发现渠道 ≠ 取证渠道（hash 型 URL 稳定性未验证，取证仍回落 openstd/ttbz）。见 `ops/intel/tuanbiao-enumeration-path-20260810.md`。
- 情报 4 条全淘汰；T/CMAP 008-2026 是两因子触发器的一次实用（层面命中、领域未命中，单因子会误收）。
- 回归 EXIT=0；部署 EXIT=0；线上 17 页 + 3 接口 0 未核验；日报未到期（21.65h，机械盖章 skipped @14:09）；待办仍 7 项，P0=ROS Discourse 投稿。未使用任何任务清单工具。

## 19:00 第 68 次飞轮

- **枚举器翻页静默失效（新的一类踩坑）**：`std.samr` 忽略 `current` 参数 → 每个检索词反复返回同一批前 50 条。日志「拉到 200 条」而 total=183，"实收 > 自报总数"这个矛盾摆了三周没人核。停止条件 `page*size>=total` 恰好把假象读成"拉满"。改 `pageNo` 后扫描面 164→434。**教训：枚举器有 bug 时，召回天花板是「第一页」，且不会报错**——这是 8/10 那条"关键词召回天花板=曾被报道过"的下一层。
- 修法配 3 道自证闸：首条与上页重复即 raise、触深度上限但 total>已收即 raise、实收≠total 告警。凡分页抓取一律照此配。
- **登记表 11→15**：补 GB/T 14468.1/.2-2006（工业机器人机械接口 板类/轴类，ISO 9409-1/-2 国标对位）、GB/T 32197-2025、GB/T 33266-2016；rejected 4→7。四条均标"不进兼容判据"（只有题录未核原文，不拿旁证补正文）。
- **台账留痕再深一层**：只存命中项 = 无法区分"筛掉了"与"没看见"。加 `scan_index`（整个扫描面）+ `--why CODE` 质询入口。实测 GB/T 43200-2023 = 领域命中/层面未命中 → 正确淘汰（证伪了我原本的漏检猜测）。
- **git 身份为空是隐性阻塞**：本仓 `user.name/email` 未配置（历史重写后 `.git/config` 被重置），63～67 轮"改完没提交"实为**提交必失败**。已配 `RoboParts Flywheel <support@roboparts.cc>`（仅本仓）→ 提交 bbf7fa6。**凡发现工作树连续多轮累积未提交，先查 git 身份/hook，别默认是"忘了"。**
- 新增回归闸 L1.81（留痕闭环 13 项）；`digest_due.py` 收为留痕唯一源。L1.80 实测抓到 training_dataset 过期快照（非空转）。
- 线上：部署 3/3、verify_live 17 页+3 接口 EXIT=0、probe 24/24。待办 8 项不变。

## 20:00 第 69 次飞轮运行 · 逐来源静默计时闸
- 修 L1.79：健康判据从"看台账最后一条"改为"逐声明来源各自计时"。根因是聚合式判据让勤快来源(gb)替哑掉来源(tuanbiao)遮丑；团标源至今 0 次成功枚举却全程绿灯。
- 查明 `enumerate_standards.py` 无任何调度器调用（跑不跑靠模型记性），改由每轮回归打印到期/冷却/从未成功提示。
- 新增 `--due` 只读入口；阈值收唯一源（regression importlib 引用枚举器）。
- 提交：主仓 f97c56a / ops 仓 6bf6b8e。回归 EXIT=0、probe 24/24、线上 17 页+3 接口全绿、无漂移未部署。

## 22:19 git 历史清洗收尾（用户授权"可以清掉"）
- 流程：bundle 备份(`backup-robopart-pre-filter.repo` 4.2MB，桌面保留) → 干净克隆重跑 filter-repo(13 个 ops/ 路径 invert) → 清洁 .git 装回 robopart → reset 保留工作树。
- 最终核验：13 份在历史中**全部为 0**；origin 接回 github.com/lm203688/roboparts.git；18 个未提交改动完好；临时目录已清。git 历史清洗**确认完成**。
- 后续若要推远端需走 Git Data API（github 协议被墙，--force 直推不通），暂未推。

## 22:19 ω-0 新闻价值评估（用户问"对项目有参考提升吗"）
- ω-0=PKU×NTU×BAAI×HKUST(GZ) 的隐空间预测世界-动作模型（人形边走边干活）。
- 结论：非客户/非可用技术/不带资金算力；但**强验证"物理 AI 数据底座"叙事**。价值在借势(GEO 文章)，不在接入/合作。
- 建议：写一篇 ω-0×兼容性数据层文库文章(articles/ + 公众号)；列入物理 AI 观察清单；不因此重启竞赛搜索。

## 23:00 第 71 次飞轮运行 —— L1.83 meta 文案数字锚点闸
- **修复**：线上 `api/entities.json` 的 `meta.provenance_coverage` 里，机读字段 `traceable_pct: 12.85` 与紧邻文案「真值 9.46%」并排打架（9.46% 是 run-70 重算中途未合并 L1.70 例外的中间值）。生成器 enrich_provenance.py 手打了这个数，而 regression.py 注释写的是对的 12.85 —— 上线的是错的那份。改为 `build_provenance_note()` 渲染：历史值读 quality-baseline 新增字段 `superseded_value`，当期值取现算，函数体零字面量。已部署上线并回探，9.46% 从生产消失。
- **新增 L1.83**（regression.py）：meta 全部文案里的百分比必须挂得住锚 = 当期现算值 ∪ 台账 `_traceable_history` 记录过的历史值，两者皆非即判红。13 项对照（9.46 判红/凭空 33.33 判红/台账 47.06 不误伤/阈值 0.30·0.6 与条数 91 不误伤）。
- **自证参照系教训（新）**：L1.83 首版用 `git show HEAD:api/entities.json` 当"修复前原版"做非空转自证 —— 提交修复前绿，**提交那一刻 HEAD 变成修好的版本，自证蒸发并永久判红**，会卡死后续每次发布。改为冻结真实上线原文做样本 + 两道护栏（样本须保有故障特征、当期 note 不得回滚出现 9.46）。**规律：自证必须锚在冻结样本上，不能锚在会移动的引用（HEAD/latest/最新一条）上。**
- **操作纪律**：`_SUMMARY.md` 摘要行必须走 `python scripts/digest_due.py --append-summary --hour HH --fixed/--improved/--todo`，它按档位幂等替换、会自动收敛 deploy 落的 AUTO-STUB；本轮先用裸 printf 追加导致同档并存（同型第 3 次，机制早就有，缺的是用它）。
- 提交：主仓 83affb7 + e8bf3bd；ops 仓 d7297ee + be4bcd7。回归 EXIT=0 / probe 24/24 / 线上 17 页+3 接口 0 未核验 / 708·325·15 无漂移。
