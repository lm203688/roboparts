# 情报归档 2026-09-15（供应链 / 竞品 / 基准）

- **归处裁定**：RoboParts（用户 2026-09-15 拍板，非 SwarmLabs）
- **纪律**：先核实一手来源，AI 不编；声明类数据须官域 `source_url` + 白名单，本文仅作情报，不升 `declared`。
- **核实日期**：2026-09-15，四条均有一手来源，无编造。

---

## 1. 鑫精诚（XJC Sensor）六维力传感器 — 供应链候选

**一手来源**：官网 `xjcsensor.cn`（深圳市鑫精诚传感技术股份有限公司）；腾讯新闻 / 东方财富 / 科创板日报 2026-09-14~15 报道。

**已核实事实**：
- 产品线：六维力传感器（6F 系列，匹配人形机器人手腕安装标准）、关节扭矩传感器、多维力、MEMS 六维力、张力、压力。
- 双技术路线：金属箔式应变计 + 玻璃微熔 MEMS 应变芯片。
- 2026-09-15 完成**数亿元 B 轮**（上海电科基金领投，深创投追投）。
- 2026 Q1 六维力 + 关节扭矩合计出货 **>10 万只**；供应链覆盖 **>200 家**（关节模组 / 灵巧手 / 人形 / 协作）。
- 2026 WRC 展出全球最小电阻应变式六维力（Ø5.5mm × H12.6mm）、微型关节扭矩 Ø10mm（中空 6mm）~14mm。
- 产品型号 **680+**。

**RoboParts 关联**：F/T 传感器属 EOAT 组件，可扩展「传感」类目。

**诚实边界（AI 不编）**：
- 未核实其传感器**具体安装接口**（是否 ISO 9409-1 法兰？螺纹规格？）。RoboParts 兼容纪律以安装接口为准，无核实接口数据 → **不能声明 compat**。
- 官域 `xjcsensor.cn` **不在**当前 `MECH_SOURCE_HOSTS` 白名单 → 即使要声明也须先用户拍板加白名单 + 核实官域 `source_url`。

**已实施（2026-09-15，用户授权「按计划全部实施」）**：
1. ✅ `xjcsensor.cn` 已加入 `scripts/regression.py` 的 `MECH_SOURCE_HOSTS` 白名单 —— 仅为「允许引用」的扩容，**白名单里有这个域 ≠ 已收录任何 declared 条目**。
2. ✅ 鑫精诚已作为**供应商种子**收录进 `api/suppliers_seed.json` → `by_category.sensors`（名称 + 官域 + 真实事实注记）；**未**作为零件实体入库、**未**声明任何接口。
3. ⏳ 官域安装接口数据我无法代审 —— 仍须用户/厂商核实后，才可能在有官域深链出处的前提下升 `declared`。

---

## 2. Archify — 工具 / 竞品情报（含可借鉴点）

**一手来源**：`github.com/tt-a1i/archify`（MIT）；betterstack / cldnavi / productcool 测评。

**已核实事实**：
- AI agent skill：把代码库 / 系统描述 → **可验证**的交互式架构图（自包含 HTML/SVG）。
- 核心范式：agent 生成 **typed JSON IR** → 用「repo evidence」对照真实代码校验 → 确定性编译器渲染；**fail-closed**（编造组件直接报错不渲染）。5 类图：架构 / 工作流 / 序列 / 数据流 / 生命周期。
- 适配 Cursor / Claude Code / Codex / OpenCode / Raven / DeepSeek Harness。无 API key、本地运行。
- Stars 报道**冲突**：cldnavi 称 15.1k（2026-08），betterstack 称 36k — 不采信单一值，须复核。

**RoboParts 关联 / 可借鉴**：
- 与 RoboParts「AI 不编」纪律**同源**：先结构化中间表示 + 校验 + 确定性渲染 + fail-closed，杜绝 AI 编造。
- **已借鉴落地（2026-09-15）**：把 Archify 的「typed JSON IR + 数据 evidence 校验 + 确定性渲染 + fail-closed」范式落进 RoboParts Copilot 的兼容性图：
  - `functions/api/copilot.js` 新增 `buildCompatIR()` / `irGrounding()` —— **规则引擎**（非 LLM）产出**已校验 IR**：节点/边必须被 canonical 法兰梯级 / 用户显式三要素 / 平台实体库 / 可构造的转接件 URL 背书；无背书项进 `dropped[]`。IR 同时喂给模型（锁死可引用节点集，禁止越出）并回传前端（`ir` 字段，三个响应分支均携带）。
  - `copilot.html` 新增 `buildCompatIRLocal()` + `renderCompatGraph()` —— 确定性 SVG 渲染（同一 IR 必出同一图），并如实展示 `dropped`（fail-closed 透明）。
  - 自测：14 项断言全通过（含 fail-closed 无 URL 不画节点、同 IR 渲染一致等阴性对照）。
- 作为文档工具：RoboParts 架构文档可用 Archify 生成可验证图。

**诚实边界**：Archify 是文档 / diagram 工具，非机器人零件；归「竞品 / 工具情报」，不入零件数据。

---

## 3. MINERVA — 基准 / 研究情报（小模型容量地板）

**一手来源**：`github.com/k1000dai/MINERVA`（Apache 2.0）；arXiv 2609.03715（2026-09）；HuggingFace `k1000dai/MINERVA`；humanoidintel.ai 解读。

**已核实事实**：
- MINERVA = **MIN**imal **E**fficient **R**obotic **V**ision-**A**ction policy，0.54M 参数紧凑操作策略，面向 LIBERO 40-task 闭环基准。
- 结果：4 套件平均 **95.75%**（L1 head），仅比 π0.5（4.1B）低 2.4 点，用 **7,700× 更少参数**；笔记本 CPU **5–9 ms/chunk**，无需 GPU。
- 容量地板：~1M 饱和，<0.25M 崩溃。
- 局限：task-ID 条件，**非通用 VLA**，不能执行未见指令；LIBERO-Plus 扰动降到 **46–56%**（光度鲁棒 ≈0）。
- 关键发现：LIBERO 指令条件主要选「记忆任务」（task-ID 置换探针降到近随机）→ 基准记忆动态；flow matching 相对 L1 回归无 detectable 优势。

**RoboParts 关联**：
- 支撑 RoboParts「零件级判定可用小模型」叙事：有界操作任务 0.54M 即够，呼应 RoboParts 轻量 Copilot 路线。
- LIBERO 饱和 + 鲁棒性差 → 研究空白信号（与 SwarmLabs 科研实体「研究空白」同源，但本次归 RoboParts 作基准情报）。

---

## 4. LIBERO — 基准情报（标准套件已饱和）

**一手来源**：百度百科 LIBERO 词条；paperswithcode LIBERO benchmark；CSDN LIBERO 详解；各模型榜单报道。

**已核实事实**：
- LIBERO：终身机器人学习基准，4 套件（Spatial / Object / Goal / Long）+ LIBERO-100（90+10），基于 RoboSuite / MuJoCo。
- SOTA 已饱和：LaST-R1 **99.9%**（1 条轨迹预热 + 在线 RL）、越疆 DobotWAM **99.25%**、π0.5 97.5、MINERVA **95.75% @0.54M**。
- 扩展 LIBERO-Plus（OpenMOSS，7 维扰动）暴露鲁棒性缺口：MINERVA 掉到 46–56%。

**对用户「libero 没难度」的核验结论**：
- ✅ 标准 LIBERO 四套件确已饱和（顶尖模型 >99%）→ 「没难度」在标准口径成立。
- ⚠️ 但鲁棒性扰动版（LIBERO-Plus）仍难（46–56%），真实硬件 / 光度偏移远未解决 → 「没难度」仅限标准基准，**非通用操作能力**。

**RoboParts 关联**：基准情报，佐证「基准驱动的规模 ≠ 部署必要规模」，支持 RoboParts 轻量判定路线。

---

## 诚实汇总 / 待用户拍板

- 本文 4 条均经一手来源核实（2026-09-15），无编造。
- 仅 **鑫精诚** 触及 RoboParts 零件数据边界，但缺官域安装接口核实 + 白名单 → 保持 **candidate**，不升 `declared`。
- **实施状态（2026-09-15）**：
  1. ✅ 鑫精诚白名单 + 供应商种子收录 —— 已落地（见 §1）；
  2. ✅ Archify 验证式图生成借鉴进 Copilot —— 已落地（见 §2）；
  3. ✅ `scripts/push-gitdata.mjs` PAT 明文泄露已修（新增 `redact()`，抹 token 与 `Authorization` 头；阴阳自测通过）；
  4. ⏳ push / deploy —— 本次飞轮推进。
- **仍归用户的硬阻塞**：鑫精诚官域安装接口核实；是否据此升 `declared`。
